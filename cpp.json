[
  {
    "id": "cpp-what-is",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "basics",
      "language"
    ],
    "question": "What is C++ and how does it differ from C?",
    "answer": "<p>C++ is a <strong>multi-paradigm</strong> language (procedural, OOP, generic, functional) built as an extension of C. Key additions over C:</p><ul><li><strong>Classes and OOP</strong> — encapsulation, inheritance, polymorphism.</li><li><strong>Templates</strong> — generic programming (STL).</li><li><strong>RAII</strong> — automatic resource management via destructors.</li><li><strong>Namespaces</strong> — avoid name collisions.</li><li><strong>Stronger type system</strong> — references, overloading, bool type.</li></ul><p>C++ is compiled, performant, and used in game engines, browsers, databases, trading systems, and embedded systems.</p>",
    "tip": "C++ is not 'C with classes' — modern C++ (11/14/17/20) has smart pointers, lambdas, move semantics, and more.",
    "code": "",
    "lang": "",
    "revise": "**C++** = multi-paradigm (procedural/OOP/generic/functional) extension of C.\n• Adds: **classes/OOP**, **templates** (STL), **RAII**, namespaces, references, overloading.\n• Compiled + fast — game engines, browsers, databases, trading, embedded.\n• **Interview tip**: it's not 'C with classes' — modern C++ (11/14/17/20) has **smart pointers, lambdas, move semantics**; mention those."
  },
  {
    "id": "cpp-oop-classes",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "oop",
      "class",
      "encapsulation"
    ],
    "question": "Classes, objects, and encapsulation in C++.",
    "answer": "<p>A <strong>class</strong> defines attributes (member variables) and behaviours (member functions). An <strong>object</strong> is an instance of a class.</p><p><strong>Encapsulation</strong> — hide internal state behind access specifiers:</p><ul><li><code class=\"inline\">public</code> — accessible everywhere.</li><li><code class=\"inline\">private</code> — only within the class.</li><li><code class=\"inline\">protected</code> — class and subclasses.</li></ul>",
    "tip": "Keep data members private and expose behaviour through public methods — that's encapsulation.",
    "code": "class BankAccount {\nprivate:\n  double balance;\npublic:\n  void deposit(double amount) { balance += amount; }\n  double getBalance() const { return balance; }\n};",
    "lang": "cpp",
    "revise": "A **class** bundles data (members) + behaviour (methods); an **object** is an instance.\n• **Encapsulation** via access specifiers: `public` (everywhere), `private` (class only), `protected` (class + subclasses).\n• **Interview tip**: keep data members **private** and expose behaviour through **public** methods — that's encapsulation in one sentence."
  },
  {
    "id": "cpp-constructor-destructor",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "constructor",
      "destructor",
      "raii"
    ],
    "question": "Constructors and destructors.",
    "answer": "<p><strong>Constructor</strong> — called when an object is created. Same name as the class, no return type. Can be overloaded or defaulted.</p><p><strong>Destructor</strong> — called when an object is destroyed (<code class=\"inline\">~ClassName()</code>). Cleans up resources — the foundation of <strong>RAII</strong> (Resource Acquisition Is Initialization).</p><p>Compiler-generated default constructor/destructor exist if you don't define them (Rule of Zero).</p>",
    "tip": "If a class manages a resource (file, memory, socket), the destructor must release it — RAII.",
    "code": "class File {\n  FILE *f;\npublic:\n  File(const char *path) { f = fopen(path, \"r\"); }\n  ~File() { if (f) fclose(f); }  // RAII\n};",
    "lang": "cpp",
    "revise": "**Constructor** runs at creation (class name, no return type, overloadable); **destructor** `~Class()` runs at destruction.\n• Destructor releases resources → foundation of **RAII**.\n• Compiler supplies defaults if you define none (**Rule of Zero**).\n• **Interview tip**: if a class owns a resource (file, memory, socket), the destructor must release it — that's RAII."
  },
  {
    "id": "cpp-inheritance",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "inheritance",
      "oop"
    ],
    "question": "Inheritance and access specifiers.",
    "answer": "<p><code class=\"inline\">class Derived : public Base</code> — Derived inherits Base's members. Access modes:</p><ul><li><strong>public inheritance</strong> — is-a relationship (most common).</li><li><strong>protected inheritance</strong> — rare; public members become protected in Derived.</li><li><strong>private inheritance</strong> — implementation inheritance (has-a via inheritance).</li></ul><p>Use <code class=\"inline\">override</code> keyword on overridden virtual methods (C++11).</p>",
    "tip": "Favor composition over inheritance unless there's a genuine is-a relationship.",
    "code": "class Animal {\npublic:\n  virtual void speak() { cout << \"...\"; }\n};\nclass Dog : public Animal {\npublic:\n  void speak() override { cout << \"woof\"; }\n};",
    "lang": "cpp",
    "revise": "**Inheritance**: `class Derived : public Base`.\n• **public** = is-a (most common); **protected**/**private** = rare implementation inheritance.\n• Use **`override`** on overridden virtual methods (C++11).\n• **Interview tip**: **favor composition over inheritance** unless there's a genuine is-a relationship."
  },
  {
    "id": "cpp-polymorphism",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "polymorphism",
      "virtual"
    ],
    "question": "Compile-time vs runtime polymorphism.",
    "answer": "<p><strong>Compile-time</strong> — function overloading (same name, different params) and templates. Resolved by the compiler.</p><p><strong>Runtime</strong> — <code class=\"inline\">virtual</code> functions enable dynamic dispatch via vtable. A base pointer/reference calls the derived version:</p><p><code class=\"inline\">Animal* a = new Dog(); a-&gt;speak();</code> calls Dog::speak().</p><p>Requires <code class=\"inline\">virtual</code> on the base method and a virtual destructor if deleting through base pointer.</p>",
    "tip": "Always make the destructor virtual in polymorphic base classes — otherwise deleting via base pointer is undefined behavior.",
    "code": "class Base {\npublic:\n  virtual ~Base() = default;\n  virtual void draw() = 0;  // pure virtual = abstract\n};",
    "lang": "cpp",
    "deep": "<h4>vtable mechanics</h4><p>Each class with virtual functions gets a vtable (array of function pointers). Each object has a vptr pointing to its class's vtable. Virtual calls go through vptr → vtable → correct function.</p><h4>Cost of virtual</h4><p>One extra indirection per call + vtable memory per class. Avoid virtual in hot inner loops unless you need polymorphism.</p><h4>How to say it in an interview</h4><p>“Virtual functions enable runtime polymorphism through the vtable. I always declare a virtual destructor in base classes. For compile-time polymorphism, I use templates or function overloading — zero runtime cost.”</p>",
    "revise": "Two kinds of polymorphism:\n• **Compile-time** — function overloading + templates (resolved by compiler).\n• **Runtime** — **virtual** functions → dynamic dispatch via **vtable** (`Animal* a = new Dog(); a->speak()` calls Dog's).\n• **Interview tip**: needs `virtual` on the base method **and a virtual destructor** if you'll delete through a base pointer."
  },
  {
    "id": "cpp-virtual-destructor",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "virtual",
      "destructor",
      "memory"
    ],
    "question": "Why must base class destructors be virtual?",
    "answer": "<p>If you <code class=\"inline\">delete</code> a derived object through a base pointer and the destructor is not virtual, only the <strong>base destructor</strong> runs — derived members are not cleaned up (undefined behavior / memory leak).</p><p>With <code class=\"inline\">virtual ~Base()</code>, the correct destructor chain runs: derived first, then base.</p>",
    "tip": "Rule: if a class has any virtual function, make the destructor virtual. Abstract bases should have virtual default destructor.",
    "code": "Base *obj = new Derived();\ndelete obj;  // safe only if ~Base() is virtual",
    "lang": "cpp",
    "revise": "**Virtual destructor** — delete a derived object through a base pointer safely.\n• Without `virtual ~Base()`, only the base destructor runs → derived members leak (UB).\n• With it, the full chain runs: derived first, then base.\n• **Interview tip**: rule — **any class with a virtual function should have a virtual destructor**."
  },
  {
    "id": "cpp-rule-of-five",
    "category": "cpp",
    "difficulty": "advanced",
    "tags": [
      "rule-of-five",
      "copy",
      "move"
    ],
    "question": "Rule of Three / Five / Zero.",
    "answer": "<p>If a class defines any of these, consider defining all:</p><ul><li><strong>Rule of Three</strong> — destructor, copy constructor, copy assignment operator.</li><li><strong>Rule of Five</strong> (C++11) — add move constructor and move assignment.</li><li><strong>Rule of Zero</strong> — if all members manage themselves (smart pointers, STL containers), define none — compiler-generated versions are correct.</li></ul><p>Needed when the class owns a raw resource (pointer, file handle).</p>",
    "tip": "Modern C++: use Rule of Zero — wrap raw resources in smart pointers and let the compiler handle copying/moving.",
    "code": "class Buffer {\n  std::unique_ptr<int[]> data;\n  size_t size;\npublic:\n  Buffer(size_t n) : data(std::make_unique<int[]>(n)), size(n) {}\n  // no copy/move/destructor needed — Rule of Zero\n};",
    "lang": "cpp",
    "revise": "**Rule of Three/Five/Zero** — if you define one special member, consider all.\n• **Three**: destructor, copy ctor, copy assignment.\n• **Five** (C++11): + move ctor, move assignment.\n• **Zero**: if members self-manage (smart pointers, STL), define **none** — compiler defaults are correct.\n• **Interview tip**: modern C++ prefers **Rule of Zero** — wrap raw resources in smart pointers and let the compiler handle copy/move."
  },
  {
    "id": "cpp-smart-pointers",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "smart-pointers",
      "memory"
    ],
    "question": "unique_ptr vs shared_ptr vs weak_ptr.",
    "answer": "<ul><li><strong>unique_ptr</strong> — exclusive ownership. Cannot copy; can move. Zero overhead vs raw pointer. Default choice.</li><li><strong>shared_ptr</strong> — shared ownership via reference counting. Thread-safe ref count. Use when multiple owners exist.</li><li><strong>weak_ptr</strong> — non-owning observer of a shared_ptr. Breaks circular reference cycles. Must lock() before use.</li></ul><p>Prefer <code class=\"inline\">make_unique</code> / <code class=\"inline\">make_shared</code> over raw new.</p>",
    "tip": "Default to unique_ptr. Upgrade to shared_ptr only when you genuinely need shared ownership.",
    "code": "auto p = std::make_unique<int>(42);\nauto s = std::make_shared<std::string>(\"hello\");\nstd::weak_ptr<std::string> w = s;",
    "lang": "cpp",
    "revise": "Smart pointers = automatic ownership.\n• **`unique_ptr`** — exclusive owner, move-only, zero overhead → **default choice**.\n• **`shared_ptr`** — shared ownership via (thread-safe) ref count.\n• **`weak_ptr`** — non-owning observer; **breaks circular references**; `lock()` before use.\n• Prefer `make_unique`/`make_shared` over raw `new`.\n• **Interview tip**: default to `unique_ptr`, upgrade to `shared_ptr` only when you truly need multiple owners."
  },
  {
    "id": "cpp-move-semantics",
    "category": "cpp",
    "difficulty": "advanced",
    "tags": [
      "move",
      "rvalue",
      "performance"
    ],
    "question": "Move semantics and rvalue references.",
    "answer": "<p><strong>rvalue reference</strong> (<code class=\"inline\">T&amp;&amp;</code>) binds to temporaries and enables <strong>moving</strong> resources instead of copying.</p><p><code class=\"inline\">std::move()</code> casts to rvalue — doesn't move by itself, just enables move constructor/assignment to be called.</p><p>Moving a <code class=\"inline\">std::vector</code> transfers the internal pointer — O(1) instead of O(n) copy. The source is left in a valid but empty state.</p>",
    "tip": "Move semantics is why returning large objects by value is cheap in modern C++ — RVO/NRVO + move.",
    "code": "std::vector<int> a = {1,2,3};\nstd::vector<int> b = std::move(a);  // a is now empty\n// a.size() == 0, b has {1,2,3}",
    "lang": "cpp",
    "revise": "**Move semantics** transfer resources instead of copying.\n• **`T&&`** rvalue reference binds to temporaries; **`std::move`** just *casts* to rvalue (doesn't move by itself).\n• Moving a `vector` steals its internal pointer — **O(1)** vs O(n) copy; source left valid-but-empty.\n• **Interview tip**: it's why returning big objects by value is cheap in modern C++ (RVO/NRVO + move)."
  },
  {
    "id": "cpp-stl-vector",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "stl",
      "vector"
    ],
    "question": "std::vector — the most important STL container.",
    "answer": "<p>A <strong>dynamic array</strong> — contiguous memory, O(1) random access, amortized O(1) push_back. Automatically grows capacity.</p><p>Key methods: <code class=\"inline\">push_back</code>, <code class=\"inline\">emplace_back</code> (constructs in place), <code class=\"inline\">size</code>, <code class=\"inline\">capacity</code>, <code class=\"inline\">reserve</code> (pre-allocate), <code class=\"inline\">begin/end</code> for iteration.</p>",
    "tip": "Call reserve() when you know the final size — avoids repeated reallocations.",
    "code": "std::vector<int> v;\nv.reserve(1000);\nfor (int i = 0; i < 1000; i++) v.push_back(i);",
    "lang": "cpp",
    "revise": "**`std::vector`** = dynamic array — contiguous memory, **O(1) random access**, amortized O(1) `push_back`.\n• Grows capacity automatically; `emplace_back` constructs in place; `reserve` pre-allocates.\n• **Interview tip**: call **`reserve()`** when you know the final size — it avoids repeated reallocations (and iterator invalidation)."
  },
  {
    "id": "cpp-stl-map-set",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "stl",
      "map",
      "unordered_map"
    ],
    "question": "map vs unordered_map vs set.",
    "answer": "<ul><li><strong>map</strong> — sorted key-value pairs (red-black tree). O(log n) lookup. Iteration in key order.</li><li><strong>unordered_map</strong> — hash table. O(1) average lookup. No ordering.</li><li><strong>set</strong> — sorted unique keys. <strong>unordered_set</strong> — hash-based unique keys.</li></ul><p>Choose map when you need ordering; unordered_map for speed when order doesn't matter.</p>",
    "tip": "unordered_map needs a hash function for custom key types. map works out of the box with operator<.",
    "code": "std::unordered_map<std::string, int> freq;\nfreq[\"hello\"]++;\n\nstd::map<std::string, int> sorted;\nsorted[\"zebra\"] = 1;\nsorted[\"apple\"] = 2;  // iterates apple, zebra",
    "lang": "cpp",
    "revise": "Associative containers:\n• **`map`** — sorted key/value (red-black tree), **O(log n)**, iterates in key order.\n• **`unordered_map`** — hash table, **O(1) average**, no order.\n• `set`/`unordered_set` = the key-only versions.\n• **Interview tip**: `map` needs `operator<`; `unordered_map` needs a **hash function** for custom key types. Choose ordering vs speed."
  },
  {
    "id": "cpp-templates",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "templates",
      "generics"
    ],
    "question": "What are templates in C++?",
    "answer": "<p>Templates enable <strong>generic programming</strong> — write code once that works with any type. The compiler generates specific versions at compile time.</p><ul><li><strong>Function templates</strong> — <code class=\"inline\">template&lt;typename T&gt; T max(T a, T b)</code></li><li><strong>Class templates</strong> — <code class=\"inline\">std::vector&lt;T&gt;</code>, <code class=\"inline\">std::map&lt;K,V&gt;</code></li></ul><p>Templates are resolved at compile time — zero runtime overhead (monomorphization).</p>",
    "tip": "Template errors are notoriously verbose — the error usually makes sense if you read the first and last lines.",
    "code": "template<typename T>\nT max(T a, T b) { return (a > b) ? a : b; }\n\nint m = max(3, 7);         // int version\ndouble d = max(3.1, 2.9);  // double version",
    "lang": "cpp",
    "revise": "**Templates** = generic programming — write once, works for any type; compiler generates concrete versions at compile time (**zero runtime overhead**).\n• Function templates (`template<typename T> T max(...)`) + class templates (`vector<T>`, `map<K,V>`).\n• **Interview tip**: template errors are famously verbose — **read the first and last lines**; the middle is instantiation noise."
  },
  {
    "id": "cpp-references-vs-pointers",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "references",
      "pointers"
    ],
    "question": "References vs pointers in C++.",
    "answer": "<table><tr><th></th><th>Reference</th><th>Pointer</th></tr><tr><td><strong>Syntax</strong></td><td><code class=\"inline\">int&amp; r = x;</code></td><td><code class=\"inline\">int* p = &amp;x;</code></td></tr><tr><td><strong>Null</strong></td><td>Cannot be null</td><td>Can be nullptr</td></tr><tr><td><strong>Reassign</strong></td><td>Cannot rebind</td><td>Can point elsewhere</td></tr><tr><td><strong>Arithmetic</strong></td><td>No</td><td>Yes (pointer++)</td></tr><tr><td><strong>Use</strong></td><td>Function params, aliases</td><td>Dynamic memory, optional values</td></tr></table>",
    "tip": "Prefer references for function parameters when null isn't valid. Use pointers when nullptr is meaningful.",
    "code": "void swap(int& a, int& b) {\n  int tmp = a; a = b; b = tmp;\n}",
    "lang": "cpp",
    "revise": "**References vs pointers**.\n• **Reference** (`int& r = x`) — can't be null, can't rebind, no arithmetic → function params, aliases.\n• **Pointer** (`int* p = &x`) — can be `nullptr`, can rebind, supports arithmetic → dynamic memory, optional values.\n• **Interview tip**: prefer **references** for params when null isn't valid; use **pointers** when `nullptr` is a meaningful state."
  },
  {
    "id": "cpp-const",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "const",
      "const-correctness"
    ],
    "question": "const correctness in C++.",
    "answer": "<ul><li><code class=\"inline\">const int x</code> — cannot modify x.</li><li><code class=\"inline\">const int* p</code> — can't modify data through p (pointer to const).</li><li><code class=\"inline\">int* const p</code> — can't change what p points to (const pointer).</li><li><code class=\"inline\">const int* const p</code> — neither.</li><li><code class=\"inline\">void foo() const</code> — member function won't modify object state.</li></ul><p>Mark methods const when they don't modify state — enables calling on const objects.</p>",
    "tip": "const member functions are the C++ way to promise 'this method is read-only'.",
    "code": "class Circle {\n  double radius;\npublic:\n  double area() const { return 3.14 * radius * radius; }\n};",
    "lang": "cpp",
    "revise": "**const correctness** = promise not to modify.\n• `const int x` (value), `const int* p` (data via p), `int* const p` (the pointer), `const int* const p` (both).\n• `void foo() const` — member won't change object state (callable on const objects).\n• **Interview tip**: mark methods `const` whenever they don't mutate — it's the C++ way to say 'read-only' and unlocks const usage."
  },
  {
    "id": "cpp-operator-overloading",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "operators",
      "overloading"
    ],
    "question": "Operator overloading.",
    "answer": "<p>C++ lets you define custom behaviour for operators on user-defined types. Common overloads: <code class=\"inline\">operator+</code>, <code class=\"inline\">operator==</code>, <code class=\"inline\">operator&lt;&lt;</code> (stream output), <code class=\"inline\">operator[]</code> (indexing).</p><p>Cannot overload: <code class=\"inline\">::</code>, <code class=\"inline\">.*</code>, <code class=\"inline\">?:</code>, <code class=\"inline\">sizeof</code>. At least one operand must be a user-defined type.</p>",
    "tip": "Overload operator== and operator< (or operator<=> since C++20) to make your types work with STL containers and algorithms.",
    "code": "struct Point { int x, y; };\nbool operator==(const Point& a, const Point& b) {\n  return a.x == b.x && a.y == b.y;\n}",
    "lang": "cpp",
    "revise": "**Operator overloading** = custom behaviour for operators on your types.\n• Common: `operator+`, `operator==`, `operator<<` (stream output), `operator[]`.\n• Can't overload `::`, `.*`, `?:`, `sizeof`; at least one operand must be user-defined.\n• **Interview tip**: overload `operator==` (and `<=>` in C++20) so your type works with STL containers/algorithms."
  },
  {
    "id": "cpp-lambda",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "lambda",
      "c++11"
    ],
    "question": "Lambda expressions in C++.",
    "answer": "<p>Anonymous function objects. Syntax: <code class=\"inline\">[capture](params) { body }</code></p><ul><li><code class=\"inline\">[=]</code> — capture all by value.</li><li><code class=\"inline\">[&amp;]</code> — capture all by reference.</li><li><code class=\"inline\">[x, &amp;y]</code> — mixed capture.</li></ul><p>Used heavily with STL algorithms: <code class=\"inline\">sort</code>, <code class=\"inline\">for_each</code>, <code class=\"inline\">find_if</code>.</p>",
    "tip": "Be careful with [=] capturing pointers — you copy the pointer value, not the pointed-to data.",
    "code": "std::vector<int> v = {3, 1, 4, 1, 5};\nstd::sort(v.begin(), v.end(), [](int a, int b) {\n  return a > b;  // descending\n});",
    "lang": "cpp",
    "revise": "**Lambdas** = anonymous function objects: `[capture](params){ body }`.\n• `[=]` capture all by value, `[&]` by reference, `[x, &y]` mixed.\n• Used heavily with STL algorithms (`sort`, `for_each`, `find_if`).\n• **Interview tip**: with `[=]` you copy a **pointer's value, not the pointed-to data** — and `[&]` capturing a local that outlives the scope dangles."
  },
  {
    "id": "cpp-exception-handling",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "exceptions",
      "error-handling"
    ],
    "question": "Exception handling in C++.",
    "answer": "<p><code class=\"inline\">throw</code> raises an exception; <code class=\"inline\">try/catch</code> handles it. Catch by reference (<code class=\"inline\">catch(const std::exception&amp; e)</code>) to avoid slicing.</p><p>Standard hierarchy: <code class=\"inline\">std::exception</code> → <code class=\"inline\">runtime_error</code>, <code class=\"inline\">logic_error</code>, <code class=\"inline\">out_of_range</code>, etc.</p><p><code class=\"inline\">noexcept</code> marks functions that won't throw — enables compiler optimizations.</p>",
    "tip": "RAII ensures cleanup even when exceptions are thrown — destructors always run during stack unwinding.",
    "code": "try {\n  if (n < 0) throw std::invalid_argument(\"negative\");\n} catch (const std::exception& e) {\n  std::cerr << e.what();\n}",
    "lang": "cpp",
    "revise": "**Exceptions**: `throw` raises, `try`/`catch` handles.\n• **Catch by reference** (`catch(const std::exception& e)`) to avoid slicing.\n• Hierarchy: `std::exception` → `runtime_error`/`logic_error`/`out_of_range`; `noexcept` marks non-throwing functions.\n• **Interview tip**: **RAII guarantees cleanup during stack unwinding** — destructors always run, so resources are released even when an exception propagates."
  },
  {
    "id": "cpp-namespace",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "namespace",
      "scope"
    ],
    "question": "Namespaces in C++.",
    "answer": "<p>Namespaces group names to avoid collisions. <code class=\"inline\">std</code> is the standard library namespace.</p><ul><li><code class=\"inline\">using std::cout;</code> — import one name.</li><li><code class=\"inline\">using namespace std;</code> — import all (avoid in headers).</li><li>Anonymous namespace — internal linkage (like static at file scope).</li></ul>",
    "tip": "Never write 'using namespace std;' in a header file — it pollutes every file that includes it.",
    "code": "namespace math {\n  double pi = 3.14159;\n  double area(double r) { return pi * r * r; }\n}\n// use: math::area(5.0)",
    "lang": "cpp",
    "revise": "**Namespaces** group names to avoid collisions (`std` = standard library).\n• `using std::cout;` imports one name; `using namespace std;` imports all.\n• **Anonymous namespace** = internal linkage (like file-scope `static`).\n• **Interview tip**: **never** put `using namespace std;` in a **header** — it pollutes every file that includes it."
  },
  {
    "id": "cpp-new-delete-vs-smart",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "new",
      "delete",
      "memory"
    ],
    "question": "new/delete vs smart pointers — when to use raw new?",
    "answer": "<p><code class=\"inline\">new</code>/<code class=\"inline\">delete</code> allocate/deallocate single objects; <code class=\"inline\">new[]</code>/<code class=\"inline\">delete[]</code> for arrays. In modern C++, <strong>avoid raw new/delete</strong> in application code.</p><p>Use instead:</p><ul><li><code class=\"inline\">make_unique</code> / <code class=\"inline\">make_shared</code> for heap objects.</li><li><code class=\"inline\">std::vector</code> instead of new[].</li><li>Stack allocation when lifetime is function-scoped.</li></ul>",
    "tip": "If you write 'new' in an interview, immediately follow with 'wrapped in a unique_ptr'.",
    "code": "// avoid:  MyClass* p = new MyClass();\n// prefer:\nauto p = std::make_unique<MyClass>();",
    "lang": "cpp",
    "revise": "**`new`/`delete`** (and `new[]`/`delete[]`) manage heap manually.\n• Modern C++: **avoid raw new/delete** in app code.\n• Use `make_unique`/`make_shared` for heap objects, `std::vector` instead of `new[]`, stack allocation when scope-bound.\n• **Interview tip**: if you write `new` in an interview, immediately add 'wrapped in a `unique_ptr`'."
  },
  {
    "id": "cpp-abstract-interface",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "abstract",
      "interface"
    ],
    "question": "Abstract classes and pure virtual functions.",
    "answer": "<p>A class with at least one <strong>pure virtual</strong> function (<code class=\"inline\">= 0</code>) is abstract — cannot be instantiated. Acts as an <strong>interface</strong> defining a contract subclasses must fulfil.</p><p>Used for polymorphic designs: plugin systems, strategy pattern, factory pattern.</p>",
    "tip": "Abstract base class + virtual destructor is the classic C++ interface pattern.",
    "code": "class Shape {\npublic:\n  virtual ~Shape() = default;\n  virtual double area() const = 0;  // pure virtual\n};\n\nclass Circle : public Shape {\n  double r;\npublic:\n  double area() const override { return 3.14 * r * r; }\n};",
    "lang": "cpp",
    "revise": "An **abstract class** has ≥1 **pure virtual** function (`= 0`) → can't be instantiated.\n• Acts as an **interface** defining a contract subclasses must implement.\n• Uses: plugin systems, strategy/factory patterns.\n• **Interview tip**: **abstract base + virtual destructor** is the classic C++ interface pattern."
  },
  {
    "id": "cpp-stl-algorithms",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "stl",
      "algorithms"
    ],
    "question": "STL algorithms overview.",
    "answer": "<p><code class=\"inline\">&lt;algorithm&gt;</code> provides generic functions operating on iterator ranges:</p><ul><li><strong>sort, stable_sort</strong> — O(n log n) sorting.</li><li><strong>find, find_if</strong> — linear search.</li><li><strong>transform</strong> — apply function to each element.</li><li><strong>accumulate</strong> — reduce/sum a range.</li><li><strong>count, count_if</strong> — count matching elements.</li><li><strong>lower_bound, binary_search</strong> — on sorted ranges.</li></ul>",
    "tip": "Prefer STL algorithms over hand-written loops — they're tested, optimized, and self-documenting.",
    "code": "std::vector<int> v = {5, 2, 8, 1};\nstd::sort(v.begin(), v.end());\nauto it = std::lower_bound(v.begin(), v.end(), 5);",
    "lang": "cpp",
    "revise": "**`<algorithm>`** = generic functions over iterator ranges.\n• `sort`/`stable_sort` (O(n log n)), `find`/`find_if`, `transform`, `accumulate`, `count`/`count_if`, `lower_bound`/`binary_search` (sorted).\n• **Interview tip**: prefer STL algorithms over hand-written loops — they're tested, optimized, and self-documenting (and pair naturally with lambdas)."
  },
  {
    "id": "cpp-auto-range-for",
    "category": "cpp",
    "difficulty": "beginner",
    "tags": [
      "auto",
      "range-for",
      "c++11"
    ],
    "question": "auto keyword and range-based for loops.",
    "answer": "<p><strong>auto</strong> — compiler deduces the type. Use for verbose iterator types and lambdas. <code class=\"inline\">auto&amp;</code> for non-copying references; <code class=\"inline\">const auto&amp;</code> for read-only.</p><p><strong>Range-for</strong> — iterate any container with begin/end:</p><p><code class=\"inline\">for (const auto&amp; item : container)</code></p>",
    "tip": "Use const auto& in range-for to avoid copying large objects. Use auto* for pointers.",
    "code": "std::vector<std::string> names = {\"Ada\", \"Bob\"};\nfor (const auto& name : names) {\n  std::cout << name << '\\n';\n}",
    "lang": "cpp",
    "revise": "**`auto`** = compiler deduces the type (great for iterators + lambdas).\n• `auto&` non-copying reference, `const auto&` read-only, `auto*` for pointers.\n• **Range-for**: `for (const auto& item : container)` iterates anything with begin/end.\n• **Interview tip**: use **`const auto&`** in range-for to avoid copying large objects each iteration."
  },
  {
    "id": "cpp-friend",
    "category": "cpp",
    "difficulty": "intermediate",
    "tags": [
      "friend",
      "access"
    ],
    "question": "What is a friend function/class?",
    "answer": "<p><code class=\"inline\">friend</code> grants a non-member function or another class access to a class's <strong>private and protected</strong> members. Breaks encapsulation deliberately — use sparingly.</p><p>Common uses: overloading <code class=\"inline\">operator&lt;&lt;</code> for stream output, tightly coupled helper classes.</p>",
    "tip": "Friend functions are not member functions — they're free functions granted access. Common for operator<<.",
    "code": "class Point {\n  int x, y;\n  friend std::ostream& operator<<(std::ostream& os, const Point& p);\n};",
    "lang": "cpp",
    "revise": "**`friend`** grants a non-member function or another class access to `private`/`protected` members.\n• Deliberately breaks encapsulation — use sparingly.\n• Common: overloading `operator<<` for stream output, tightly-coupled helpers.\n• **Interview tip**: a friend function is a **free function granted access**, not a member — the classic use is `operator<<`."
  }
]
