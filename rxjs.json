[
  {
    "id": "rx-observable-promise",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "observable",
      "promise",
      "fundamentals"
    ],
    "question": "Observable vs Promise.",
    "answer": "<p>An <strong>Observable</strong> is a lazy stream of values that starts executing only when subscribed. It can emit <strong>zero, one, or many</strong> values over time and supports cancellation through <code class=\"inline\">unsubscribe()</code>. A <strong>Promise</strong> is eager, starts executing immediately when created, resolves or rejects only once, and cannot be cancelled. Observables also provide powerful RxJS operators such as <code class=\"inline\">map</code>, <code class=\"inline\">filter</code>, <code class=\"inline\">switchMap</code>, and <code class=\"inline\">catchError</code>, making them ideal for Angular applications.</p>",
    "tip": "Remember: <strong>Promise = One value.</strong> <strong>Observable = Stream of values.</strong> Angular's <code class=\"inline\">HttpClient</code> returns Observables because they are cancellable and composable.",
    "code": "// Promise\nconst promise = fetch('/api/users')\n  .then(res => res.json());\n\npromise.then(users => console.log(users));\n\n// Observable\nthis.http.get<User[]>('/api/users')\n  .subscribe(users => console.log(users));",
    "lang": "ts",
    "deep": "<h4>What is a Promise?</h4><p>A Promise represents the result of a single asynchronous operation. It starts executing immediately after creation and eventually becomes either <strong>fulfilled</strong> or <strong>rejected</strong>.</p><pre><code>const promise = new Promise((resolve) => {\n  console.log('Started');\n  resolve('Success');\n});\n\npromise.then(console.log);</code></pre><p>The executor function runs immediately, even if nobody calls <code class=\"inline\">then()</code>.</p><h4>What is an Observable?</h4><p>An Observable is a lazy stream of asynchronous values. Nothing executes until someone subscribes to it.</p><pre><code>const observable = new Observable(subscriber => {\n  console.log('Started');\n  subscriber.next('Hello');\n  subscriber.complete();\n});\n\nobservable.subscribe(console.log);</code></pre><p>The code inside the Observable runs only after <code class=\"inline\">subscribe()</code> is called.</p><h4>Lazy vs Eager</h4><pre><code>// Promise (Eager)\nconst promise = fetch('/users');\n// Request starts immediately\n\n// Observable (Lazy)\nconst users$ = this.http.get('/users');\n// No request yet\n\nusers$.subscribe();\n// Request starts here</code></pre><h4>Single vs Multiple Values</h4><table><tr><th>Promise</th><th>Observable</th></tr><tr><td>One value</td><td>Zero, one, or many values</td></tr><tr><td>Completes automatically</td><td>Can emit indefinitely</td></tr></table><pre><code>// Promise\nfetch('/users')\n  .then(console.log);\n\n// Observable\ninterval(1000)\n  .subscribe(console.log);\n// 0,1,2,3,...</code></pre><h4>Cancellation</h4><p>One of the biggest advantages of Observables is that they can be cancelled.</p><pre><code>const sub = this.http.get('/users')\n  .subscribe();\n\nsub.unsubscribe();</code></pre><p>Promises cannot be cancelled once started.</p><h4>Operators</h4><p>Observables support powerful RxJS operators for transforming and combining streams.</p><pre><code>this.search.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(value =>\n    this.userService.search(value)\n  )\n).subscribe();</code></pre><p>Promises support simple chaining using <code class=\"inline\">then()</code> but do not have an equivalent operator ecosystem.</p><h4>Observable vs Promise</h4><table><tr><th>Feature</th><th>Promise</th><th>Observable</th></tr><tr><td>Execution</td><td>Eager</td><td>Lazy</td></tr><tr><td>Values</td><td>One</td><td>Zero, One, or Many</td></tr><tr><td>Cancellation</td><td>❌ No</td><td>✅ Yes</td></tr><tr><td>Operators</td><td>Limited (.then)</td><td>Rich RxJS operators</td></tr><tr><td>Retry</td><td>Difficult</td><td>Built-in (retry)</td></tr><tr><td>Angular HttpClient</td><td>❌ No</td><td>✅ Yes</td></tr></table><h4>Real-world Angular Example</h4><p>Consider a search box.</p><pre><code>this.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(text =>\n    this.http.get(`/users?q=${text}`)\n  )\n).subscribe();</code></pre><p>Every new keystroke cancels the previous HTTP request using <code class=\"inline\">switchMap()</code>. With Promises, older requests would continue running, potentially returning stale results.</p><h4>When to Use What?</h4><ul><li><strong>Promise:</strong> One-time async operations like loading a configuration file or requesting browser permissions.</li><li><strong>Observable:</strong> API calls, form value changes, WebSockets, routing events, user interactions, timers, and any stream of data.</li></ul><h4>Interview Tip</h4><p>The most common follow-up question is <strong>'Why does Angular HttpClient return an Observable instead of a Promise?'</strong> Mention that Observables are lazy, cancellable, support multiple values, and integrate with powerful RxJS operators like <code class=\"inline\">switchMap()</code>, making them ideal for reactive applications.</p><h4>Interview Answer</h4><p><strong>\"A Promise represents a single asynchronous result and starts executing immediately. An Observable is a lazy stream that starts only when subscribed and can emit multiple values over time. Observables are cancellable and provide rich RxJS operators for transforming and combining streams, which is why Angular's HttpClient and forms are built around Observables instead of Promises.\"</strong></p>",
    "revise": "• **Promise** — a SINGLE value, eager (runs immediately), not cancellable.\n• **Observable** — a LAZY stream of 0..many values, cancellable (unsubscribe), with rich operators.\nAn Observable does nothing until subscribed; a Promise has already started."
  },
  {
    "id": "rx-flattening",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "switchMap",
      "mergeMap",
      "concatMap",
      "exhaustMap"
    ],
    "question": "switchMap vs mergeMap vs concatMap vs exhaustMap.",
    "answer": "<p>These are called <strong>flattening operators</strong> because they transform an Observable that emits Observables (<code class=\"inline\">Observable&lt;Observable&lt;T&gt;&gt;</code>) into a single Observable. The difference lies in how they handle multiple inner Observables.</p><ul><li><strong>switchMap</strong> — Cancels the previous inner Observable when a new value arrives. Best for search/autocomplete and route changes.</li><li><strong>mergeMap</strong> — Runs all inner Observables concurrently without cancellation. Best for independent parallel requests.</li><li><strong>concatMap</strong> — Queues requests and executes them one after another, preserving order. Best for sequential operations like saving records.</li><li><strong>exhaustMap</strong> — Ignores new emissions while the current Observable is still running. Best for preventing duplicate actions such as login or payment requests.</li></ul>",
    "tip": "This is the **#1 most asked RxJS interview question.** Remember the keywords: <strong>switch = Cancel</strong>, <strong>merge = Parallel</strong>, <strong>concat = Queue</strong>, <strong>exhaust = Ignore</strong>.",
    "code": "// switchMap - Search Box\nthis.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(term => this.userService.search(term))\n).subscribe();",
    "lang": "ts",
    "deep": "<h4>Why do we need Flattening Operators?</h4><p>Many RxJS operators return another Observable. This creates an <code class=\"inline\">Observable&lt;Observable&gt;</code>, which isn't useful directly. Flattening operators subscribe to the inner Observable and merge its values into a single stream.</p><pre><code>Search Input\n      ↓\nvalueChanges\n      ↓\nswitchMap()\n      ↓\nHTTP Observable\n      ↓\nActual Result</code></pre><h4>1. switchMap()</h4><p><strong>switchMap()</strong> cancels the previous inner Observable whenever a new value arrives and subscribes only to the latest one.</p><pre><code>search.valueChanges.pipe(\n  debounceTime(300),\n  switchMap(term =>\n    this.http.get(`/users?q=${term}`)\n  )\n);</code></pre><pre><code>User types:\nA\nAn\nAng\nAngu\nAngular\n\nPrevious API Calls\n❌ Cancelled\n❌ Cancelled\n❌ Cancelled\n❌ Cancelled\n✅ Only Angular executes</code></pre><p><strong>Best For:</strong></p><ul><li>Search autocomplete</li><li>Route parameter changes</li><li>Live filtering</li><li>Cancel stale HTTP requests</li></ul><h4>2. mergeMap()</h4><p><strong>mergeMap()</strong> subscribes to every inner Observable immediately. All requests run in parallel and complete independently.</p><pre><code>from([1,2,3]).pipe(\n  mergeMap(id =>\n    this.http.get(`/users/${id}`)\n  )\n);</code></pre><pre><code>Request 1 ─────►\nRequest 2 ─────►\nRequest 3 ─────►\n\nAll run simultaneously</code></pre><p><strong>Best For:</strong></p><ul><li>Independent API calls</li><li>File uploads</li><li>Image processing</li><li>Background tasks</li></ul><p><strong>Warning:</strong> Results may arrive in a different order than requests were started.</p><h4>3. concatMap()</h4><p><strong>concatMap()</strong> queues each inner Observable and executes them one after another. The next request starts only after the previous one completes.</p><pre><code>from([1,2,3]).pipe(\n  concatMap(id =>\n    this.http.post('/save', { id })\n  )\n);</code></pre><pre><code>Request 1 ✅\n      ↓\nRequest 2 ✅\n      ↓\nRequest 3 ✅</code></pre><p><strong>Best For:</strong></p><ul><li>Saving records</li><li>Database updates</li><li>Payment processing</li><li>Operations that must preserve order</li></ul><h4>4. exhaustMap()</h4><p><strong>exhaustMap()</strong> ignores new emissions while the current Observable is still executing.</p><pre><code>fromEvent(loginBtn, 'click').pipe(\n  exhaustMap(() =>\n    this.authService.login()\n  )\n);</code></pre><pre><code>User clicks:\nClick\nClick\nClick\nClick\n\nFirst Request ✅\nOther Clicks ❌ Ignored</code></pre><p><strong>Best For:</strong></p><ul><li>Login button</li><li>Payment submission</li><li>OTP verification</li><li>Preventing double-clicks</li></ul><h4>Comparison</h4><table><tr><th>Operator</th><th>Previous Request</th><th>New Request</th><th>Order</th><th>Typical Use</th></tr><tr><td><code class=\"inline\">switchMap</code></td><td>Cancelled</td><td>Runs</td><td>Latest Only</td><td>Search</td></tr><tr><td><code class=\"inline\">mergeMap</code></td><td>Keeps Running</td><td>Runs</td><td>Not Guaranteed</td><td>Parallel APIs</td></tr><tr><td><code class=\"inline\">concatMap</code></td><td>Completes First</td><td>Queued</td><td>Guaranteed</td><td>Saving Data</td></tr><tr><td><code class=\"inline\">exhaustMap</code></td><td>Keeps Running</td><td>Ignored</td><td>First Only</td><td>Login/Payment</td></tr></table><h4>Real-world Angular Examples</h4><p><strong>Search Box</strong> → <code class=\"inline\">switchMap()</code></p><pre><code>valueChanges.pipe(\n  debounceTime(300),\n  switchMap(term => this.api.search(term))\n);</code></pre><p><strong>Dashboard Loading</strong> → <code class=\"inline\">mergeMap()</code></p><pre><code>from(ids).pipe(\n  mergeMap(id => this.api.getUser(id))\n);</code></pre><p><strong>Save Button</strong> → <code class=\"inline\">concatMap()</code></p><pre><code>from(records).pipe(\n  concatMap(record => this.api.save(record))\n);</code></pre><p><strong>Login Button</strong> → <code class=\"inline\">exhaustMap()</code></p><pre><code>fromEvent(loginBtn, 'click').pipe(\n  exhaustMap(() => this.auth.login())\n);</code></pre><h4>Memory Trick</h4><table><tr><th>Operator</th><th>Keyword</th></tr><tr><td>switchMap</td><td>🔄 Switch (Cancel Previous)</td></tr><tr><td>mergeMap</td><td>⚡ Parallel (Everything Runs)</td></tr><tr><td>concatMap</td><td>📋 Queue (One by One)</td></tr><tr><td>exhaustMap</td><td>🚫 Ignore (Busy)</td></tr></table><h4>Interview Tip</h4><p>This question is almost guaranteed in Angular interviews. Interviewers rarely want the definitions alone—they want the <strong>correct use case</strong>. Mention:</p><ul><li><strong>switchMap</strong> → Search/autocomplete.</li><li><strong>mergeMap</strong> → Parallel independent requests.</li><li><strong>concatMap</strong> → Sequential updates where order matters.</li><li><strong>exhaustMap</strong> → Prevent duplicate actions like login or payment.</li></ul><h4>Interview Answer</h4><p><strong>\"All four operators flatten higher-order Observables but differ in how they manage concurrent inner Observables. switchMap cancels previous requests and keeps only the latest, making it ideal for search. mergeMap executes all requests in parallel, concatMap queues them sequentially, and exhaustMap ignores new requests until the current one completes. Choosing the correct operator depends on whether you need cancellation, concurrency, ordering, or duplicate prevention.\"</strong></p>",
    "revise": "Four flattening operators, differing on how they handle a NEW inner while one is active:\n• **switchMap** — CANCELS the previous inner (latest wins) — search, navigation.\n• **mergeMap** — runs ALL inners concurrently — independent writes.\n• **concatMap** — QUEUES inners in order (one at a time) — ordered writes.\n• **exhaustMap** — IGNORES new inners until the current finishes — prevent double-submit."
  },
  {
    "id": "rx-subjects",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "subject",
      "behaviorsubject",
      "replaysubject"
    ],
    "question": "Subject vs BehaviorSubject vs ReplaySubject vs AsyncSubject.",
    "answer": "<p><strong>Subject</strong> is both an <strong>Observable</strong> and an <strong>Observer</strong>. It multicasts values to multiple subscribers, but subscribers only receive values emitted after they subscribe.</p><p><strong>BehaviorSubject</strong> stores the latest value, requires an initial value, and immediately emits the current value to new subscribers.</p><p><strong>ReplaySubject</strong> stores and replays the last <code class=\"inline\">N</code> emitted values to new subscribers.</p><p><strong>AsyncSubject</strong> emits only the last value, and only after the Observable completes.</p>",
    "tip": "Interview Tip: <strong>BehaviorSubject</strong> is the most commonly used Subject in Angular for shared application state such as the current user, theme, or authentication status.",
    "code": "const subject = new Subject<number>();\nconst behavior = new BehaviorSubject<number>(0);\nconst replay = new ReplaySubject<number>(2);\nconst asyncSubject = new AsyncSubject<number>();",
    "lang": "ts",
    "deep": "<h4>What is a Subject?</h4><p>A <strong>Subject</strong> is a special type of Observable that can both <strong>emit values</strong> and <strong>be subscribed to</strong>. Unlike normal Observables where each subscriber gets its own execution, a Subject shares (multicasts) the same values with all subscribers.</p><pre><code>Producer\n      ↓\n   Subject\n   ↙   ↓   ↘\nSub1 Sub2 Sub3</code></pre><p>Every subscriber receives the same emitted value.</p><h4>1. Subject</h4><p>A Subject does not store previous values. New subscribers only receive values emitted after they subscribe.</p><pre><code>const subject = new Subject<number>();\n\nsubject.next(1);\n\nsubject.subscribe(v => console.log('A:', v));\n\nsubject.next(2);\nsubject.next(3);\n\n// Output\n// A: 2\n// A: 3</code></pre><p>The value <code class=\"inline\">1</code> is lost because no subscriber existed when it was emitted.</p><h4>2. BehaviorSubject</h4><p>BehaviorSubject always stores the latest emitted value. Every new subscriber immediately receives that current value.</p><pre><code>const user$ = new BehaviorSubject<string>('Guest');\n\nuser$.subscribe(v => console.log('A:', v));\n// Guest\n\nuser$.next('John');\n\nuser$.subscribe(v => console.log('B:', v));\n// B immediately receives 'John'</code></pre><p>BehaviorSubject requires an initial value.</p><h4>Real-world Example</h4><pre><code>export class AuthService {\n  currentUser$ = new BehaviorSubject<User | null>(null);\n}\n\nthis.authService.currentUser$.subscribe(user => {\n  console.log(user);\n});</code></pre><p>Every component immediately knows the currently logged-in user.</p><h4>3. ReplaySubject</h4><p>ReplaySubject stores multiple previous values and replays them to new subscribers.</p><pre><code>const replay = new ReplaySubject<number>(2);\n\nreplay.next(1);\nreplay.next(2);\nreplay.next(3);\n\nreplay.subscribe(console.log);\n\n// Output\n// 2\n// 3</code></pre><p>Since the buffer size is 2, only the last two values are replayed.</p><h4>Real-world Example</h4><ul><li>Chat history.</li><li>Application logs.</li><li>Notification history.</li><li>Recently viewed items.</li></ul><h4>4. AsyncSubject</h4><p>AsyncSubject stores only the last value and emits it only after <code class=\"inline\">complete()</code> is called.</p><pre><code>const subject = new AsyncSubject<number>();\n\nsubject.subscribe(console.log);\n\nsubject.next(1);\nsubject.next(2);\nsubject.next(3);\n\nsubject.complete();\n\n// Output\n// 3</code></pre><p>If <code class=\"inline\">complete()</code> is never called, subscribers receive nothing.</p><h4>Comparison</h4><table><tr><th>Type</th><th>Stores Values?</th><th>New Subscriber Gets</th><th>Needs Initial Value?</th></tr><tr><td>Subject</td><td>❌ No</td><td>Future values only</td><td>❌ No</td></tr><tr><td>BehaviorSubject</td><td>✅ Latest</td><td>Latest + future values</td><td>✅ Yes</td></tr><tr><td>ReplaySubject</td><td>✅ Last N</td><td>Buffered values + future values</td><td>❌ No</td></tr><tr><td>AsyncSubject</td><td>✅ Last</td><td>Last value after complete()</td><td>❌ No</td></tr></table><h4>When to Use Each?</h4><ul><li><strong>Subject</strong> → Event bus, button clicks, notifications.</li><li><strong>BehaviorSubject</strong> → Authentication state, current user, theme, shopping cart.</li><li><strong>ReplaySubject</strong> → Chat history, cached responses, activity logs.</li><li><strong>AsyncSubject</strong> → One-time async result after completion.</li></ul><h4>Angular Example</h4><p>BehaviorSubject is widely used for shared state between unrelated components.</p><pre><code>@Injectable({ providedIn: 'root' })\nexport class ThemeService {\n  theme$ = new BehaviorSubject<'light' | 'dark'>('light');\n\n  changeTheme(theme: 'light' | 'dark') {\n    this.theme$.next(theme);\n  }\n}</code></pre><p>Every subscribed component immediately receives the current theme without waiting for another emission.</p><h4>Interview Tip</h4><p>The most common interview question is <strong>'Why BehaviorSubject instead of Subject?'</strong> Answer: because BehaviorSubject stores the latest value and immediately sends it to new subscribers. This makes it ideal for shared application state such as the logged-in user or application theme.</p><h4>Interview Answer</h4><p><strong>\"Subject multicasts values but does not retain previous emissions. BehaviorSubject stores the latest value and immediately emits it to new subscribers, making it ideal for shared state. ReplaySubject stores the last N values and replays them to new subscribers, while AsyncSubject emits only the final value after completion. In Angular, BehaviorSubject is the most commonly used because components often need access to the current application state as soon as they subscribe.\"</strong></p>",
    "revise": "Subjects are multicast (both Observable AND Observer):\n• **Subject** — no initial value; only future emissions.\n• **BehaviorSubject** — holds the LATEST value + needs an initial; new subscribers get it immediately.\n• **ReplaySubject** — replays the last N values to new subscribers.\n• **AsyncSubject** — emits only the final value, on complete."
  },
  {
    "id": "rx-unsubscribe",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "memory-leak",
      "unsubscribe",
      "takeUntil",
      "real-world"
    ],
    "question": "How do you avoid memory leaks / unsubscribe?",
    "answer": "<ul><li><strong>async pipe</strong> in the template — subscribes and unsubscribes automatically (preferred).</li><li><strong>takeUntil(destroy$)</strong> with a Subject completed in <code class=\"inline\">ngOnDestroy</code>.</li><li><code class=\"inline\">takeUntilDestroyed()</code> in modern Angular.</li><li>Store the <code class=\"inline\">Subscription</code> and call <code class=\"inline\">unsubscribe()</code> in <code class=\"inline\">ngOnDestroy</code>.</li></ul><p>HTTP calls complete on their own, but long-lived streams (Subjects, valueChanges, intervals) leak if you don't clean up.</p>",
    "tip": "async pipe first; takeUntil when you must subscribe in the class.",
    "code": "this.data$.pipe(takeUntil(this.destroy$)).subscribe(...);\nngOnDestroy() { this.destroy$.next(); this.destroy$.complete(); }",
    "lang": "ts",
    "deep": "<h4>Why a subscription leaks</h4><p>Subscribing registers a callback with the <em>source</em>. That callback closes over your component, so <strong>the source now holds a reference to it</strong>. When Angular destroys the component, the source is still alive and still holding on — so the component is never garbage-collected. Navigate in and out 50 times and you have 50 live components, all still reacting to events.</p><h4>Which streams actually leak</h4><table><thead><tr><th>Stream</th><th>Completes by itself?</th><th>Cleanup needed?</th></tr></thead><tbody><tr><td><code class=\"inline\">http.get()</code></td><td>yes, after 1 emission</td><td>no (but harmless)</td></tr><tr><td><code class=\"inline\">of()</code> / <code class=\"inline\">from()</code></td><td>yes</td><td>no</td></tr><tr><td><code class=\"inline\">interval()</code> / <code class=\"inline\">timer(0, n)</code></td><td><strong>never</strong></td><td><strong>yes</strong></td></tr><tr><td><code class=\"inline\">fromEvent()</code></td><td><strong>never</strong></td><td><strong>yes</strong></td></tr><tr><td>A <code class=\"inline\">Subject</code> you own</td><td>only if you complete it</td><td><strong>yes</strong></td></tr><tr><td><code class=\"inline\">form.valueChanges</code></td><td><strong>never</strong></td><td><strong>yes</strong></td></tr><tr><td><code class=\"inline\">router.events</code>, <code class=\"inline\">store.select()</code></td><td><strong>never</strong></td><td><strong>yes</strong></td></tr></tbody></table><p>“HTTP doesn't need unsubscribing” is true but often misused — the leak is almost always a <em>long-lived</em> stream.</p><h4>The four options, best first</h4><pre><code>// 1. async pipe — no subscription in your class at all\n&lt;div *ngIf=\"user$ | async as user\"&gt;{{ user.name }}&lt;/div&gt;\n\n// 2. takeUntilDestroyed (Angular 16+) — no boilerplate\ndata$.pipe(takeUntilDestroyed()).subscribe();\n\n// 3. takeUntil + destroy$ Subject (the classic)\nprivate destroy$ = new Subject&lt;void&gt;();\nngOnInit()    { this.data$.pipe(takeUntil(this.destroy$)).subscribe(); }\nngOnDestroy() { this.destroy$.next(); this.destroy$.complete(); }\n\n// 4. manual — fine for one, ugly for many\nprivate sub = new Subscription();\nngOnInit()    { this.sub.add(this.data$.subscribe()); }\nngOnDestroy() { this.sub.unsubscribe(); }   // unsubscribes all children</code></pre><h4>The bug interviewers look for: takeUntil must be LAST</h4><pre><code>// BROKEN — switchMap creates a NEW inner subscription\n// downstream of takeUntil, so the inner stream never gets cut off\nsource$.pipe(takeUntil(this.destroy$), switchMap(() =&gt; interval(1000)))\n\n// CORRECT — takeUntil last, so it tears down everything above it\nsource$.pipe(switchMap(() =&gt; interval(1000)), takeUntil(this.destroy$))</code></pre><p>Any operator placed <em>after</em> <code class=\"inline\">takeUntil</code> can resubscribe to something that outlives the component. This is the single most-cited RxJS leak, and naming it is a strong signal.</p><h4>Don't forget destroy$.complete()</h4><p><code class=\"inline\">next()</code> alone fires the notification but leaves the Subject itself subscribed to by <code class=\"inline\">takeUntil</code>. <code class=\"inline\">complete()</code> releases it.</p><h4>How to prove it in an interview</h4><p>Chrome DevTools → Memory → take a heap snapshot, navigate away and back a few times, snapshot again, and filter by your component class name. If the instance count climbs, you're leaking.</p><h4>How to say it in an interview</h4><p>“The subscription makes the source hold a reference to my component, so it never gets collected. I default to the <code class=\"inline\">async</code> pipe so there's no subscription to manage, and <code class=\"inline\">takeUntilDestroyed</code> when I must subscribe in the class. The detail that matters is that <code class=\"inline\">takeUntil</code> has to be the <em>last</em> operator — if a <code class=\"inline\">switchMap</code> sits after it, the inner stream escapes the teardown and still leaks.”</p>",
    "revise": "Un-cleaned subscriptions leak (duplicate HTTP, growing memory).\n• **BEST**: the async pipe (auto-unsubscribes) or `takeUntilDestroyed()` in Angular.\n• **Manual**: `takeUntil`(destroy$) or store and unsubscribe in `ngOnDestroy`.\nHTTP completes on its own; long-lived streams (Subjects, intervals, `valueChanges`) do NOT."
  },
  {
    "id": "rx-hot-cold",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "hot",
      "cold",
      "multicast",
      "shareReplay"
    ],
    "question": "Hot vs cold observables.",
    "answer": "<p><strong>Cold</strong> — each subscriber triggers its own producer, getting an independent execution (e.g. an HTTP call runs per subscribe).</p><p><strong>Hot</strong> — the producer is shared; subscribers see values as they happen (e.g. DOM events, a Subject). Use <code class=\"inline\">share()</code> / <code class=\"inline\">shareReplay()</code> to make a cold stream hot.</p>",
    "tip": "Wrap an HTTP call with shareReplay(1) to avoid firing it once per subscriber.",
    "code": "readonly config$ = this.http.get('/config').pipe(shareReplay(1));",
    "lang": "ts",
    "deep": "<h4>The actual definition</h4><p>It's about <strong>where the producer lives</strong>.</p><ul><li><strong>Cold</strong> — the producer is created <em>inside</em> the observable, once <strong>per subscriber</strong>. Everyone gets their own private execution from the start. <em>Unicast.</em></li><li><strong>Hot</strong> — the producer lives <em>outside</em> and is <strong>shared</strong>. Subscribers tune in mid-flight and only see what's emitted from then on. <em>Multicast.</em></li></ul><p>Cold is a <strong>DVD</strong> — everyone watches from the beginning, whenever they press play. Hot is a <strong>live broadcast</strong> — tune in late, miss the first half.</p><h4>Which is which</h4><table><thead><tr><th>Cold</th><th>Hot</th></tr></thead><tbody><tr><td><code class=\"inline\">http.get()</code></td><td><code class=\"inline\">fromEvent(document,'click')</code></td></tr><tr><td><code class=\"inline\">of()</code>, <code class=\"inline\">from()</code></td><td><code class=\"inline\">Subject</code> / <code class=\"inline\">BehaviorSubject</code></td></tr><tr><td><code class=\"inline\">interval()</code>, <code class=\"inline\">timer()</code></td><td>anything through <code class=\"inline\">share()</code></td></tr><tr><td>most operators' output</td><td><code class=\"inline\">store.select()</code></td></tr></tbody></table><h4>Why it bites in practice</h4><pre><code>const user$ = this.http.get('/me');   // COLD\n\nuser$.subscribe();   // request #1\nuser$.subscribe();   // request #2 — a SECOND network call!</code></pre><p>Two <code class=\"inline\">async</code> pipes on the same observable = two HTTP calls. Nothing is cached, because each subscribe re-runs the producer from scratch.</p><h4>Making a cold stream hot</h4><pre><code>// share() — multicast while at least one subscriber is active\nconst clicks$ = interval(1000).pipe(share());\n\n// shareReplay(1) — multicast AND replay the last value to latecomers\nreadonly config$ = this.http.get('/config').pipe(shareReplay(1));</code></pre><p><code class=\"inline\">share()</code> gives a late subscriber <em>nothing</em> until the next emission. <code class=\"inline\">shareReplay(1)</code> immediately replays the last value — which is what makes it a cache.</p><h4>The nuance most candidates miss</h4><p>“Hot vs cold” isn't a property you can inspect — it's about the <em>subscription behaviour</em>. And a Subject is hot, but <code class=\"inline\">BehaviorSubject</code> blurs the line: it's hot (shared producer) yet replays its current value to new subscribers, so it <em>feels</em> partly cold. The precise framing is <strong>unicast vs multicast</strong> plus <strong>replay or not</strong>.</p><h4>How to say it in an interview</h4><p>“Cold means the producer is created per subscription, so every subscriber gets an independent run — that's why two async pipes on one <code class=\"inline\">http.get</code> fire two requests. Hot means one shared producer, so subscribers see values from whenever they joined. I make things hot with <code class=\"inline\">share()</code>, or <code class=\"inline\">shareReplay(1)</code> when late subscribers also need the last value — that's the standard config/cache pattern.”</p>",
    "revise": "• **Cold** — each subscriber gets its OWN execution (e.g. an HTTP call re-runs per subscribe).\n• **Hot** — a SHARED, already-running source (Subject, `fromEvent`); late subscribers MISS earlier values.\nTurn cold into hot/shared with `shareReplay`/share."
  },
  {
    "id": "rx-common-operators",
    "category": "rxjs",
    "difficulty": "beginner",
    "tags": [
      "operators"
    ],
    "question": "Name common RxJS operators and what they do.",
    "answer": "<p>RxJS operators are functions used inside <code class=\"inline\">pipe()</code> to transform, filter, combine, or control Observables. Common operators include <code class=\"inline\">map</code>, <code class=\"inline\">filter</code>, <code class=\"inline\">tap</code>, <code class=\"inline\">debounceTime</code>, <code class=\"inline\">distinctUntilChanged</code>, <code class=\"inline\">switchMap</code>, <code class=\"inline\">mergeMap</code>, <code class=\"inline\">concatMap</code>, <code class=\"inline\">take</code>, <code class=\"inline\">takeUntil</code>, <code class=\"inline\">catchError</code>, and <code class=\"inline\">forkJoin</code>. They are the building blocks of reactive programming in Angular.</p>",
    "tip": "Interview Tip: Know <strong>switchMap</strong>, <strong>mergeMap</strong>, <strong>concatMap</strong>, and <strong>exhaustMap</strong>. They are among the most frequently asked RxJS interview topics.",
    "code": "this.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(value => this.userService.search(value)),\n  catchError(() => of([]))\n).subscribe(users => {\n  this.users = users;\n});",
    "lang": "ts",
    "deep": "<h4>What are RxJS Operators?</h4><p>Operators are pure functions that work on Observables. They allow us to transform, filter, combine, delay, or handle asynchronous streams without modifying the original Observable.</p><pre><code>Observable\n      ↓\npipe()\n      ↓\nOperators\n      ↓\nSubscriber</code></pre><p>Operators are chained together using the <code class=\"inline\">pipe()</code> method.</p><h4>Transformation Operators</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">map()</code></td><td>Transform emitted values.</td></tr><tr><td><code class=\"inline\">switchMap()</code></td><td>Cancel previous request and switch to the latest.</td></tr><tr><td><code class=\"inline\">mergeMap()</code></td><td>Run multiple inner Observables concurrently.</td></tr><tr><td><code class=\"inline\">concatMap()</code></td><td>Execute inner Observables sequentially.</td></tr><tr><td><code class=\"inline\">exhaustMap()</code></td><td>Ignore new emissions while one is already running.</td></tr></table><pre><code>of(1,2,3).pipe(\n  map(x => x * 10)\n);\n// 10,20,30</code></pre><h4>Filtering Operators</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">filter()</code></td><td>Keep matching values.</td></tr><tr><td><code class=\"inline\">take()</code></td><td>Take first N emissions.</td></tr><tr><td><code class=\"inline\">takeUntil()</code></td><td>Automatically unsubscribe.</td></tr><tr><td><code class=\"inline\">distinctUntilChanged()</code></td><td>Ignore duplicate consecutive values.</td></tr></table><pre><code>of(1,2,3,4,5).pipe(\n  filter(x => x % 2 === 0)\n);\n// 2,4</code></pre><h4>Utility Operators</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">tap()</code></td><td>Perform side effects such as logging.</td></tr><tr><td><code class=\"inline\">finalize()</code></td><td>Execute cleanup logic when Observable completes.</td></tr><tr><td><code class=\"inline\">delay()</code></td><td>Delay emissions.</td></tr></table><pre><code>this.http.get('/users').pipe(\n  tap(() => console.log('API Called'))\n);</code></pre><p><strong>Note:</strong> Never modify data inside <code class=\"inline\">tap()</code>. It is intended only for side effects.</p><h4>Time-based Operators</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">debounceTime()</code></td><td>Wait until events stop.</td></tr><tr><td><code class=\"inline\">throttleTime()</code></td><td>Limit execution frequency.</td></tr><tr><td><code class=\"inline\">auditTime()</code></td><td>Emit the latest value after a time interval.</td></tr></table><pre><code>search.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged()\n);</code></pre><p>This is commonly used for search boxes to avoid unnecessary API requests.</p><h4>Error Handling</h4><pre><code>this.http.get('/users').pipe(\n  catchError(err => {\n    console.error(err);\n    return of([]);\n  })\n);</code></pre><p><code class=\"inline\">catchError()</code> intercepts errors and allows the Observable to recover by returning another Observable.</p><h4>Combination Operators</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">combineLatest()</code></td><td>Latest values from multiple Observables.</td></tr><tr><td><code class=\"inline\">forkJoin()</code></td><td>Wait until all Observables complete.</td></tr><tr><td><code class=\"inline\">zip()</code></td><td>Combine values one by one.</td></tr><tr><td><code class=\"inline\">merge()</code></td><td>Merge multiple streams.</td></tr></table><pre><code>forkJoin({\n  users: this.userService.getUsers(),\n  orders: this.orderService.getOrders()\n}).subscribe(result => {\n  console.log(result.users);\n  console.log(result.orders);\n});</code></pre><h4>Real-world Angular Example</h4><pre><code>this.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(search =>\n    this.userService.search(search)\n  ),\n  catchError(() => of([])),\n  takeUntil(this.destroy$)\n).subscribe(users => {\n  this.users = users;\n});</code></pre><p>This example demonstrates several operators working together:</p><ul><li><strong>debounceTime()</strong> – Wait for the user to stop typing.</li><li><strong>distinctUntilChanged()</strong> – Ignore duplicate searches.</li><li><strong>switchMap()</strong> – Cancel previous API calls.</li><li><strong>catchError()</strong> – Handle API failures.</li><li><strong>takeUntil()</strong> – Prevent memory leaks.</li></ul><h4>Interview Tip</h4><p>For Angular interviews, memorize these operators:</p><ul><li><strong>map()</strong></li><li><strong>filter()</strong></li><li><strong>tap()</strong></li><li><strong>switchMap()</strong> ⭐</li><li><strong>mergeMap()</strong></li><li><strong>concatMap()</strong></li><li><strong>forkJoin()</strong></li><li><strong>combineLatest()</strong></li><li><strong>debounceTime()</strong></li><li><strong>distinctUntilChanged()</strong></li><li><strong>takeUntil()</strong></li><li><strong>catchError()</strong></li></ul><p>Interviewers almost always ask follow-up questions comparing <code class=\"inline\">switchMap</code>, <code class=\"inline\">mergeMap</code>, <code class=\"inline\">concatMap</code>, and <code class=\"inline\">exhaustMap</code>.</p><h4>Interview Answer</h4><p><strong>\"RxJS operators are functions used inside pipe() to transform, filter, combine, or control Observable streams. Common operators include map, filter, tap, debounceTime, switchMap, mergeMap, concatMap, takeUntil, catchError, forkJoin, and combineLatest. In Angular, they are widely used with HttpClient, forms, routing, and state management to build reactive applications.\"</strong></p>",
    "revise": "The everyday operator kit:\n• **Transform**: map, filter, tap.\n• **Rate**: `debounceTime`, `distinctUntilChanged`.\n• **Flatten**: `switchMap`, `mergeMap`.\n• **Errors**: `catchError`, retry.\n• **Share/combine**: `shareReplay`, `combineLatest`, `forkJoin`."
  },
  {
    "id": "rx-error-handling",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "error",
      "catchError",
      "retry",
      "real-world"
    ],
    "question": "How do you handle errors in RxJS?",
    "answer": "<p>Use <code class=\"inline\">catchError</code> to intercept an error and return a fallback observable (or rethrow). <code class=\"inline\">retry(n)</code> / <code class=\"inline\">retryWhen</code> re-subscribe on failure.</p><p>Remember: an error <strong>terminates</strong> the stream — nothing emits after it, so handle it inside the inner observable if you want the outer stream to survive.</p>",
    "tip": "In a switchMap search, put catchError inside the inner call so one failed search doesn't kill the whole stream.",
    "code": "switchMap(term => this.api.search(term).pipe(\n  catchError(() => of([]))\n))",
    "lang": "ts",
    "deep": "<h4>The rule everything follows: an error is terminal</h4><p>An observable emits <em>either</em> many <code class=\"inline\">next</code>s then one <code class=\"inline\">complete</code>, <em>or</em> one <code class=\"inline\">error</code> — and then it's dead. Not paused. Dead. The subscription is torn down and nothing emits again. Every RxJS error question comes back to that.</p><h4>catchError returns a REPLACEMENT stream</h4><pre><code>source$.pipe(\n  catchError(err =&gt; of([]))          // swallow -&gt; emit [] then complete\n  // catchError(err =&gt; EMPTY)        // swallow -&gt; complete, emit nothing\n  // catchError(err =&gt; throwError(() =&gt; err))  // rethrow after logging\n)</code></pre><table><thead><tr><th>Return</th><th>Effect</th></tr></thead><tbody><tr><td><code class=\"inline\">of(fallback)</code></td><td>emits the fallback, then completes</td></tr><tr><td><code class=\"inline\">EMPTY</code></td><td>completes silently, no emission</td></tr><tr><td><code class=\"inline\">throwError(...)</code></td><td>rethrows to the next handler</td></tr></tbody></table><h4>The placement bug — this is the whole question</h4><pre><code>// BROKEN — one failed search kills the search box forever\ninput$.pipe(\n  switchMap(q =&gt; this.api.search(q)),\n  catchError(() =&gt; of([]))     // outer: catches, then the OUTER stream completes\n)\n\n// CORRECT — the error dies inside the inner stream\ninput$.pipe(\n  switchMap(q =&gt; this.api.search(q).pipe(\n    catchError(() =&gt; of([]))   // inner: outer never sees an error\n  ))\n)</code></pre><p><strong>Outer <code class=\"inline\">catchError</code> = the stream dies after the first failure.</strong> The user types again and nothing happens — a bug that looks like a mystery in production and is trivially explained by this rule. Same applies to NgRx effects: an uncaught error kills the effect for the app's lifetime.</p><h4>Retrying</h4><pre><code>req$.pipe(\n  retry({ count: 3, delay: (_e, i) =&gt; timer(2 ** i * 1000) }),  // backoff\n  catchError(err =&gt; { this.toast.error('Failed'); return EMPTY; })\n)</code></pre><p><code class=\"inline\">retry</code> works by <strong>re-subscribing</strong> to the source — which only makes sense because the source is cold. Never blanket-retry: a 400 or 401 will fail identically three times. Retry transient failures (network, 503) only.</p><h4>Where errors belong architecturally</h4><ul><li><strong>HTTP interceptor</strong> — cross-cutting: 401 → refresh token, 5xx → toast. Keeps components clean.</li><li><strong>Global <code class=\"inline\">ErrorHandler</code></strong> — last resort, log to Sentry.</li><li><strong>Inner <code class=\"inline\">catchError</code></strong> — when <em>this</em> stream must survive.</li></ul><h4>How to say it in an interview</h4><p>“An error is terminal, so the question is always <em>where</em> you catch it. <code class=\"inline\">catchError</code> returns a replacement observable — <code class=\"inline\">of([])</code> to fall back, <code class=\"inline\">EMPTY</code> to swallow, <code class=\"inline\">throwError</code> to rethrow. The classic bug is catching on the outer stream of a <code class=\"inline\">switchMap</code> search: it handles the first error and then the whole search box is dead. Put it on the inner observable so the outer never sees it. Cross-cutting stuff like 401s I handle once in an interceptor.”</p>",
    "revise": "• **catchError** — intercept an error; return a fallback/replacement Observable or rethrow.\n• **retry / retryWhen** — re-subscribe on transient failures.\nAn UNCAUGHT error TERMINATES the stream (no more emissions) — place `catchError` on the INNER stream (inside `switchMap`) to keep the outer alive."
  },
  {
    "id": "rx-combine",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "combineLatest",
      "forkJoin",
      "zip",
      "merge"
    ],
    "question": "combineLatest vs forkJoin vs merge vs zip.",
    "answer": "<p><strong>combineLatest</strong> emits the latest value from each Observable whenever any source emits (after all have emitted at least once). <strong>forkJoin</strong> waits for all Observables to complete and emits their last values once (similar to <code class=\"inline\">Promise.all()</code>). <strong>merge</strong> emits values from all Observables as they arrive without waiting. <strong>zip</strong> combines values by their emission order (index), waiting until each Observable has a corresponding value.</p>",
    "tip": "Remember: <strong>combineLatest = Latest Values</strong>, <strong>forkJoin = Wait for All</strong>, <strong>merge = As They Arrive</strong>, <strong>zip = Pair by Index</strong>.",
    "code": "// forkJoin Example\nforkJoin({\n  user: this.userService.getUser(),\n  roles: this.userService.getRoles()\n}).subscribe(({ user, roles }) => {\n  console.log(user, roles);\n});",
    "lang": "ts",
    "deep": "<h4>Why do we need Combination Operators?</h4><p>Real-world Angular applications often work with multiple asynchronous streams such as HTTP requests, form controls, route parameters, timers, and WebSockets. RxJS provides combination operators to coordinate these streams.</p><h4>1. combineLatest()</h4><p><strong>combineLatest()</strong> waits until every Observable has emitted at least once. After that, whenever any Observable emits, it combines the latest value from every source.</p><pre><code>const a$ = interval(1000);\nconst b$ = interval(2000);\n\ncombineLatest([a$, b$])\n  .subscribe(console.log);</code></pre><pre><code>Time →\nA: 0---1---2---3---4\nB: ----0-------1----\n\nOutput:\n[1,0]\n[2,0]\n[3,1]\n[4,1]</code></pre><p><strong>Use Cases:</strong></p><ul><li>Search + Filters</li><li>Form validation</li><li>Dashboard widgets</li><li>Route params + Query params</li></ul><h4>2. forkJoin()</h4><p><strong>forkJoin()</strong> waits until every Observable completes, then emits one object or array containing the final value from each Observable.</p><pre><code>forkJoin({\n  users: this.http.get('/users'),\n  orders: this.http.get('/orders')\n}).subscribe(result => {\n  console.log(result.users);\n  console.log(result.orders);\n});</code></pre><p><strong>Use Cases:</strong></p><ul><li>Load dashboard data.</li><li>Fetch multiple APIs on page load.</li><li>Parallel HTTP requests.</li></ul><p><strong>Important:</strong> If one Observable never completes (for example <code class=\"inline\">interval()</code>), <code class=\"inline\">forkJoin()</code> never emits.</p><h4>3. merge()</h4><p><strong>merge()</strong> combines multiple Observables into one by emitting values immediately as they arrive. It does not preserve order.</p><pre><code>const a$ = interval(1000);\nconst b$ = interval(1500);\n\nmerge(a$, b$)\n  .subscribe(console.log);</code></pre><pre><code>Time →\nA: 0---1---2---3\nB: --0----1----2\n\nOutput:\n0\n0\n1\n2\n1\n3\n2</code></pre><p><strong>Use Cases:</strong></p><ul><li>Merge multiple event streams.</li><li>Combine button clicks.</li><li>Listen to multiple WebSocket streams.</li></ul><h4>4. zip()</h4><p><strong>zip()</strong> waits until every Observable has produced the next value, then emits them together as a pair.</p><pre><code>const a$ = of('A', 'B', 'C');\nconst b$ = of(1, 2, 3);\n\nzip(a$, b$)\n  .subscribe(console.log);</code></pre><pre><code>Output:\n['A',1]\n['B',2]\n['C',3]</code></pre><p><strong>Use Cases:</strong></p><ul><li>Pair related data.</li><li>Synchronize streams.</li><li>Process records one by one.</li></ul><h4>Comparison</h4><table><tr><th>Operator</th><th>Waits?</th><th>Emits</th><th>Typical Use</th></tr><tr><td><code class=\"inline\">combineLatest</code></td><td>Until each emits once</td><td>Latest values whenever any changes</td><td>Reactive UI, Forms</td></tr><tr><td><code class=\"inline\">forkJoin</code></td><td>Until all complete</td><td>One final combined value</td><td>Parallel API Calls</td></tr><tr><td><code class=\"inline\">merge</code></td><td>No</td><td>Immediately as values arrive</td><td>Events, WebSockets</td></tr><tr><td><code class=\"inline\">zip</code></td><td>Yes (per emission)</td><td>Pairs values by index</td><td>Synchronizing streams</td></tr></table><h4>Real-world Angular Examples</h4><p><strong>combineLatest()</strong> - Update search results whenever either the search text or selected category changes.</p><pre><code>combineLatest([\n  this.search.valueChanges,\n  this.category.valueChanges\n]).subscribe(([text, category]) => {\n  this.searchProducts(text, category);\n});</code></pre><p><strong>forkJoin()</strong> - Load multiple APIs when opening a dashboard.</p><pre><code>forkJoin({\n  users: this.userService.getUsers(),\n  products: this.productService.getProducts(),\n  orders: this.orderService.getOrders()\n}).subscribe();</code></pre><p><strong>merge()</strong> - Listen to multiple button clicks.</p><pre><code>merge(saveClick$, cancelClick$)\n  .subscribe(console.log);</code></pre><p><strong>zip()</strong> - Pair usernames with generated IDs.</p><pre><code>zip(usernames$, ids$)\n  .subscribe(console.log);</code></pre><h4>Interview Tip</h4><p>The most common interview question is <strong>'When would you use combineLatest instead of forkJoin?'</strong> Answer: <strong>combineLatest</strong> is for continuously changing streams like forms and filters, while <strong>forkJoin</strong> is for one-time parallel HTTP requests where you need all results before continuing.</p><h4>Interview Answer</h4><p><strong>\"combineLatest emits whenever any source changes after each has emitted once, making it ideal for reactive UI updates. forkJoin waits for all Observables to complete and emits once, making it perfect for parallel API calls. merge emits values immediately as they arrive from multiple streams, while zip pairs emissions by index, waiting until every Observable has produced the next value. In Angular, combineLatest is commonly used with forms, forkJoin with HttpClient, merge with event streams, and zip for synchronized data processing.\"</strong></p>",
    "revise": "Combining operators:\n• **combineLatest** — emits when ANY source emits (after all have emitted once).\n• **forkJoin** — waits for ALL to COMPLETE, emits their last values once (like `Promise.all`).\n• **merge** — interleaves emissions from all sources.\n• **zip** — pairs emissions by INDEX."
  },
  {
    "id": "rx-of-from",
    "category": "rxjs",
    "difficulty": "beginner",
    "tags": [
      "creation",
      "of",
      "from"
    ],
    "question": "of vs from vs fromEvent.",
    "answer": "<ul><li><code class=\"inline\">of(1,2,3)</code> — emits the given values then completes.</li><li><code class=\"inline\">from(arrayOrPromise)</code> — turns an array/iterable/Promise into an observable.</li><li><code class=\"inline\">fromEvent(el,'click')</code> — an observable of DOM events.</li></ul>",
    "tip": "from unpacks an array element-by-element; of emits the array as a single value.",
    "code": "of([1, 2, 3]);   // one emission: [1,2,3]\nfrom([1, 2, 3]); // three emissions: 1, 2, 3",
    "lang": "ts",
    "revise": "Creation operators:\n• **of(a, b, c)** — emit the given values, then complete.\n• **from(source)** — convert an array / Promise / iterable into a stream.\n• **fromEvent(el, 'click')** — a stream of DOM/Node events."
  },
  {
    "id": "rx-pipe",
    "category": "rxjs",
    "difficulty": "beginner",
    "tags": [
      "pipe",
      "operators"
    ],
    "question": "What is the pipe() method?",
    "answer": "<p><code class=\"inline\">pipe()</code> is a method on an Observable that lets you chain multiple RxJS operators together. Each operator receives the output of the previous one and returns a new Observable without modifying the original stream. This makes data transformation, filtering, error handling, and asynchronous operations easy to compose in a readable way.</p>",
    "tip": "Think of <code class=\"inline\">pipe()</code> as an assembly line. Each operator performs one job and passes the result to the next operator.",
    "code": "this.http.get<User[]>('/api/users').pipe(\n  map(users => users.filter(u => u.active)),\n  tap(() => console.log('Users loaded')),\n  catchError(() => of([]))\n).subscribe(users => {\n  console.log(users);\n});",
    "lang": "ts",
    "deep": "<h4>Why do we need pipe()?</h4><p>Observables emit values over time. Before those values reach the subscriber, we often need to transform them, filter them, log them, delay them, or handle errors. Instead of writing all this logic inside <code class=\"inline\">subscribe()</code>, RxJS provides <code class=\"inline\">pipe()</code> to process the data step by step.</p><pre><code>Observable\n      ↓\npipe()\n      ↓\nOperator 1\n      ↓\nOperator 2\n      ↓\nOperator 3\n      ↓\nSubscriber</code></pre><p>Each operator receives the output of the previous operator.</p><h4>Simple Example</h4><pre><code>of(1,2,3,4).pipe(\n  map(x => x * 10),\n  filter(x => x > 20)\n).subscribe(console.log);\n\n// Output\n30\n40</code></pre><p>Execution happens like this:</p><pre><code>1,2,3,4\n      ↓\nmap(x * 10)\n10,20,30,40\n      ↓\nfilter(x > 20)\n30,40\n      ↓\nsubscribe()</code></pre><h4>pipe() does NOT modify the original Observable</h4><p>Every operator returns a <strong>new Observable</strong>.</p><pre><code>const source$ = of(1,2,3);\n\nconst doubled$ = source$.pipe(\n  map(x => x * 2)\n);\n\nsource$.subscribe(console.log);\n// 1 2 3\n\ndoubled$.subscribe(console.log);\n// 2 4 6</code></pre><p>The original Observable remains unchanged.</p><h4>Common Operators inside pipe()</h4><table><tr><th>Operator</th><th>Purpose</th></tr><tr><td><code class=\"inline\">map()</code></td><td>Transform values.</td></tr><tr><td><code class=\"inline\">filter()</code></td><td>Keep matching values.</td></tr><tr><td><code class=\"inline\">tap()</code></td><td>Logging or debugging.</td></tr><tr><td><code class=\"inline\">debounceTime()</code></td><td>Wait before emitting.</td></tr><tr><td><code class=\"inline\">switchMap()</code></td><td>Switch to another Observable.</td></tr><tr><td><code class=\"inline\">catchError()</code></td><td>Handle errors.</td></tr><tr><td><code class=\"inline\">takeUntil()</code></td><td>Auto unsubscribe.</td></tr></table><h4>Real-world Angular Example</h4><pre><code>this.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(text =>\n    this.userService.search(text)\n  ),\n  catchError(() => of([]))\n).subscribe(users => {\n  this.users = users;\n});</code></pre><p>Here every operator has one responsibility:</p><ol><li><strong>debounceTime()</strong> → Wait until the user stops typing.</li><li><strong>distinctUntilChanged()</strong> → Ignore duplicate searches.</li><li><strong>switchMap()</strong> → Cancel the previous API call.</li><li><strong>catchError()</strong> → Handle API failures.</li><li><strong>subscribe()</strong> → Receive the final result.</li></ol><h4>Why not write everything inside subscribe()?</h4><pre><code>// ❌ Not Recommended\nthis.http.get('/users').subscribe(users => {\n  const active = users.filter(u => u.active);\n  console.log(active);\n});</code></pre><p>Instead:</p><pre><code>// ✅ Recommended\nthis.http.get('/users').pipe(\n  map(users => users.filter(u => u.active))\n).subscribe(console.log);</code></pre><p>The data is transformed before it reaches the subscriber, making the code cleaner and easier to test.</p><h4>Execution Order</h4><p>Operators execute from left to right.</p><pre><code>source$\n  .pipe(\n    map(...),\n    filter(...),\n    tap(...),\n    catchError(...)\n  )\n  .subscribe();</code></pre><p>The output of <code class=\"inline\">map()</code> becomes the input to <code class=\"inline\">filter()</code>, then to <code class=\"inline\">tap()</code>, and so on.</p><h4>Interview Tip</h4><p>Interviewers often ask <strong>'Why do we use pipe()?' </strong> A good answer is: <strong>pipe() allows us to compose multiple RxJS operators into a readable processing pipeline. Each operator performs one transformation and returns a new Observable, making the code modular, reusable, and easier to maintain.</strong></p><h4>Interview Answer</h4><p><strong>\"pipe() is used to chain RxJS operators together. Each operator receives the output of the previous operator and returns a new Observable. This lets us transform, filter, combine, delay, or handle errors in a clean, declarative way before the data reaches subscribe(). In Angular, pipe() is commonly used with HttpClient, forms, routing, and state management.\"</strong></p>",
    "revise": "pipe(...operators) composes operators into a NEW Observable.\n• It's the standard way to chain transformations (`source.pipe(map(...), filter(...))`).\n• **Pure and reusable** — the source stays untouched; each operator returns a new Observable."
  },
  {
    "id": "rx-distinct",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "distinctUntilChanged",
      "debounce"
    ],
    "question": "Why pair debounceTime with distinctUntilChanged in search?",
    "answer": "<p><code class=\"inline\">debounceTime</code> waits for the user to pause typing; <code class=\"inline\">distinctUntilChanged</code> drops emissions equal to the previous one (e.g. type then delete back to the same term) so you don't fire a duplicate request.</p>",
    "tip": "This trio — debounceTime, distinctUntilChanged, switchMap — is the canonical search pattern.",
    "code": "",
    "lang": "",
    "revise": "For type-ahead, pair two operators:\n• **debounceTime(300)** — wait until the user PAUSES typing.\n• **distinctUntilChanged** — skip when the term is the SAME as last time.\nTogether they cut needless, duplicate API calls."
  },
  {
    "id": "rx-startwith",
    "category": "rxjs",
    "difficulty": "beginner",
    "tags": [
      "startWith",
      "operators"
    ],
    "question": "What does startWith do?",
    "answer": "<p><code class=\"inline\">startWith()</code> emits one or more values immediately when an Observable is subscribed to, before the source Observable starts emitting its own values. It is commonly used to provide an initial value so the UI has something to display while waiting for real data.</p>",
    "tip": "Think of <code class=\"inline\">startWith()</code> as adding a default value at the beginning of an Observable stream.",
    "code": "this.items$ = this.http.get<Item[]>('/api/items').pipe(\n  startWith([])\n);\n\nthis.searchControl.valueChanges.pipe(\n  startWith('')\n).subscribe(console.log);",
    "lang": "ts",
    "deep": "<h4>What does startWith() do?</h4><p><code class=\"inline\">startWith()</code> prepends one or more values to an Observable. These values are emitted immediately when someone subscribes, before the original Observable produces its own values.</p><pre><code>Source Observable\n      ↓\nstartWith('Initial')\n      ↓\nSubscriber\n\nOutput:\nInitial\nActual Values...</code></pre><h4>Simple Example</h4><pre><code>of(2, 3, 4).pipe(\n  startWith(1)\n).subscribe(console.log);\n\n// Output\n1\n2\n3\n4</code></pre><p>The value <code class=\"inline\">1</code> is emitted before the source Observable starts.</p><h4>Why is it Useful?</h4><p>Many Observables do not emit immediately. For example, <code class=\"inline\">valueChanges</code> only emits after the user changes a value.</p><pre><code>this.searchControl.valueChanges\n  .subscribe(console.log);</code></pre><p>Initially, nothing is emitted.</p><p>Using <code class=\"inline\">startWith()</code>:</p><pre><code>this.searchControl.valueChanges.pipe(\n  startWith('')\n).subscribe(console.log);</code></pre><p>The subscriber immediately receives an empty string, allowing the UI to initialize.</p><h4>Real-world Angular Example 1 - Search</h4><pre><code>this.filteredUsers$ = this.searchControl.valueChanges.pipe(\n  startWith(''),\n  switchMap(text =>\n    this.userService.search(text)\n  )\n);</code></pre><p>Without <code class=\"inline\">startWith('')</code>, the first API call would happen only after the user typed something. With it, the full user list loads immediately.</p><h4>Real-world Angular Example 2 - Initial Empty List</h4><pre><code>this.items$ = this.http.get<Item[]>('/api/items').pipe(\n  startWith([])\n);</code></pre><p>The template receives an empty array immediately, preventing undefined errors while waiting for the HTTP response.</p><h4>Real-world Angular Example 3 - Loading State</h4><pre><code>this.loading$ = this.http.get('/users').pipe(\n  map(() => false),\n  startWith(true)\n);</code></pre><p>The UI displays a loading spinner immediately, then changes to <code class=\"inline\">false</code> when the request finishes.</p><h4>Comparison</h4><table><tr><th>Without startWith()</th><th>With startWith()</th></tr><tr><td>No initial value</td><td>Initial value emitted immediately</td></tr><tr><td>UI waits for first emission</td><td>UI can render immediately</td></tr><tr><td>Possible undefined state</td><td>Default state available</td></tr></table><h4>Common Uses</h4><ul><li>Initialize form controls.</li><li>Display an empty list before API data arrives.</li><li>Show loading state.</li><li>Trigger an initial search.</li><li>Provide default filter values.</li></ul><h4>Interview Tip</h4><p>Interviewers often ask why <code class=\"inline\">startWith('')</code> is used with <code class=\"inline\">valueChanges</code>. The answer is that <code class=\"inline\">valueChanges</code> does not emit until the user changes the input. <code class=\"inline\">startWith()</code> provides an initial value so filtering or searching can happen immediately.</p><h4>Interview Answer</h4><p><strong>\"startWith() emits one or more initial values before the source Observable begins emitting. It is commonly used in Angular to initialize UI state, provide default values, trigger an initial search, or avoid undefined values while waiting for asynchronous data.\"</strong></p>",
    "revise": "`startWith`(value) emits an INITIAL value immediately, before the source's own emissions.\n• Great for a default/loading UI state so the template has something to render right away.\n• Common with `combineLatest` to avoid waiting on every source."
  },
  {
    "id": "rx-typeahead",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "pattern",
      "search",
      "switchMap",
      "real-world"
    ],
    "question": "Show the canonical type-ahead search pipeline.",
    "answer": "<p>Combine the four operators every interviewer wants: <code class=\"inline\">debounceTime</code> (wait for a pause), <code class=\"inline\">distinctUntilChanged</code> (skip duplicate terms), <code class=\"inline\">switchMap</code> (cancel stale requests), and <code class=\"inline\">catchError</code> inside the inner call (so one failure doesn't kill the stream).</p>",
    "tip": "Put catchError on the inner observable, not the outer — otherwise the stream dies after one error.",
    "code": "input$.pipe(\n  debounceTime(300),\n  distinctUntilChanged(),\n  switchMap(q => this.api.search(q).pipe(catchError(() => of([]))))\n)",
    "lang": "ts",
    "deep": "<h4>The full pipeline</h4><pre><code>results$ = this.searchControl.valueChanges.pipe(\n  debounceTime(300),                    // 1. wait for a pause in typing\n  distinctUntilChanged(),               // 2. skip if the term didn't change\n  filter(term =&gt; term.length &gt;= 2),     // 3. don't search on 1 char\n  switchMap(term =&gt; this.api.search(term).pipe(   // 4. cancel stale requests\n    catchError(() =&gt; of([]))            // 5. inner — keep the stream alive\n  )),\n  takeUntilDestroyed()                  // 6. clean up\n);</code></pre><p>In the template: <code class=\"inline\">results$ | async</code> — no manual subscription at all.</p><h4>Why each operator earns its place</h4><table><thead><tr><th>Operator</th><th>Remove it and…</th></tr></thead><tbody><tr><td><code class=\"inline\">debounceTime(300)</code></td><td>a request per keystroke — \"angular\" = 7 calls</td></tr><tr><td><code class=\"inline\">distinctUntilChanged()</code></td><td>type \"a\", delete, retype \"a\" → duplicate call</td></tr><tr><td><code class=\"inline\">filter(len &gt;= 2)</code></td><td>\"a\" returns half the database</td></tr><tr><td><code class=\"inline\">switchMap</code></td><td><strong>race condition</strong> — see below</td></tr><tr><td>inner <code class=\"inline\">catchError</code></td><td>one failed search kills the box permanently</td></tr></tbody></table><h4>The race condition — why switchMap, not mergeMap</h4><p>You type <code class=\"inline\">\"ang\"</code> then <code class=\"inline\">\"angular\"</code>. Two requests are in flight. The <code class=\"inline\">\"ang\"</code> response is slower and lands <em>second</em>.</p><ul><li><code class=\"inline\">mergeMap</code> — both complete; the stale <code class=\"inline\">\"ang\"</code> results overwrite <code class=\"inline\">\"angular\"</code>. The user sees results for a query they've already replaced. <strong>Intermittent, timing-dependent, miserable to debug.</strong></li><li><code class=\"inline\">switchMap</code> — subscribing to the new inner observable <em>unsubscribes</em> the previous one, which cancels the in-flight XHR. Only the latest response can arrive.</li></ul><p>That cancellation is real: Angular's <code class=\"inline\">HttpClient</code> calls <code class=\"inline\">xhr.abort()</code> on unsubscribe, so you'll see the request go red/cancelled in the Network tab. That's the answer to \"prove switchMap cancels\".</p><h4>Adding a loading indicator</h4><pre><code>switchMap(term =&gt; this.api.search(term).pipe(\n  catchError(() =&gt; of([])),\n  finalize(() =&gt; this.loading.set(false))   // always runs\n))</code></pre><h4>Where else the same shape appears</h4><p>Autocomplete, filter-as-you-type tables, live validation (username availability), and NgRx effects handling a search action. Same five operators.</p><h4>How to say it in an interview</h4><p>“<code class=\"inline\">debounceTime</code> to wait for a pause, <code class=\"inline\">distinctUntilChanged</code> to drop repeats, a <code class=\"inline\">filter</code> for minimum length, then <code class=\"inline\">switchMap</code> into the API with <code class=\"inline\">catchError</code> <em>inside</em>. <code class=\"inline\">switchMap</code> is the important choice — it cancels the in-flight request, so a slow earlier response can't overwrite the latest results. With <code class=\"inline\">mergeMap</code> you'd get an intermittent race where stale results win.”</p>",
    "revise": "The canonical type-ahead pipeline:\n• `valueChanges` → `debounceTime` → `distinctUntilChanged` → `switchMap`(term => search(term)).\n• `switchMap` CANCELS the previous request, so only the LATEST result renders (no stale/out-of-order).\nAdd `catchError` inside `switchMap` so one failure doesn't kill the stream."
  },
  {
    "id": "rx-polling",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "polling",
      "timer",
      "interval"
    ],
    "question": "How do you poll an API every N seconds with RxJS?",
    "answer": "<p>Use <code class=\"inline\">timer(0, N)</code> to emit immediately then every N ms, and <code class=\"inline\">switchMap</code> to the request (so a slow response doesn't overlap the next tick). Stop with <code class=\"inline\">takeUntil</code>.</p>",
    "tip": "switchMap prevents overlapping requests if one poll is slower than the interval.",
    "code": "timer(0, 5000).pipe(\n  switchMap(() => this.api.getStatus()),\n  takeUntil(this.destroy$)\n)",
    "lang": "ts",
    "revise": "Poll every N seconds:\n• timer(0, N) (or interval) → `switchMap`(() => `apiCall()`).\n• `switchMap` so a slow response doesn't pile up requests.\n• Clean up with `takeUntilDestroyed` / `takeUntil`."
  },
  {
    "id": "rx-retry-backoff",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "error",
      "retry",
      "backoff",
      "real-world"
    ],
    "question": "How do you retry a failed request with exponential backoff?",
    "answer": "<p>Use <code class=\"inline\">retry</code> with a <code class=\"inline\">delay</code> function (RxJS 7.4+) that returns an increasing delay per attempt, or <code class=\"inline\">retryWhen</code> in older versions. Cap the number of retries so it doesn't loop forever.</p>",
    "tip": "Exponential backoff avoids hammering a struggling server — mention capping the retries.",
    "code": "req$.pipe(\n  retry({ count: 3, delay: (_e, i) => timer(2 ** i * 1000) })\n)",
    "lang": "ts",
    "deep": "<h4>The modern form (RxJS 7.4+)</h4><pre><code>req$.pipe(\n  retry({\n    count: 3,\n    delay: (error, retryCount) =&gt; {\n      if (error.status &gt;= 400 &amp;&amp; error.status &lt; 500) throw error;  // don't retry 4xx\n      return timer(2 ** retryCount * 1000);   // 2s, 4s, 8s\n    }\n  }),\n  catchError(err =&gt; { this.toast.error('Service unavailable'); return EMPTY; })\n)</code></pre><p><code class=\"inline\">retryWhen</code> is <strong>deprecated</strong> — knowing that is a quick credibility marker. Its replacement is the <code class=\"inline\">delay</code> callback above, which is far easier to read.</p><h4>How retry actually works</h4><p>It <strong>re-subscribes to the source</strong>. That's only meaningful because HTTP observables are <em>cold</em> — re-subscribing genuinely re-issues the request. Retrying a hot observable would just re-listen, not re-run anything. It's a neat place to show the cold/hot concept paying off.</p><h4>Why exponential, and why cap it</h4><p>A struggling server fails, so every client retries at once, so it gets more load and fails harder — a <strong>retry storm</strong>. Exponential backoff spreads attempts out (2s, 4s, 8s) to give it room to recover. The <code class=\"inline\">count</code> cap stops you retrying forever and turning a transient blip into an infinite loop.</p><h4>What you must NOT retry</h4><table><thead><tr><th>Retry</th><th>Never retry</th></tr></thead><tbody><tr><td>network / timeout</td><td><strong>400</strong> — the payload is wrong; it'll fail identically</td></tr><tr><td>503, 502, 429</td><td><strong>401 / 403</strong> — refresh the token instead</td></tr><tr><td>idempotent GET/PUT</td><td><strong>404</strong> — it won't appear</td></tr><tr><td></td><td><strong>non-idempotent POST</strong> — may create duplicates</td></tr></tbody></table><p>Blanket <code class=\"inline\">retry(3)</code> on every request is the mistake: retrying a POST three times can create three orders. Mentioning idempotency here is exactly the seniority signal.</p><h4>Jitter — the detail almost nobody gives</h4><pre><code>delay: (_e, i) =&gt; timer(2 ** i * 1000 + Math.random() * 1000)</code></pre><p>Pure exponential backoff still synchronises clients — they all retry at exactly 2s. Random jitter de-synchronises them. This is standard practice in AWS/Google client libraries.</p><h4>In a real app: put it in an interceptor</h4><pre><code>intercept(req: HttpRequest&lt;any&gt;, next: HttpHandler) {\n  return next.handle(req).pipe(\n    retry({ count: 2, delay: (e, i) =&gt;\n      e.status &gt;= 500 ? timer(2 ** i * 1000) : throwError(() =&gt; e) })\n  );\n}</code></pre><h4>How to say it in an interview</h4><p>“<code class=\"inline\">retry({ count, delay })</code> since RxJS 7.4 — <code class=\"inline\">retryWhen</code>'s deprecated. It works by re-subscribing, which only re-issues the request because HTTP is cold. I use exponential backoff with jitter so clients don't all retry in lockstep and cause a retry storm, cap the count, and — most importantly — only retry transient failures. Retrying a 400 fails identically three times, and retrying a POST can create duplicate records.”</p>",
    "revise": "Retry with EXPONENTIAL backoff:\n• **retry({ count, delay })** — return a delayed Observable whose wait GROWS each attempt (e.g. 1s, 2s, 4s).\n• Or `retryWhen` with an expanding timer.\n• Finish with `catchError` for a final fallback after retries are exhausted."
  },
  {
    "id": "rx-shareReplay-cache",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "caching",
      "shareReplay",
      "multicast",
      "memory-leak"
    ],
    "question": "How do you cache an HTTP result across subscribers?",
    "answer": "<p>Pipe the request through <code class=\"inline\">shareReplay(1)</code>: the underlying call runs <strong>once</strong>, and every current <em>and future</em> subscriber gets the last emitted value — a one-line in-memory cache.</p><p>The buffer size <code class=\"inline\">1</code> means \"replay the most recent value\". Without <code class=\"inline\">shareReplay</code>, each subscriber re-runs the cold HTTP call.</p>",
    "tip": "shareReplay(1) turns a per-subscriber HTTP call into a single shared, cached one.",
    "code": "readonly config$ = this.http.get('/config').pipe(shareReplay(1));",
    "lang": "ts",
    "deep": "<h4>The pattern</h4><pre><code>@Injectable({ providedIn: 'root' })\nexport class ConfigService {\n  readonly config$ = this.http.get&lt;Config&gt;('/api/config').pipe(\n    shareReplay(1)\n  );\n}\n// 10 components subscribe -&gt; 1 HTTP call, 10 subscribers get the value</code></pre><p>Note it's a <strong>field, not a method</strong>. <code class=\"inline\">getConfig()</code> returning a new piped observable each call would rebuild the pipeline and defeat the cache entirely — a subtle and very common mistake.</p><h4>The trap: shareReplay can leak</h4><pre><code>// LEAK — the source stays subscribed FOREVER, even with zero subscribers\ninterval(1000).pipe(shareReplay(1)).subscribe(...);  // timer never stops\n\n// SAFE — drops the source when the last subscriber leaves\ninterval(1000).pipe(shareReplay({ bufferSize: 1, refCount: true }))</code></pre><p>By default <code class=\"inline\">refCount</code> is <strong>false</strong>: <code class=\"inline\">shareReplay</code> subscribes to the source and <em>never</em> unsubscribes, keeping it alive for the app's lifetime. Harmless for a finite <code class=\"inline\">http.get()</code>, a real leak for an infinite source.</p><h4>So which do you want?</h4><table><thead><tr><th></th><th><code class=\"inline\">refCount: false</code> (default)</th><th><code class=\"inline\">refCount: true</code></th></tr></thead><tbody><tr><td>Source when subscribers hit 0</td><td>stays subscribed</td><td>unsubscribed</td></tr><tr><td>Cached value</td><td>kept forever</td><td>discarded</td></tr><tr><td>Good for</td><td>config/lookup caching</td><td>infinite sources</td></tr><tr><td>Risk</td><td>leaks infinite sources</td><td>re-fetches after last unsubscribe</td></tr></tbody></table><p>Counter-intuitively, for HTTP caching the default (<code class=\"inline\">refCount: false</code>) is what you <em>want</em> — it's precisely what keeps the value cached after everyone unsubscribes. Adding <code class=\"inline\">refCount: true</code> to an HTTP cache means navigating away and back re-fetches.</p><h4>share() vs shareReplay()</h4><p><code class=\"inline\">share()</code> multicasts but replays nothing — a subscriber arriving after the HTTP response gets <strong>nothing, forever</strong>, because the call already completed. That's why caching always uses <code class=\"inline\">shareReplay(1)</code>.</p><h4>Cache invalidation</h4><pre><code>private refresh$ = new BehaviorSubject&lt;void&gt;(undefined);\n\nreadonly users$ = this.refresh$.pipe(\n  switchMap(() =&gt; this.http.get&lt;User[]&gt;('/users')),\n  shareReplay(1)\n);\nrefresh() { this.refresh$.next(); }   // re-fetch, all subscribers update</code></pre><h4>How to say it in an interview</h4><p>“<code class=\"inline\">shareReplay(1)</code> multicasts the cold HTTP call so it runs once and replays the last value to late subscribers — but it has to be a shared field, not a method, or you rebuild the pipeline each call. The catch is <code class=\"inline\">refCount</code>: it defaults to false, so the source is never unsubscribed. For an <code class=\"inline\">http.get</code> that's exactly what you want for caching; for an <code class=\"inline\">interval</code> it's a leak, so there I pass <code class=\"inline\">refCount: true</code>.”</p>",
    "revise": "Cache an HTTP result across subscribers:\n• `shareReplay({ bufferSize: 1, refCount: true })` — MULTICASTS one execution and REPLAYS the last value to late subscribers.\n• Prevents N identical HTTP calls for N subscribers.\n• `refCount`:true unsubscribes the source when no one listens."
  },
  {
    "id": "rx-scan",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "scan",
      "state",
      "reduce"
    ],
    "question": "What does scan do (vs reduce)?",
    "answer": "<p><code class=\"inline\">scan</code> is like <code class=\"inline\">reduce</code> but emits the accumulated value on <em>every</em> emission, not just at the end — perfect for accumulating state over a stream (a running total, or building up a list from actions).</p>",
    "tip": "scan is the RxJS building block for a reducer-style state stream.",
    "code": "actions$.pipe(\n  scan((state, action) => reducer(state, action), initial)\n)",
    "lang": "ts",
    "deep": "<h4>scan vs reduce</h4><pre><code>const nums$ = of(1, 2, 3);\n\nnums$.pipe(reduce((a, n) =&gt; a + n, 0)).subscribe(console.log);\n// 6                      -- ONE value, only on complete\n\nnums$.pipe(scan((a, n) =&gt; a + n, 0)).subscribe(console.log);\n// 1, 3, 6                -- every intermediate value</code></pre><p>Same signature, different timing. <code class=\"inline\">reduce</code> waits for <code class=\"inline\">complete</code> and emits once; <code class=\"inline\">scan</code> emits the running accumulator on every source emission.</p><p><strong>The consequence:</strong> <code class=\"inline\">reduce</code> on an infinite stream (<code class=\"inline\">interval</code>, a Subject, clicks) emits <strong>nothing, ever</strong> — it's waiting for a completion that never comes. On live streams you almost always want <code class=\"inline\">scan</code>.</p><h4>Why it matters: scan is a reducer</h4><pre><code>scan((state, action) =&gt; reducer(state, action), initialState)</code></pre><p>That signature is <em>identical</em> to Redux/NgRx. A store is, at its core, a stream of actions run through <code class=\"inline\">scan</code> and multicast:</p><pre><code>// A working mini-store in 5 lines\nprivate actions$ = new Subject&lt;Action&gt;();\n\nreadonly state$ = this.actions$.pipe(\n  scan((state, action) =&gt; reducer(state, action), initialState),\n  shareReplay(1)          // late subscribers get current state\n);\n\ndispatch(action: Action) { this.actions$.next(action); }</code></pre><p>Being able to write that is a genuinely strong answer to “do you actually need NgRx?” — it shows you understand what the library is doing rather than treating it as magic.</p><h4>Everyday uses</h4><pre><code>// accumulate paginated results\nloadMore$.pipe(\n  concatMap(page =&gt; this.api.getPage(page)),\n  scan((all, page) =&gt; [...all, ...page], [] as Item[])\n)\n\n// a running counter\nclicks$.pipe(scan(n =&gt; n + 1, 0))\n\n// undo history\nactions$.pipe(scan((hist, a) =&gt; [...hist, a], []))</code></pre><h4>Keep it pure</h4><pre><code>// BROKEN — mutating breaks OnPush and replay\nscan((acc, x) =&gt; { acc.push(x); return acc; }, [])\n// CORRECT — new reference each time\nscan((acc, x) =&gt; [...acc, x], [])</code></pre><p>Mutating the accumulator returns the <em>same reference</em>, so <code class=\"inline\">OnPush</code> and <code class=\"inline\">distinctUntilChanged</code> see \"no change\" and skip the update.</p><h4>How to say it in an interview</h4><p>“Same reducer signature, different timing: <code class=\"inline\">reduce</code> emits once on complete, <code class=\"inline\">scan</code> emits the accumulator every time. So on an infinite stream <code class=\"inline\">reduce</code> never emits at all. <code class=\"inline\">scan</code> is really a reducer over a stream — <code class=\"inline\">scan</code> plus <code class=\"inline\">shareReplay(1)</code> over a Subject of actions is essentially a miniature NgRx store. And the accumulator has to stay immutable or OnPush won't see the change.”</p>",
    "revise": "• **scan** — like reduce but emits the running accumulator on EVERY value (accumulate over time).\n• **reduce** — emits only ONCE, on complete.\nUse scan for live totals/state that update as values arrive."
  },
  {
    "id": "rx-withlatestfrom",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "withLatestFrom",
      "combineLatest",
      "real-world"
    ],
    "question": "combineLatest vs withLatestFrom.",
    "answer": "<p><strong>combineLatest</strong> emits when <em>any</em> source emits. <strong>withLatestFrom</strong> emits only when the <em>source</em> emits, pulling in the latest value of the others — use it when one stream is the trigger and the rest are just context.</p>",
    "tip": "withLatestFrom = 'when A fires, also give me the current B' — one driver, others passive.",
    "code": "saveClicks$.pipe(\n  withLatestFrom(this.formValue$),\n  switchMap(([_, form]) => this.api.save(form))\n)",
    "lang": "ts",
    "deep": "<h4>Trigger vs context</h4><p>The distinction is <strong>who is allowed to cause an emission</strong>.</p><ul><li><code class=\"inline\">combineLatest([a$, b$])</code> — <em>either</em> can fire it. Emits whenever <strong>any</strong> source emits (once all have a value).</li><li><code class=\"inline\">a$.pipe(withLatestFrom(b$))</code> — <strong>only <code class=\"inline\">a$</code></strong> fires it. <code class=\"inline\">b$</code> is passive context, sampled at that moment.</li></ul><pre><code>a$: --1-----2-----3---\nb$: ----A-----B-------\n\ncombineLatest:  ----1A--2A-2B--3B---   // every change from either\nwithLatestFrom: --------2A----3B---   // only when a$ emits (1 dropped: no b yet)</code></pre><h4>The classic use: save button + form value</h4><pre><code>// The click is the trigger; the form value is just context.\nthis.saveClicks$.pipe(\n  withLatestFrom(this.form.valueChanges),\n  switchMap(([_click, form]) =&gt; this.api.save(form))\n).subscribe();</code></pre><p>With <code class=\"inline\">combineLatest</code> this would be a <strong>bug</strong>: every keystroke changes <code class=\"inline\">valueChanges</code>, which would fire a save on every character typed. The user never clicked. That's the answer to \"why not combineLatest here?\"</p><h4>The gotcha: it silently drops early emissions</h4><pre><code>trigger$.pipe(withLatestFrom(config$))   // config$ hasn't emitted yet</code></pre><p>If the \"other\" observable has <strong>no value yet</strong>, the trigger's emission is <strong>dropped silently</strong> — no error, nothing. Click save before config loads and nothing happens. Fix by making the other stream a <code class=\"inline\">BehaviorSubject</code>/<code class=\"inline\">shareReplay(1)</code> so it always has a current value.</p><h4>Argument order</h4><pre><code>a$.pipe(withLatestFrom(b$, c$))\n  .subscribe(([a, b, c]) =&gt; ...);   // source is ALWAYS first</code></pre><h4>Where you'll actually use it — NgRx effects</h4><pre><code>saveOrder$ = createEffect(() =&gt; this.actions$.pipe(\n  ofType(saveOrder),\n  withLatestFrom(this.store.select(selectUserId)),   // action triggers; state is context\n  switchMap(([action, userId]) =&gt; this.api.save(action.order, userId))\n));</code></pre><p>Using <code class=\"inline\">combineLatest</code> there would re-run the effect whenever the <em>store</em> changed — firing saves nobody asked for. (Modern NgRx prefers <code class=\"inline\">concatLatestFrom</code>, which is the lazy version — the selector isn't subscribed until the action arrives.)</p><h4>How to say it in an interview</h4><p>“<code class=\"inline\">combineLatest</code> emits when any source emits; <code class=\"inline\">withLatestFrom</code> only when the source emits and just samples the others. So it's for one trigger plus passive context — a save click pulling in the current form value. With <code class=\"inline\">combineLatest</code> there, every keystroke would trigger a save. The gotcha is that if the other stream hasn't emitted yet the trigger is dropped silently, so I make sure it's a BehaviorSubject or shareReplay'd.”</p>",
    "revise": "• **withLatestFrom** — emits ONLY when the PRIMARY source emits, sampling the latest of the others.\n• **combineLatest** — emits when ANY source emits.\nUse `withLatestFrom` when one stream is the trigger and the rest are context (e.g. an action + current state)."
  },
  {
    "id": "rx-finalize",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "finalize",
      "cleanup"
    ],
    "question": "What is finalize used for?",
    "answer": "<p><code class=\"inline\">finalize</code> runs a callback when the observable <strong>completes, errors, or is unsubscribed</strong> — the RxJS equivalent of <code class=\"inline\">finally</code>. Common use: hide a loading spinner regardless of success or failure.</p>",
    "tip": "finalize is the clean place to turn off a loading flag — it always runs.",
    "code": "this.loading = true;\nreq$.pipe(finalize(() => this.loading = false)).subscribe();",
    "lang": "ts",
    "revise": "finalize(fn) runs on complete, error, OR unsubscribe — always exactly once at teardown.\n• Perfect for clearing a loading flag / hiding a spinner regardless of outcome.\n• Runs AFTER `catchError`'s replacement completes."
  },
  {
    "id": "rx-throttle-audit-sample",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "debounceTime",
      "throttleTime",
      "auditTime",
      "sampleTime"
    ],
    "question": "debounceTime vs throttleTime vs auditTime vs sampleTime.",
    "answer": "<ul><li><strong>debounceTime</strong> — emit after a quiet gap (last value).</li><li><strong>throttleTime</strong> — emit the first value, then ignore for a window.</li><li><strong>auditTime</strong> — ignore for a window, then emit the <em>latest</em> value.</li><li><strong>sampleTime</strong> — emit the latest value on a fixed interval.</li></ul>",
    "tip": "debounce for search, throttle for scroll/resize, sample for periodic snapshots.",
    "code": "// search — wait until they stop typing\ninput$.pipe(debounceTime(300))\n\n// scroll/resize — react immediately, then at most 1/100ms\nscroll$.pipe(throttleTime(100))\n\n// high-frequency -> render at most 1 per frame, with the LATEST value\nmousemove$.pipe(auditTime(16))\n\n// periodic snapshot of a live value\nprice$.pipe(sampleTime(1000))",
    "lang": "ts",
    "deep": "<h4>The four, on one timeline</h4><p>Source emits rapidly: <code class=\"inline\">1-2-3-------4-5---</code></p><table><thead><tr><th>Operator</th><th>Emits</th><th>Which value</th><th>Timer starts on</th></tr></thead><tbody><tr><td><code class=\"inline\">debounceTime(t)</code></td><td>after <strong>t of silence</strong></td><td>the <strong>last</strong></td><td>every emission (resets)</td></tr><tr><td><code class=\"inline\">throttleTime(t)</code></td><td><strong>immediately</strong>, then mutes for t</td><td>the <strong>first</strong></td><td>first emission</td></tr><tr><td><code class=\"inline\">auditTime(t)</code></td><td>at the <strong>end</strong> of the window</td><td>the <strong>latest</strong></td><td>first emission</td></tr><tr><td><code class=\"inline\">sampleTime(t)</code></td><td>on a <strong>fixed clock</strong></td><td>the <strong>latest</strong></td><td>independent interval</td></tr></tbody></table><h4>The two axes that actually distinguish them</h4><ol><li><strong>First value or last value?</strong> <code class=\"inline\">throttleTime</code> is the only one that's eager — it emits <em>immediately</em>, then goes quiet. The other three emit a value from the <em>end</em> of some window.</li><li><strong>Does the timer reset?</strong> Only <code class=\"inline\">debounceTime</code> restarts on every emission — which is why <strong>a user typing continuously for 10 seconds produces zero emissions</strong>. That's the debounce gotcha: with a fast typist and <code class=\"inline\">debounceTime(1000)</code>, the search never fires until they pause.</li></ol><h4>Choosing in practice</h4><ul><li><strong>Search box</strong> → <code class=\"inline\">debounceTime(300)</code>. You only care about the final term, and waiting for a pause is exactly right.</li><li><strong>Scroll / resize handler</strong> → <code class=\"inline\">throttleTime(100)</code>. You want an <em>immediate</em> response (debounce would feel laggy — nothing happens until scrolling stops), just not 60×/second.</li><li><strong>Button double-click guard</strong> → <code class=\"inline\">throttleTime(1000)</code> — first click wins, rest ignored. (Though <code class=\"inline\">exhaustMap</code> is better if the guard is about an in-flight request.)</li><li><strong>mousemove → render</strong> → <code class=\"inline\">auditTime(16)</code>. Throttle would give you the <em>stale first</em> position of each window; audit gives the <em>latest</em>. For following a cursor, latest is what you want.</li><li><strong>Live price/telemetry snapshot</strong> → <code class=\"inline\">sampleTime(1000)</code>. \"Whatever the value is each second.\"</li></ul><h4>throttleTime's config (the follow-up)</h4><pre><code>throttleTime(100, asyncScheduler, { leading: true, trailing: false })  // default\nthrottleTime(100, asyncScheduler, { leading: false, trailing: true })  // ~= auditTime</code></pre><p>Default is leading-only. Set <code class=\"inline\">trailing: true</code> and you also get the last value of each window — which is how <code class=\"inline\">throttleTime</code> and <code class=\"inline\">auditTime</code> converge.</p><h4>The trap: debounce ≠ throttle for scroll</h4><p>Using <code class=\"inline\">debounceTime</code> on scroll means your sticky header does nothing while the user scrolls and snaps into place after they stop. Feels broken. Scroll wants <code class=\"inline\">throttleTime</code> or <code class=\"inline\">auditTime</code>.</p><h4>How to say it in an interview</h4><p>“<code class=\"inline\">debounceTime</code> waits for a pause and emits the last value — search. <code class=\"inline\">throttleTime</code> emits the first immediately then mutes — scroll, where you need an instant response. <code class=\"inline\">auditTime</code> waits out the window then emits the <em>latest</em> — better than throttle for mousemove, since throttle gives you a stale position. <code class=\"inline\">sampleTime</code> is on its own clock. The debounce gotcha is that the timer resets, so someone typing continuously never triggers a search at all.”</p>",
    "revise": "Rate-limiting operators differ on WHICH emission they keep:\n• **debounceTime** — emit after a QUIET gap (last value).\n• **throttleTime** — emit the FIRST, then ignore for the window (leading).\n• **auditTime** — emit the LAST value at the end of the window (trailing).\n• **sampleTime** — emit the latest value on a fixed clock."
  },
  {
    "id": "rx-mergeall",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "mergeAll",
      "concatAll",
      "higher-order",
      "internals"
    ],
    "question": "What are higher-order observables and *All operators?",
    "answer": "<p>A <strong>higher-order observable</strong> emits other observables. Flattening operators subscribe to the inner ones: <code class=\"inline\">mergeAll</code> (concurrent), <code class=\"inline\">concatAll</code> (in order), <code class=\"inline\">switchAll</code> (cancel previous). The <code class=\"inline\">*Map</code> variants (mergeMap, concatMap…) are just <code class=\"inline\">map + *All</code>.</p>",
    "tip": "switchMap === map(...) + switchAll — knowing this shows deep RxJS understanding.",
    "code": "source$.pipe(map(id => this.load(id)), concatAll());\n// same as: source$.pipe(concatMap(id => this.load(id)))",
    "lang": "ts",
    "deep": "<h4>What makes an observable \"higher-order\"</h4><p>Ordinary <code class=\"inline\">map</code> returning an observable gives you a stream <em>of streams</em>:</p><pre><code>ids$.pipe(map(id =&gt; this.http.get(`/user/${id}`)))\n// Type: Observable&lt;Observable&lt;User&gt;&gt;  -- nested!\n// Subscribing gives you Observable objects, NOT users. And no HTTP fires,\n// because nobody subscribed to the inner ones.</code></pre><p>A <strong>flattening</strong> operator subscribes to each inner observable and re-emits its values on the outer stream, collapsing <code class=\"inline\">Observable&lt;Observable&lt;T&gt;&gt;</code> → <code class=\"inline\">Observable&lt;T&gt;</code>.</p><h4>The identity that makes it click</h4><pre><code>source$.pipe(map(fn), mergeAll())   ===   source$.pipe(mergeMap(fn))\nsource$.pipe(map(fn), concatAll())  ===   source$.pipe(concatMap(fn))\nsource$.pipe(map(fn), switchAll())  ===   source$.pipe(switchMap(fn))\nsource$.pipe(map(fn), exhaustAll()) ===   source$.pipe(exhaustMap(fn))</code></pre><p>Every <code class=\"inline\">*Map</code> operator is literally <code class=\"inline\">map</code> + the matching <code class=\"inline\">*All</code>. Saying this out loud is a strong senior signal — it reframes four \"different\" operators as one idea (map) plus a <strong>concurrency strategy</strong>.</p><h4>The four strategies</h4><table><thead><tr><th>Operator</th><th>When a new inner arrives while one is active</th><th>Use for</th></tr></thead><tbody><tr><td><code class=\"inline\">mergeAll</code></td><td>run both concurrently</td><td>independent parallel work</td></tr><tr><td><code class=\"inline\">concatAll</code></td><td>queue it, preserve order</td><td>ordered writes</td></tr><tr><td><code class=\"inline\">switchAll</code></td><td><strong>cancel</strong> the previous</td><td>search / latest-wins</td></tr><tr><td><code class=\"inline\">exhaustAll</code></td><td><strong>ignore</strong> the new one</td><td>double-click submit guard</td></tr></tbody></table><h4>Limiting concurrency</h4><pre><code>// mergeAll/mergeMap take a concurrency limit — an underrated answer\nurls$.pipe(mergeMap(url =&gt; this.http.get(url), 3));  // at most 3 in flight\n// concatMap === mergeMap with concurrency 1</code></pre><p>“How would you fetch 500 URLs without hammering the server?” → <code class=\"inline\">mergeMap</code> with a concurrency cap.</p><h4>Where you meet higher-order observables by accident</h4><pre><code>// Forgetting to flatten — the bug\nthis.search$.pipe(map(q =&gt; this.api.search(q))).subscribe(x =&gt; console.log(x));\n// logs: Observable {...} — and no request ever fires</code></pre><h4>How to say it in an interview</h4><p>“A higher-order observable emits observables — you get that the moment you <code class=\"inline\">map</code> to an HTTP call. The <code class=\"inline\">*All</code> operators subscribe to the inner streams and flatten them, and every <code class=\"inline\">*Map</code> is exactly <code class=\"inline\">map</code> plus the matching <code class=\"inline\">*All</code> — <code class=\"inline\">switchMap</code> is <code class=\"inline\">map</code> + <code class=\"inline\">switchAll</code>. So the real choice isn't four operators, it's one concurrency strategy: run in parallel, queue, cancel, or ignore.”</p>",
    "revise": "A higher-order Observable is an Observable OF Observables.\n• Flatten with `mergeAll` / `switchAll` / `concatAll`.\n• The `*Map` operators (`switchMap`, `mergeMap`...) = map to inner Observables + flatten in ONE step.\nYou almost always use the `*Map` form."
  },
  {
    "id": "rx-takeuntildestroyed",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "takeUntilDestroyed",
      "cleanup",
      "modern",
      "memory-leak"
    ],
    "question": "What is takeUntilDestroyed?",
    "answer": "<p>(Angular 16+) An operator that auto-completes a subscription when the component/directive is destroyed — replacing the manual <code class=\"inline\">destroy$</code> Subject + <code class=\"inline\">takeUntil</code> boilerplate. Call it in an injection context, or pass a <code class=\"inline\">DestroyRef</code>.</p>",
    "tip": "It's the modern, boilerplate-free way to prevent subscription leaks.",
    "code": "data$.pipe(takeUntilDestroyed()).subscribe(...);",
    "lang": "ts",
    "deep": "<h4>What it replaces</h4><pre><code>// Before — 5 lines of boilerplate in every component\nprivate destroy$ = new Subject&lt;void&gt;();\nngOnInit() {\n  this.data$.pipe(takeUntil(this.destroy$)).subscribe();\n}\nngOnDestroy() {\n  this.destroy$.next();\n  this.destroy$.complete();\n}\n\n// After (Angular 16+)\ndata$ = inject(DataService).data$.pipe(takeUntilDestroyed());</code></pre><p>It hooks into <code class=\"inline\">DestroyRef</code> and completes the stream when the component, directive, pipe, or service is destroyed.</p><h4>The rule that trips everyone: injection context</h4><pre><code>// ✔ field initializer — injection context\nexport class MyComp {\n  data$ = this.svc.data$.pipe(takeUntilDestroyed());\n}\n\n// ✔ constructor — injection context\nconstructor() {\n  this.svc.data$.pipe(takeUntilDestroyed()).subscribe();\n}\n\n// ✘ ngOnInit — NOT an injection context -&gt; runtime error\nngOnInit() {\n  this.svc.data$.pipe(takeUntilDestroyed()).subscribe();  // throws NG0203\n}\n\n// ✔ ngOnInit — pass the DestroyRef explicitly\nprivate destroyRef = inject(DestroyRef);\nngOnInit() {\n  this.svc.data$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe();\n}</code></pre><p>Calling it with no argument outside an injection context throws <strong>NG0203</strong>. Knowing the <code class=\"inline\">DestroyRef</code> escape hatch is the practical half of the answer — most real subscriptions do start in <code class=\"inline\">ngOnInit</code>.</p><h4>Still last in the pipe</h4><pre><code>// ✘ inner interval escapes the teardown\nsource$.pipe(takeUntilDestroyed(), switchMap(() =&gt; interval(1000)))\n// ✔\nsource$.pipe(switchMap(() =&gt; interval(1000)), takeUntilDestroyed())</code></pre><p><code class=\"inline\">takeUntilDestroyed</code> inherits <code class=\"inline\">takeUntil</code>'s positioning rule — it must come last, or operators after it can resubscribe to something that outlives the component.</p><h4>DestroyRef directly</h4><pre><code>// For non-RxJS cleanup\nprivate destroyRef = inject(DestroyRef);\nconstructor() {\n  const id = setInterval(tick, 1000);\n  this.destroyRef.onDestroy(() =&gt; clearInterval(id));\n}</code></pre><p>Useful in services and functional guards/interceptors, which have no <code class=\"inline\">ngOnDestroy</code>.</p><h4>Where it sits in the hierarchy</h4><p><code class=\"inline\">async</code> pipe → <code class=\"inline\">toSignal</code> → <code class=\"inline\">takeUntilDestroyed</code> → <code class=\"inline\">takeUntil(destroy$)</code>. The first two mean no subscription to manage at all; reach for <code class=\"inline\">takeUntilDestroyed</code> when you genuinely must subscribe imperatively.</p><h4>How to say it in an interview</h4><p>“Angular 16's replacement for the <code class=\"inline\">destroy$</code> Subject boilerplate — it uses <code class=\"inline\">DestroyRef</code> to complete the stream when the component is destroyed. The catch is it must run in an injection context, so a bare call in <code class=\"inline\">ngOnInit</code> throws NG0203 — there I inject <code class=\"inline\">DestroyRef</code> and pass it in. And like <code class=\"inline\">takeUntil</code>, it has to be the last operator.”</p>",
    "revise": "`takeUntilDestroyed()` is an Angular operator that auto-COMPLETES a stream when the component/directive is destroyed.\n• No manual destroy$ Subject or `ngOnDestroy` boilerplate.\n• Call it in an injection context, or pass a `DestroyRef`."
  },
  {
    "id": "rx-custom-operator",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "custom-operator",
      "pipe"
    ],
    "question": "How do you write a custom operator?",
    "answer": "<p>A pipeable operator is just a function that takes a source observable and returns a new one — usually by composing existing operators with <code class=\"inline\">pipe</code>. This makes reusable, named stream transformations.</p>",
    "tip": "A custom operator is often just a named composition of built-in operators.",
    "code": "const cleanSearch = () =>\n  (src$) => src$.pipe(debounceTime(300), distinctUntilChanged());\n\ninput$.pipe(cleanSearch());",
    "lang": "ts",
    "revise": "A custom operator is just a function `(source) => source.pipe(...)`.\n• It takes an Observable and returns an Observable.\n• Compose existing operators inside, then drop it into any `pipe()`.\nKeeps repeated pipelines DRY and testable."
  },
  {
    "id": "rx-scheduler",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "scheduler",
      "timing",
      "testing",
      "internals"
    ],
    "question": "What are RxJS schedulers?",
    "answer": "<p>A scheduler controls <em>when</em> and <em>on what context</em> emissions happen — e.g. <code class=\"inline\">asyncScheduler</code> (macrotask), <code class=\"inline\">asapScheduler</code> (microtask), <code class=\"inline\">animationFrameScheduler</code> (rAF). Used for fine timing control and for virtual time in tests (<code class=\"inline\">TestScheduler</code>).</p>",
    "tip": "Most code never touches schedulers directly — but marble tests rely on the TestScheduler.",
    "code": "observeOn(animationFrameScheduler)",
    "lang": "ts",
    "deep": "<h4>What a scheduler is</h4><p>A scheduler answers <strong>\"when does this work run?\"</strong>. It's a combination of a clock and an execution context — every RxJS operator that involves timing delegates to one. Most code never names one explicitly, but they're always there.</p><h4>The four</h4><table><thead><tr><th>Scheduler</th><th>Runs work</th><th>Analogous to</th></tr></thead><tbody><tr><td><em>none</em> (default)</td><td>synchronously, right now</td><td>a direct call</td></tr><tr><td><code class=\"inline\">queueScheduler</code></td><td>synchronously, but queued</td><td>a trampoline (avoids stack overflow in recursion)</td></tr><tr><td><code class=\"inline\">asapScheduler</code></td><td>microtask</td><td><code class=\"inline\">Promise.resolve().then()</code></td></tr><tr><td><code class=\"inline\">asyncScheduler</code></td><td>macrotask</td><td><code class=\"inline\">setInterval</code></td></tr><tr><td><code class=\"inline\">animationFrameScheduler</code></td><td>before the next repaint</td><td><code class=\"inline\">requestAnimationFrame</code></td></tr></tbody></table><p><code class=\"inline\">delay</code>, <code class=\"inline\">interval</code>, <code class=\"inline\">timer</code>, <code class=\"inline\">debounceTime</code> all default to <code class=\"inline\">asyncScheduler</code>.</p><h4>subscribeOn vs observeOn — the classic pairing</h4><pre><code>source$.pipe(\n  subscribeOn(asyncScheduler),   // WHERE/WHEN the subscription (producer) starts\n  observeOn(animationFrameScheduler)  // WHERE/WHEN each emission is DELIVERED\n)</code></pre><ul><li><code class=\"inline\">subscribeOn</code> affects the <em>whole chain's start</em> — it only matters once, and position in the pipe is irrelevant.</li><li><code class=\"inline\">observeOn</code> affects everything <em>downstream of it</em> — so position matters.</li></ul><h4>Where it's genuinely useful</h4><pre><code>// Render-heavy stream — don't do layout work more than once per frame\nmouseMove$.pipe(observeOn(animationFrameScheduler)).subscribe(pos =&gt; this.draw(pos));\n\n// Escape ExpressionChangedAfterItHasBeenCheckedError\nvalue$.pipe(observeOn(asapScheduler)).subscribe(v =&gt; this.value = v);</code></pre><p>That second one is the practical Angular payoff: deferring to a microtask pushes the update into the next change-detection turn, which is a cleaner fix than <code class=\"inline\">setTimeout(0)</code>.</p><h4>The real reason to know them: marble testing</h4><pre><code>it('debounces', () =&gt; {\n  const scheduler = new TestScheduler((actual, expected) =&gt;\n    expect(actual).toEqual(expected));\n\n  scheduler.run(({ cold, expectObservable }) =&gt; {\n    const source$ = cold('a-b-c------|');\n    const result$ = source$.pipe(debounceTime(30));\n    expectObservable(result$).toBe('--------c--|');\n  });\n});</code></pre><p><code class=\"inline\">TestScheduler</code> uses <strong>virtual time</strong>: a <code class=\"inline\">debounceTime(300)</code> test runs in microseconds instead of taking 300ms, and there's no flakiness. This is the answer to \"how do you test time-based RxJS without <code class=\"inline\">fakeAsync</code>?\"</p><h4>Honest framing</h4><p>If asked \"do you use schedulers?\", the credible answer is: rarely by name in app code — but constantly via <code class=\"inline\">TestScheduler</code>, and occasionally <code class=\"inline\">animationFrameScheduler</code> for render-bound streams. Claiming daily use would be a red flag.</p><h4>How to say it in an interview</h4><p>“A scheduler decides when and in what context work runs — sync, microtask, macrotask, or animation frame. <code class=\"inline\">subscribeOn</code> controls when the producer starts, <code class=\"inline\">observeOn</code> where emissions are delivered downstream. I don't reach for them often in app code, apart from <code class=\"inline\">animationFrameScheduler</code> for render-heavy streams — but <code class=\"inline\">TestScheduler</code> matters a lot: marble tests run time-based operators in virtual time, so a 300ms debounce test is instant and never flaky.”</p>",
    "revise": "Schedulers control WHEN and on which context work runs:\n• `queueScheduler` (sync), `asapScheduler` (microtask), `asyncScheduler` (`setTimeout`), `animationFrameScheduler`.\n• Most apps never set one explicitly.\nUseful for testing (virtual time) and fine-grained timing control."
  },
  {
    "id": "rx-firstvaluefrom",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "promise",
      "interop",
      "firstValueFrom"
    ],
    "question": "How do you convert an Observable to a Promise?",
    "answer": "<p>Use <code class=\"inline\">firstValueFrom(obs$)</code> (resolves with the first emission) or <code class=\"inline\">lastValueFrom(obs$)</code> (waits for completion). These replaced the deprecated <code class=\"inline\">.toPromise()</code>. Handy when awaiting a one-shot stream in async/await code.</p>",
    "tip": ".toPromise() is deprecated — use firstValueFrom / lastValueFrom.",
    "code": "const user = await firstValueFrom(this.api.getUser(id));",
    "lang": "ts",
    "revise": "Convert an Observable to a Promise:\n• **firstValueFrom(obs)** — resolves with the FIRST value, then unsubscribes.\n• **lastValueFrom(obs)** — resolves with the LAST value on complete.\nPrefer these over the deprecated `toPromise()`."
  },
  {
    "id": "rx-cold-multiple-subscribers",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "cold",
      "http",
      "multicast",
      "real-world"
    ],
    "question": "Why does my HTTP call fire twice with two async pipes?",
    "answer": "<p>Observables are <strong>cold</strong> — each subscription re-runs the producer, so two <code class=\"inline\">async</code> pipes on the same <code class=\"inline\">http.get()</code> trigger two requests. Fix by multicasting with <code class=\"inline\">shareReplay(1)</code>, or subscribe once and reuse the value.</p>",
    "tip": "Two async pipes on one HTTP observable = two network calls unless you shareReplay it.",
    "code": "readonly user$ = this.http.get('/me').pipe(shareReplay(1));",
    "lang": "ts",
    "deep": "<h4>What's happening</h4><pre><code>// component\nuser$ = this.http.get&lt;User&gt;('/me');</code></pre><pre><code>&lt;!-- template --&gt;\n&lt;span&gt;{{ (user$ | async)?.name }}&lt;/span&gt;\n&lt;img  [src]=\"(user$ | async)?.avatar\"&gt;\n&lt;!-- 2 async pipes = 2 subscriptions = 2 GET /me requests --&gt;</code></pre><p><code class=\"inline\">HttpClient</code> observables are <strong>cold</strong>: the request isn't made when you create the observable, it's made <em>on subscribe</em>. Each <code class=\"inline\">async</code> pipe subscribes independently, so each fires its own request. Nothing is shared or cached by default.</p><h4>Three fixes, in order of preference</h4><pre><code>// 1. Subscribe once in the template with *ngIf ... as\n&lt;ng-container *ngIf=\"user$ | async as user\"&gt;\n  &lt;span&gt;{{ user.name }}&lt;/span&gt;\n  &lt;img [src]=\"user.avatar\"&gt;\n&lt;/ng-container&gt;\n// or Angular 17+:  @if (user$ | async; as user) { ... }\n\n// 2. Multicast the source\nuser$ = this.http.get&lt;User&gt;('/me').pipe(shareReplay(1));\n\n// 3. Convert to a signal (Angular 16+) — one subscription\nuser = toSignal(this.http.get&lt;User&gt;('/me'));</code></pre><p>Fix 1 is best when the data is used in one template region: one subscription, no extra operator, and it doubles as the loading guard. Fix 2 is right when several <em>components</em> need it — put it in a service.</p><h4>The related trap: it's not just async pipes</h4><pre><code>const req$ = this.http.post('/orders', body);\nreq$.subscribe();   // order created\nreq$.subscribe();   // ANOTHER order created!</code></pre><p>Because cold means \"re-run the producer per subscribe\", subscribing twice to a POST creates two records. This is why you never hand a raw request observable to code that might subscribe more than once.</p><h4>How to spot it</h4><p>The Network tab shows duplicate identical requests. If you see <code class=\"inline\">GET /me</code> twice on one page load, count your <code class=\"inline\">async</code> pipes.</p><h4>How to say it in an interview</h4><p>“HTTP observables are cold — the request fires on subscribe, so two async pipes mean two subscriptions and two calls. I'd either subscribe once in the template with <code class=\"inline\">*ngIf=\"user$ | async as user\"</code>, or multicast with <code class=\"inline\">shareReplay(1)</code> if multiple components need it. The scarier version is a POST — subscribing twice submits twice.”</p>",
    "revise": "Two async pipes on a COLD HTTP Observable = TWO HTTP calls (cold re-executes per subscriber).\n• **Fix**: `shareReplay`(1) to multicast one execution.\n• Or subscribe once into a view-model / signal and bind to that."
  },
  {
    "id": "rx-error-terminates",
    "category": "rxjs",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "error",
      "completion",
      "lifecycle"
    ],
    "question": "What happens to a stream after it errors?",
    "answer": "<p>An error is a <strong>terminal</strong> event — the observable stops and emits nothing further, and the subscription is torn down. To keep a long-lived stream alive (like a search box), handle the error <em>inside</em> the inner observable with <code class=\"inline\">catchError</code> so the outer stream never sees it.</p>",
    "tip": "Error and complete both end a stream permanently — design for that.",
    "code": "// A Subject that errors is dead — permanently.\nconst s = new Subject<number>();\ns.subscribe({ error: e => console.log('sub1', e) });\ns.error('boom');\ns.subscribe({ error: e => console.log('sub2', e) }); // fires IMMEDIATELY\ns.next(1);                                           // ignored — nothing happens",
    "lang": "ts",
    "deep": "<h4>The contract</h4><p>An observable's lifetime is: <strong>zero or more <code class=\"inline\">next</code></strong>, then <strong>exactly one</strong> of <code class=\"inline\">complete</code> or <code class=\"inline\">error</code> — never both, never anything after. Both are <em>terminal</em>: the subscription is torn down, teardown logic runs, and the stream can never emit again.</p><pre><code>source$: --1--2--X\n//                ^ error -- nothing can follow. Not a pause: an end.</code></pre><h4>Why this causes real bugs</h4><pre><code>// The search box that mysteriously stops working\nthis.searchTerm$.pipe(\n  switchMap(t =&gt; this.api.search(t)),\n  catchError(() =&gt; of([]))      // outer!\n).subscribe(r =&gt; this.results = r);</code></pre><p>The first failed request errors the <strong>outer</strong> stream. <code class=\"inline\">catchError</code> handles it, emits <code class=\"inline\">[]</code>, and the outer stream <strong>completes</strong>. The user keeps typing and nothing ever happens again — no error in the console, no clue. Fix: <code class=\"inline\">catchError</code> <em>inside</em> the <code class=\"inline\">switchMap</code>, so the outer stream never sees an error.</p><h4>A dead Subject stays dead</h4><pre><code>const sub = new Subject&lt;number&gt;();\nsub.error('boom');\nsub.next(1);           // silently ignored\nsub.subscribe({ error: e =&gt; console.log(e) });  // immediately re-errors 'boom'</code></pre><p>A Subject remembers it errored: <strong>every future subscriber is immediately handed the same error</strong>. This is why an errored NgRx-style state Subject bricks a feature until reload — and why you never let an error reach a long-lived Subject.</p><h4>Complete is terminal too</h4><pre><code>this.destroy$.complete();\nthis.destroy$.next();   // no-op — completed streams ignore next</code></pre><p>Same trap in reverse: completing a Subject you still intend to push through means later <code class=\"inline\">next()</code> calls vanish silently.</p><h4>Keeping a stream alive: catch on the inner</h4><table><thead><tr><th>catchError position</th><th>Result</th></tr></thead><tbody><tr><td>Outer pipe</td><td>error handled, <strong>outer stream ends</strong></td></tr><tr><td>Inside <code class=\"inline\">switchMap</code></td><td>inner dies, <strong>outer keeps going</strong> ✔</td></tr></tbody></table><p>Same rule in NgRx effects: an uncaught error kills the effect permanently, so the inner <code class=\"inline\">catchError</code> returning a failure action is mandatory.</p><h4>retry re-subscribes rather than resurrects</h4><p><code class=\"inline\">retry</code> doesn't revive the dead stream — it <em>re-subscribes to the source</em>, creating a fresh execution. That only works because the source is cold.</p><h4>How to say it in an interview</h4><p>“Error and complete are both terminal — one or the other, and then the stream is finished for good. The practical fallout is the search box that dies after one failed request: the <code class=\"inline\">catchError</code> was on the outer stream, so it handled the error and completed everything. Catch inside the <code class=\"inline\">switchMap</code> and the outer stream never sees it. And an errored Subject is permanently poisoned — it hands the same error to every new subscriber.”</p>",
    "revise": "**Once a stream ERRORS, it's DONE** — no further next or complete.\n• The consumer stops receiving values.\n• Recover with `catchError` (upstream, or inside the inner stream) to keep the outer stream alive.\nAn error is a terminal event, just like complete."
  },
  {
    "id": "rx-tosignal",
    "category": "rxjs",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "signals",
      "interop",
      "toSignal",
      "modern"
    ],
    "question": "How do Observables and Signals interoperate?",
    "answer": "<p>(Angular 16+) <code class=\"inline\">toSignal(obs$)</code> subscribes and exposes the latest value as a signal (auto-unsubscribes); <code class=\"inline\">toObservable(sig)</code> turns a signal into a stream. Use RxJS for async/events, signals for synchronous local state, and bridge between them.</p>",
    "tip": "toSignal replaces the async pipe in signal-based components and auto-cleans up.",
    "code": "count = toSignal(this.store.count$, { initialValue: 0 });",
    "lang": "ts",
    "deep": "<h4>The two bridges</h4><pre><code>// Observable -&gt; Signal  (subscribes, auto-unsubscribes on destroy)\nuser = toSignal(this.http.get&lt;User&gt;('/me'));            // Signal&lt;User | undefined&gt;\ncount = toSignal(this.store.count$, { initialValue: 0 }); // Signal&lt;number&gt;\n\n// Signal -&gt; Observable  (emits on every signal change)\nquery = signal('');\nresults$ = toObservable(this.query).pipe(\n  debounceTime(300),\n  switchMap(q =&gt; this.api.search(q))\n);</code></pre><h4>toSignal's initial-value problem</h4><p>A signal must <em>always</em> have a value, but an observable may not have emitted yet. Three ways to resolve it:</p><table><thead><tr><th>Call</th><th>Type</th><th>Behaviour</th></tr></thead><tbody><tr><td><code class=\"inline\">toSignal(obs$)</code></td><td><code class=\"inline\">T | undefined</code></td><td><code class=\"inline\">undefined</code> until first emission</td></tr><tr><td><code class=\"inline\">toSignal(obs$, { initialValue: 0 })</code></td><td><code class=\"inline\">T</code></td><td>your default until first emission</td></tr><tr><td><code class=\"inline\">toSignal(obs$, { requireSync: true })</code></td><td><code class=\"inline\">T</code></td><td>throws unless it emits <strong>synchronously</strong></td></tr></tbody></table><p><code class=\"inline\">requireSync: true</code> is for sources that are guaranteed synchronous — a <code class=\"inline\">BehaviorSubject</code> or an NgRx selector. Use it on an <code class=\"inline\">http.get</code> and it throws immediately.</p><h4>toSignal vs the async pipe</h4><pre><code>&lt;!-- async pipe: subscribes PER USAGE -- 2 pipes = 2 HTTP calls --&gt;\n&lt;span&gt;{{ (user$ | async)?.name }}&lt;/span&gt;\n&lt;img [src]=\"(user$ | async)?.avatar\"&gt;\n\n&lt;!-- toSignal: ONE subscription, read as many times as you like --&gt;\n&lt;span&gt;{{ user()?.name }}&lt;/span&gt;\n&lt;img [src]=\"user()?.avatar\"&gt;</code></pre><p>That's a real advantage: <code class=\"inline\">toSignal</code> subscribes once in the class, so the multiple-async-pipe duplicate-request problem disappears. It's also readable in TypeScript (<code class=\"inline\">this.user()</code>), which the async pipe never was.</p><h4>toObservable is effect-based — the subtle bit</h4><pre><code>const s = signal(0);\nconst obs$ = toObservable(s);\ns.set(1); s.set(2); s.set(3);   // obs$ emits only 3, not 1,2,3</code></pre><p><code class=\"inline\">toObservable</code> is built on an <code class=\"inline\">effect</code>, so it emits on the <strong>next microtask</strong>, not synchronously per <code class=\"inline\">set</code>. Intermediate values are <strong>coalesced away</strong>. If you need every value, you needed a Subject, not a signal.</p><h4>When to use which</h4><ul><li><strong>Signals</strong> — synchronous local/derived UI state. No subscription, glitch-free <code class=\"inline\">computed</code>, and they enable zoneless change detection.</li><li><strong>RxJS</strong> — anything <em>async or event-based</em>: HTTP, WebSockets, debouncing, cancellation, retry, combining streams. Signals have no concept of time.</li></ul><p>The mature line: signals didn't replace RxJS. Signals are for <em>state</em>, RxJS is for <em>events over time</em> — you convert at the boundary.</p><h4>How to say it in an interview</h4><p>“<code class=\"inline\">toSignal</code> subscribes and exposes the latest value as a signal, auto-unsubscribing on destroy — and unlike the async pipe it subscribes once, so reading it twice in a template doesn't fire two HTTP calls. You have to handle the initial value, either with <code class=\"inline\">initialValue</code> or <code class=\"inline\">requireSync</code> for a synchronous source like a selector. <code class=\"inline\">toObservable</code> goes the other way, but it's effect-based, so it coalesces intermediate values. I use signals for synchronous state and RxJS for async and event streams, converting at the boundary.”</p>",
    "revise": "Bridge RxJS and Signals in modern Angular:\n• **toSignal(obs)** — Observable → Signal (auto-subscribes, unsubscribes on destroy).\n• **toObservable(sig)** — Signal → Observable.\nUse Signals for state, RxJS for async pipelines; convert at the boundary."
  },
  {
    "id": "rx-tap-side-effects",
    "category": "rxjs",
    "difficulty": "beginner",
    "tags": [
      "tap",
      "side-effects",
      "debugging"
    ],
    "question": "When should you use tap?",
    "answer": "<p><code class=\"inline\">tap</code> runs a side effect (logging, setting a flag, triggering an action) without altering the emitted value or the stream. Great for debugging and for effects that shouldn't change data. Never mutate values inside it.</p>",
    "tip": "tap is for side effects only — if you're transforming the value, use map.",
    "code": "req$.pipe(tap(() => this.loading = true), /* ... */);",
    "lang": "ts",
    "revise": "tap runs a SIDE EFFECT (logging, set a loading flag) WITHOUT changing the emitted values.\n• Use it for debugging and incidental effects.\n• **Keep real transformations in map/switchMap** — don't hide business logic in tap."
  }
]
