[
  {
    "id": "gen-rest",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "V.Imp",
      "rest",
      "http",
      "api",
      "idempotency"
    ],
    "question": "REST basics — methods, status codes, idempotency.",
    "answer": "<p><strong>GET</strong> read, <strong>POST</strong> create, <strong>PUT</strong> replace, <strong>PATCH</strong> partial update, <strong>DELETE</strong> remove. GET/PUT/DELETE are idempotent; POST is not.</p><p>Status codes: <strong>200</strong> OK, <strong>201</strong> Created, <strong>204</strong> No Content, <strong>400</strong> bad request, <strong>401</strong> unauthenticated, <strong>403</strong> forbidden, <strong>404</strong> not found, <strong>500</strong> server error.</p>",
    "tip": "401 = 'who are you?'; 403 = 'I know you, but no'.",
    "code": "",
    "lang": "",
    "deep": "<h4>The methods, and the two properties that matter</h4><table><thead><tr><th>Method</th><th>Safe?</th><th>Idempotent?</th><th>Use</th></tr></thead><tbody><tr><td>GET</td><td>✔</td><td>✔</td><td>read</td></tr><tr><td>POST</td><td>✘</td><td><strong>✘</strong></td><td>create</td></tr><tr><td>PUT</td><td>✘</td><td>✔</td><td>replace whole resource</td></tr><tr><td>PATCH</td><td>✘</td><td><strong>✘*</strong></td><td>partial update</td></tr><tr><td>DELETE</td><td>✘</td><td>✔</td><td>remove</td></tr></tbody></table><ul><li><strong>Safe</strong> — doesn't modify anything. GET must never have side effects (a <code class=\"inline\">GET /deleteUser?id=1</code> is a real bug — crawlers and prefetchers will fire it).</li><li><strong>Idempotent</strong> — <em>same request N times = same end state as once</em>. Not \"same response\".</li></ul><h4>Idempotency is the question they're really asking</h4><pre><code>PUT /users/1  { name: 'Ann' }   x3  -&gt; name is 'Ann'.  Same state. IDEMPOTENT.\nPOST /users   { name: 'Ann' }   x3  -&gt; THREE users.    NOT idempotent.\nDELETE /users/1                 x3  -&gt; gone (then 404). Same STATE. IDEMPOTENT.</code></pre><p>DELETE catches people: the second call returns 404, a <em>different response</em> — but the resulting state is identical, and that's the definition.</p><p><strong>Why it matters practically:</strong> idempotent requests are safe to <strong>retry</strong>. That's exactly why you can blanket-retry a GET or PUT on a network blip, but retrying a POST may create duplicate orders. It's the same reasoning behind choosing <code class=\"inline\">concatMap</code> over <code class=\"inline\">switchMap</code> for writes. If a POST must be retry-safe, use an <strong>Idempotency-Key</strong> header so the server dedupes.</p><p><em>*PATCH is idempotent if it sets absolute values (<code class=\"inline\">{ status: 'done' }</code>) but not if it's relative (<code class=\"inline\">{ increment: 1 }</code>).</em></p><h4>Status codes — the ones you must know cold</h4><table><thead><tr><th>Code</th><th>Meaning</th><th>Note</th></tr></thead><tbody><tr><td>200 / 201 / 204</td><td>OK / Created / No Content</td><td>201 returns a <code class=\"inline\">Location</code> header</td></tr><tr><td><strong>400</strong></td><td>Bad Request</td><td>your payload is malformed</td></tr><tr><td><strong>401</strong></td><td>Unauthorized</td><td>= <em>unauthenticated</em>. Who are you?</td></tr><tr><td><strong>403</strong></td><td>Forbidden</td><td>we know you; you're not allowed</td></tr><tr><td>404 / 409 / 422</td><td>Not Found / Conflict / Unprocessable</td><td>409 = version conflict</td></tr><tr><td>429</td><td>Too Many Requests</td><td>rate limited — back off</td></tr><tr><td>500 / 503</td><td>Server Error / Unavailable</td><td><strong>retry these</strong>, not 4xx</td></tr></tbody></table><p><strong>401 vs 403</strong> is the classic trap: 401 means \"authenticate\" (refresh the token, redirect to login); 403 means \"you're authenticated and still can't\" (don't retry, don't redirect — show a message). Wiring an interceptor to log out on 403 is a real bug.</p><h4>What makes an API RESTful</h4><ul><li><strong>Resources, not verbs</strong> — <code class=\"inline\">POST /users</code>, not <code class=\"inline\">POST /createUser</code>. Nouns in the URL; the verb is the method.</li><li><strong>Stateless</strong> — every request carries its own auth; the server keeps no session. That's what lets you scale horizontally behind a load balancer.</li><li><strong>Plural collections</strong> — <code class=\"inline\">/users/1/orders</code>.</li><li>Filtering/paging in the query string, not the path: <code class=\"inline\">/users?page=2&amp;sort=name</code>.</li></ul><h4>How to say it in an interview</h4><p>“Resources as nouns, HTTP methods as the verbs, and stateless requests. The concept worth being precise about is idempotency — same request N times leaves the same state, which is why GET, PUT and DELETE are safe to retry and POST isn't, and why a retried POST can create duplicate orders unless you send an Idempotency-Key. On status codes the one people get wrong is 401 versus 403: 401 is unauthenticated so you refresh or redirect, 403 is authenticated but not permitted, so retrying or logging out is wrong.”</p>",
    "revise": "**REST** = resources + HTTP verbs.\n• **GET** read, **POST** create, **PUT** replace, **PATCH** partial, **DELETE** remove.\n• **Idempotent**: GET/PUT/DELETE; **POST is not**.\n• Codes: 200/201/204, 400/401/403/404, 500.\n• **Interview tip**: **401** = 'who are you?' (not authenticated); **403** = 'I know you, but no' (not authorized)."
  },
  {
    "id": "gen-jwt",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "auth",
      "jwt",
      "security"
    ],
    "question": "Explain the JWT authentication flow.",
    "answer": "<p>User logs in → server returns a signed <strong>JWT</strong> (header.payload.signature). The client stores it and sends it as <code class=\"inline\">Authorization: Bearer &lt;token&gt;</code>. The server verifies the signature and expiry on each request — no server-side session needed (stateless).</p>",
    "tip": "The payload is only base64-encoded, not encrypted — never put secrets in it.",
    "code": "",
    "lang": "",
    "deep": "<h4>The structure</h4><pre><code>header.payload.signature      // three base64url segments joined by dots\n\neyJhbGciOiJIUzI1NiJ9  .  eyJzdWIiOiIxIiwicm9sZSI6ImFkbWluIn0  .  4pcPyMD0...\n{alg, typ}                 {sub, role, exp, iat}                 HMAC/RSA</code></pre><p><strong>The payload is base64, NOT encrypted.</strong> Anyone can paste the token into jwt.io and read every claim. Base64 is encoding, not encryption — so <strong>never put anything secret in a JWT</strong>: no PII, no passwords, no internal ids you don't want leaked.</p><p>What the signature gives you is <strong>integrity</strong>, not confidentiality: change one character of the payload and the signature no longer matches, so the server rejects it. You can read it; you can't forge it without the key.</p><h4>The flow</h4><pre><code>1. POST /login {user, pass}  -&gt;  server verifies, signs a JWT, returns it\n2. Client stores it\n3. Every request:  Authorization: Bearer &lt;token&gt;\n4. Server verifies the SIGNATURE + exp — no DB lookup needed  ← the whole point\n5. On 401 -&gt; use the refresh token to get a new access token</code></pre><h4>Why JWTs exist: statelessness</h4><p>A session cookie requires the server to look up a session store on every request. A JWT is <strong>self-contained</strong> — the server verifies the signature with a key it already holds and trusts the claims. No shared session store, so any instance behind a load balancer can serve any request. That's the entire selling point.</p><h4>And the cost of statelessness: you can't revoke it</h4><p>A signed JWT is valid until <code class=\"inline\">exp</code>, full stop. Ban a user, and their token keeps working. Log out, and the token still works if someone copied it. There's no server-side \"delete the session\" — because there is no session.</p><p>Mitigations (this is the senior part of the answer):</p><ul><li><strong>Short-lived access tokens</strong> (5–15 min) + a long-lived <strong>refresh token</strong>. Damage window is minutes.</li><li>A <strong>denylist</strong> of revoked token ids — but now you're hitting a store on every request, which gives back the statelessness you bought.</li><li>A <code class=\"inline\">tokenVersion</code> claim bumped on logout/password-change.</li></ul><h4>Storage — the question behind the question</h4><table><thead><tr><th></th><th>localStorage</th><th>httpOnly cookie</th></tr></thead><tbody><tr><td>Readable by JS</td><td><strong>yes → XSS steals it</strong></td><td>no</td></tr><tr><td>Sent automatically</td><td>no (you add the header)</td><td>yes → <strong>CSRF risk</strong></td></tr><tr><td>Mitigation</td><td>—</td><td><code class=\"inline\">SameSite=Strict</code> + CSRF token</td></tr></tbody></table><p>The common answer \"store it in localStorage\" is the <strong>wrong</strong> one: any XSS — including from a compromised npm dependency — reads it instantly. The better answer is an <strong>httpOnly, Secure, SameSite cookie</strong> for the refresh token, keeping the access token in memory only.</p><h4>The alg: none attack</h4><p>Historically, libraries would accept a token whose header said <code class=\"inline\">{\"alg\":\"none\"}</code> and skip verification entirely — trivial forgery. Always pin the expected algorithm server-side. It's a great example of why you never hand-roll JWT verification.</p><h4>In Angular</h4><pre><code>// interceptor attaches the token; refreshes once on 401\nintercept(req, next) {\n  const authReq = req.clone({ setHeaders: { Authorization: `Bearer ${this.token}` }});\n  return next.handle(authReq).pipe(\n    catchError(e =&gt; e.status === 401 ? this.refreshAndRetry(req, next) : throwError(() =&gt; e))\n  );\n}</code></pre><h4>How to say it in an interview</h4><p>“Header, payload, signature — base64, so the payload is readable by anyone; the signature only guarantees integrity. That means no secrets in the claims. The value is statelessness: the server verifies a signature instead of hitting a session store, so any instance can serve any request. The trade-off is you can't revoke one — it's valid until it expires — which is why you use short-lived access tokens with a refresh token. And I wouldn't put it in localStorage; any XSS reads it. An httpOnly SameSite cookie for the refresh token, access token in memory.”</p>",
    "revise": "**JWT auth** = stateless token flow.\n• Login → server returns a **signed** `header.payload.signature`.\n• Client stores it, sends `Authorization: Bearer <token>`.\n• Server verifies signature + expiry each request — **no server session**.\n• **Interview tip**: the payload is only **base64-encoded, not encrypted** — never put secrets in it."
  },
  {
    "id": "gen-cors",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "cors",
      "security",
      "browser",
      "real-world"
    ],
    "question": "What is CORS and how do you fix it?",
    "answer": "<p><strong>CORS</strong> (Cross-Origin Resource Sharing) is a browser rule that blocks requests to a different origin unless the server opts in with <code class=\"inline\">Access-Control-Allow-Origin</code> headers. Fix it on the <strong>server</strong>; in dev you can use a proxy.</p>",
    "tip": "CORS is enforced by the browser, so the fix always lives on the server.",
    "code": "",
    "lang": "",
    "deep": "<h4>What it actually is</h4><p>CORS is a <strong>browser</strong> mechanism that <em>relaxes</em> the Same-Origin Policy. Two framings people get backwards:</p><ul><li><strong>It's not a server-side restriction</strong> — the request usually <em>reaches</em> your server and the server responds normally. The <strong>browser</strong> then inspects the response headers and, if they don't permit your origin, refuses to hand the response to JavaScript. The Network tab shows a 200; your code gets an error. That's why \"it works in Postman\" — Postman isn't a browser and doesn't enforce it.</li><li><strong>It doesn't protect your server.</strong> curl, Postman, and any backend ignore it entirely. It protects <em>users</em> — stopping evil.com's JS from reading your bank's authenticated responses.</li></ul><h4>What counts as an origin</h4><pre><code>scheme :// host : port      — all three must match\n\nhttps://app.com        vs  http://app.com          ✘ scheme\nhttps://app.com        vs  https://api.app.com     ✘ host (subdomain!)\nhttps://app.com        vs  https://app.com:8080    ✘ port</code></pre><h4>Simple vs preflighted</h4><pre><code>// SIMPLE — sent straight away\nGET/HEAD/POST + only \"safe\" headers + content-type of\nform-urlencoded | multipart/form-data | text/plain\n\n// PREFLIGHTED — browser sends OPTIONS FIRST, asking permission\nOPTIONS /api/users\n  Origin: https://app.com\n  Access-Control-Request-Method: PUT\n  Access-Control-Request-Headers: authorization, content-type\n\n// server must answer:\n  Access-Control-Allow-Origin: https://app.com\n  Access-Control-Allow-Methods: GET, PUT, POST\n  Access-Control-Allow-Headers: authorization, content-type\n  Access-Control-Max-Age: 86400        ← cache the preflight; skips it next time</code></pre><p>Two things that surprise people: sending <code class=\"inline\">Content-Type: application/json</code> or an <code class=\"inline\">Authorization</code> header <strong>triggers a preflight</strong> — so almost every real API call is preflighted. And a preflight <strong>doubles your round trips</strong> until <code class=\"inline\">Access-Control-Max-Age</code> caches it.</p><h4>The credentials rule</h4><pre><code>// Client\nfetch(url, { credentials: 'include' })        // or Angular: withCredentials: true\n\n// Server MUST then:\nAccess-Control-Allow-Origin: https://app.com  // an EXACT origin — '*' is REJECTED\nAccess-Control-Allow-Credentials: true</code></pre><p><strong><code class=\"inline\">Allow-Origin: *</code> is illegal with credentials.</strong> The browser refuses it outright — otherwise any site could make authenticated requests on your behalf and read the response. You must echo the specific origin. This is the #1 CORS gotcha in production.</p><h4>How you actually fix it</h4><ul><li><strong>Fix the server</strong> — it's a server-header problem. Only the server can grant access.</li><li><strong>Dev proxy</strong> — Angular's <code class=\"inline\">proxy.config.json</code> makes the browser see one origin, so CORS never applies. This is why it works in dev and breaks in prod.</li><li><strong>Same origin in prod</strong> — serve the API under <code class=\"inline\">/api</code> on the app's domain via a reverse proxy. No CORS at all.</li><li><strong>Not</strong> a browser extension or <code class=\"inline\">--disable-web-security</code>. That \"fixes\" your machine and nobody else's.</li></ul><pre><code>// proxy.config.json — dev only\n{ \"/api\": { \"target\": \"http://localhost:3000\", \"secure\": false } }</code></pre><h4>How to say it in an interview</h4><p>“It's a browser relaxation of the same-origin policy. The key insight is the request usually reaches the server and the server replies fine — the browser just refuses to give the response to JS unless the headers allow the origin, which is why it works in Postman. So it protects users, not the server. Anything with a JSON content-type or an auth header gets preflighted with an OPTIONS, so I cache that with <code class=\"inline\">Access-Control-Max-Age</code>. The gotcha that bites in prod is that <code class=\"inline\">Allow-Origin: *</code> is rejected when credentials are included — you have to echo the exact origin. In dev I use the CLI proxy; in prod I'd rather serve the API same-origin behind a reverse proxy.”</p>",
    "revise": "**CORS** = a **browser** rule blocking cross-origin requests unless the server opts in.\n• Server opts in with `Access-Control-Allow-Origin` (+ related headers).\n• Dev workaround: a proxy so requests look same-origin.\n• **Interview tip**: it's enforced by the browser, so the **fix always lives on the server** — not in your frontend code."
  },
  {
    "id": "gen-http-https",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "http",
      "https",
      "network"
    ],
    "question": "HTTP vs HTTPS, and what is TLS?",
    "answer": "<p><strong>HTTPS</strong> is HTTP over <strong>TLS</strong>, which encrypts traffic, verifies the server's identity via certificates, and prevents tampering. It's required for PWAs, service workers, and modern browser features.</p>",
    "tip": "HTTPS gives you confidentiality, integrity, and authentication of the server.",
    "code": "",
    "lang": "",
    "revise": "**HTTPS = HTTP over TLS**.\n• TLS gives **confidentiality** (encryption), **integrity** (no tampering), and **authentication** (server identity via certificates).\n• Required for PWAs, service workers, and modern browser APIs.\n• **Interview tip**: summarise TLS as the three guarantees — confidentiality, integrity, authentication of the server."
  },
  {
    "id": "gen-get-post",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "http",
      "get",
      "post"
    ],
    "question": "GET vs POST — key differences.",
    "answer": "<p><strong>GET</strong> — params in the URL, cacheable, idempotent, no body; for reads. <strong>POST</strong> — data in the body, not cached, not idempotent; for creating/side-effecting operations. Never send sensitive data in a GET query string.</p>",
    "tip": "If refreshing the page shouldn't repeat the action, it shouldn't be a GET.",
    "code": "",
    "lang": "",
    "revise": "**GET vs POST**.\n• **GET** — params in URL, cacheable, idempotent, no body; for reads.\n• **POST** — data in body, not cached, not idempotent; for create/side-effecting ops.\n• Never put sensitive data in a GET query string.\n• **Interview tip**: if refreshing the page shouldn't repeat the action, it shouldn't be a GET."
  },
  {
    "id": "gen-auth-authz",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "V.Imp",
      "auth",
      "security"
    ],
    "question": "Authentication vs authorization.",
    "answer": "<p><strong>Authentication</strong> — proving who you are (login). <strong>Authorization</strong> — what you're allowed to do (roles/permissions). AuthN comes first, then AuthZ.</p>",
    "tip": "AuthN = identity (401); AuthZ = permission (403).",
    "code": "",
    "lang": "",
    "deep": "<h4>The distinction</h4><ul><li><strong>Authentication (AuthN)</strong> — <em>who are you?</em> Proving identity: password, OTP, biometric, SSO.</li><li><strong>Authorization (AuthZ)</strong> — <em>what may you do?</em> Checking permission, <strong>after</strong> identity is established.</li></ul><p>Authentication always comes first: you can't decide what someone's allowed to do before you know who they are. Airport analogy: <strong>passport = authentication</strong> (you are who you claim), <strong>boarding pass = authorization</strong> (you may board <em>this</em> flight, in <em>this</em> seat).</p><h4>Where it shows up in HTTP</h4><table><thead><tr><th></th><th>401 Unauthorized</th><th>403 Forbidden</th></tr></thead><tbody><tr><td>Really means</td><td><strong>unauthenticated</strong> (misnamed!)</td><td>authenticated, not permitted</td></tr><tr><td>Cause</td><td>no/expired token</td><td>wrong role</td></tr><tr><td>Client should</td><td>refresh token → login</td><td><strong>show a message</strong></td></tr><tr><td>Retry?</td><td>after refresh</td><td><strong>never</strong> — it'll fail identically</td></tr></tbody></table><p>The HTTP spec's naming is genuinely wrong: <strong>401 is about authentication</strong> despite being called \"Unauthorized\". Interviewers love this. And it's practical — an interceptor that logs users out on a 403 will boot people for clicking one admin-only button.</p><h4>Authorization models</h4><ul><li><strong>RBAC</strong> (role-based) — permissions attach to roles: admin, editor, viewer. Simple, most common.</li><li><strong>ABAC</strong> (attribute-based) — rules over attributes: \"editors may edit docs <em>in their own department</em> during business hours\". Flexible, harder to reason about.</li><li><strong>ACL</strong> — per-resource lists, like file sharing.</li><li><strong>Ownership</strong> — \"you may edit <em>your own</em> post\". Extremely common and easy to forget.</li></ul><h4>The security point that matters most</h4><pre><code>// Angular guard — UX ONLY. Not security.\ncanActivate() { return this.auth.isAdmin(); }</code></pre><p><strong>Client-side authorization is not authorization.</strong> A route guard just stops the menu item rendering. Anyone can open DevTools, edit the JS, or simply <code class=\"inline\">curl</code> your API directly — the browser is fully under the attacker's control. <strong>The server must re-check every rule on every request.</strong></p><p>The classic vulnerability is <strong>IDOR</strong> — Insecure Direct Object Reference:</p><pre><code>GET /api/orders/1001   -&gt; my order   ✔\nGET /api/orders/1002   -&gt; someone else's order... does the server check ownership?</code></pre><p>Hiding the link in the UI protects nobody. Saying \"guards are UX, the API enforces it\" is the answer that marks you as someone who's thought about security.</p><h4>Angular in practice</h4><pre><code>// authN — is there a valid session?\nexport const authGuard = () =&gt; inject(AuthService).isLoggedIn() || inject(Router).parseUrl('/login');\n\n// authZ — does the role permit this route?\nexport const adminGuard = () =&gt; inject(AuthService).hasRole('admin') || inject(Router).parseUrl('/403');</code></pre><h4>How to say it in an interview</h4><p>“Authentication is who you are, authorization is what you're allowed to do — passport versus boarding pass, and authN always comes first. In HTTP it's 401 versus 403, and the naming is misleading: 401 actually means unauthenticated, so you refresh or redirect, while 403 means authenticated but not permitted, so retrying is pointless. The important part is that client-side guards are UX only — anyone can bypass the browser and call the API directly, so the server has to enforce every rule. Otherwise you get IDOR, where changing an id in the URL returns someone else's data.”</p>",
    "revise": "**AuthN vs AuthZ**.\n• **Authentication** — proving **who you are** (login).\n• **Authorization** — **what you're allowed** to do (roles/permissions).\n• AuthN first, then AuthZ.\n• **Interview tip**: AuthN failure = **401**; AuthZ failure = **403**."
  },
  {
    "id": "gen-caching",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "caching",
      "performance",
      "http"
    ],
    "question": "How does browser/HTTP caching work?",
    "answer": "<p>Servers send caching headers: <code class=\"inline\">Cache-Control</code> (max-age, no-cache), <code class=\"inline\">ETag</code>/<code class=\"inline\">Last-Modified</code> for revalidation. The browser reuses cached responses until they expire, then revalidates. CDNs cache at the edge for speed.</p>",
    "tip": "Fingerprint filenames (app.abc123.js) so you can cache forever and bust on change.",
    "code": "Cache-Control: max-age=31536000, immutable",
    "lang": "text",
    "deep": "<h4>The two questions caching answers</h4><ol><li><strong>Can I skip the request entirely?</strong> → freshness (<code class=\"inline\">Cache-Control: max-age</code>)</li><li><strong>Can I skip the download?</strong> → validation (<code class=\"inline\">ETag</code> → 304)</li></ol><p>The first costs zero network. The second still round-trips but returns an empty body. Understanding that these are <em>different</em> is the core of the answer.</p><h4>Cache-Control — the directives that matter</h4><table><thead><tr><th>Directive</th><th>Means</th></tr></thead><tbody><tr><td><code class=\"inline\">max-age=3600</code></td><td>fresh for 1h — <strong>no request at all</strong></td></tr><tr><td><code class=\"inline\">no-cache</code></td><td><strong>cache it, but revalidate every time</strong> ← misleading name!</td></tr><tr><td><code class=\"inline\">no-store</code></td><td>never write it to disk — the real \"don't cache\"</td></tr><tr><td><code class=\"inline\">private</code></td><td>browser only, not CDNs/proxies</td></tr><tr><td><code class=\"inline\">public</code></td><td>CDNs may cache it</td></tr><tr><td><code class=\"inline\">immutable</code></td><td>never revalidate — for hashed filenames</td></tr><tr><td><code class=\"inline\">s-maxage</code></td><td>max-age, but for shared caches only</td></tr></tbody></table><p><strong><code class=\"inline\">no-cache</code> does not mean \"don't cache\"</strong> — it means \"always revalidate before using\". The one that means don't store is <code class=\"inline\">no-store</code>. This is the most-asked CORS-style trick question in the caching space, and it matters: putting <code class=\"inline\">no-cache</code> on a page with personal data still writes it to disk.</p><h4>Validation: ETag / Last-Modified</h4><pre><code>// First response\n200 OK\nETag: \"abc123\"\n\n// Next request — browser asks \"still abc123?\"\nGET /api/users\nIf-None-Match: \"abc123\"\n\n// Server:\n304 Not Modified          ← empty body! Headers only. Browser reuses its copy.</code></pre><p>A 304 still costs a round trip but saves the payload — worth it for large or slow-changing resources. <code class=\"inline\">ETag</code> is a content hash (exact); <code class=\"inline\">Last-Modified</code> is a timestamp (1-second granularity, so weaker).</p><h4>The pattern that makes SPAs fast</h4><pre><code>index.html          Cache-Control: no-cache        ← always revalidate (tiny)\nmain.a1b2c3.js      Cache-Control: max-age=31536000, immutable\nstyles.d4e5f6.css   Cache-Control: max-age=31536000, immutable</code></pre><p>This is <strong>cache busting via content hashing</strong>, and it's why Angular's build fingerprints filenames. The HTML is always checked (it's ~2KB), and it points at hashed assets that can be cached forever — because if the content changes, the <em>filename</em> changes, so there's nothing to invalidate. You get permanent caching and instant deploys simultaneously.</p><p>Get this backwards — cache <code class=\"inline\">index.html</code> for a year — and users are stuck on the old app referencing deleted bundle filenames. That's a real outage.</p><h4>The layers</h4><p>Browser memory → browser disk → Service Worker → CDN → server. A Service Worker is the one <em>you</em> program (Angular's <code class=\"inline\">@angular/pwa</code>), and it's the only one that works offline.</p><h4>Cache invalidation</h4><p>The old joke — \"there are only two hard things: cache invalidation and naming things\" — is really about this: once you've told a cache something is fresh for a year, <strong>you cannot take it back</strong>. There's no \"please forget that\". Which is exactly why content-hashed filenames win: you never invalidate, you just change the URL.</p><h4>How to say it in an interview</h4><p>“Two mechanisms: freshness with <code class=\"inline\">max-age</code>, which skips the request entirely, and validation with an ETag, where you still round-trip but get a 304 with no body. The naming trap is <code class=\"inline\">no-cache</code> — it means revalidate every time, not don't store; that's <code class=\"inline\">no-store</code>. The pattern I use for an SPA is <code class=\"inline\">no-cache</code> on index.html and immutable year-long caching on content-hashed bundles — the filename changes when the content does, so you never need to invalidate, which is the hard part.”</p>",
    "revise": "**HTTP caching** = servers send caching headers.\n• `Cache-Control` (max-age, no-cache); `ETag`/`Last-Modified` for revalidation.\n• Browser reuses cached responses until expiry, then revalidates; CDNs cache at the edge.\n• **Interview tip**: **fingerprint filenames** (`app.abc123.js`) so you can cache forever and bust the cache on every change."
  },
  {
    "id": "gen-spa-ssr",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "spa",
      "ssr",
      "ssg",
      "rendering",
      "performance"
    ],
    "question": "SPA vs SSR vs SSG.",
    "answer": "<ul><li><strong>SPA</strong> — renders in the browser; fast navigation, weaker initial SEO/first paint.</li><li><strong>SSR</strong> — server renders HTML per request; better SEO and first paint (Angular Universal).</li><li><strong>SSG</strong> — pre-render at build time; fastest, best for static content.</li></ul>",
    "tip": "SSR/SSG mainly help SEO and time-to-first-paint for content sites.",
    "code": "",
    "lang": "",
    "deep": "<h4>Where the HTML is built</h4><table><thead><tr><th></th><th>SPA / CSR</th><th>SSR</th><th>SSG</th></tr></thead><tbody><tr><td>Rendered</td><td>in the browser</td><td>on the server, per request</td><td>at <strong>build time</strong></td></tr><tr><td>First paint</td><td>slow (blank → JS → fetch)</td><td>fast</td><td><strong>fastest (CDN)</strong></td></tr><tr><td>SEO</td><td>weakest</td><td>good</td><td>good</td></tr><tr><td>Server cost</td><td>none (static files)</td><td><strong>per request</strong></td><td>none</td></tr><tr><td>Data freshness</td><td>live</td><td>live</td><td><strong>stale until rebuild</strong></td></tr><tr><td>Best for</td><td>dashboards, internal apps</td><td>e-commerce, personalised</td><td>blogs, docs, marketing</td></tr></tbody></table><h4>The SPA problem SSR solves</h4><pre><code>// What a crawler / a slow phone first receives from an SPA:\n&lt;body&gt;&lt;app-root&gt;&lt;/app-root&gt;&lt;script src=\"main.js\"&gt;&lt;/script&gt;&lt;/body&gt;\n// Empty. Then: download JS -&gt; parse -&gt; bootstrap -&gt; fetch data -&gt; render.</code></pre><p>Nothing is visible until the JS lands and runs — bad LCP on a slow network, and weak SEO because crawlers may not wait for JS. SSR sends real HTML on the first response, so content paints immediately.</p><h4>Hydration — where SSR's complexity lives</h4><p>SSR sends HTML, then the JS loads and Angular takes over the existing DOM. Historically it <strong>destroyed and re-rendered everything</strong>, causing a visible flicker. Angular 16+ has <strong>non-destructive hydration</strong> (<code class=\"inline\">provideClientHydration()</code>), which reuses the server DOM instead.</p><p>The gap between \"HTML visible\" and \"JS ready\" is the <strong>uncanny valley</strong>: the page looks done but clicks do nothing. So SSR improves <em>perceived</em> load and FCP/LCP, but can make <strong>TTI worse</strong> — you shipped HTML <em>and</em> the same JS bundle. That nuance is the senior answer.</p><h4>The SSR gotchas that break builds</h4><pre><code>// ✘ There is no `window` on the server -&gt; ReferenceError\nconst w = window.innerWidth;\n\n// ✔ guard it\nif (isPlatformBrowser(this.platformId)) { const w = window.innerWidth; }</code></pre><p><code class=\"inline\">window</code>, <code class=\"inline\">document</code>, and <code class=\"inline\">localStorage</code> don't exist in Node. Any library touching them at import time breaks SSR. This is the most common real-world SSR pain.</p><h4>The modern answer: it's per-route, not per-app</h4><p>The framing \"pick one\" is outdated. Angular 17+ ships <strong>hybrid rendering</strong>:</p><pre><code>export const serverRoutes: ServerRoute[] = [\n  { path: '',          renderMode: RenderMode.Prerender },  // marketing -&gt; SSG\n  { path: 'product/:id', renderMode: RenderMode.Server },   // SEO + live -&gt; SSR\n  { path: 'dashboard', renderMode: RenderMode.Client }      // private -&gt; CSR\n];</code></pre><p>Prerender the landing page, SSR the product pages, CSR the logged-in dashboard — where SEO is irrelevant and SSR would just cost you servers. Add <strong>ISR</strong> (regenerate a static page every N seconds) and the SSG \"stale data\" weakness mostly disappears.</p><h4>How to choose</h4><ul><li>Public + SEO + content rarely changes → <strong>SSG</strong>. Nothing beats a CDN file.</li><li>Public + SEO + personalised or live → <strong>SSR</strong>.</li><li>Behind a login → <strong>CSR</strong>. No crawler, no benefit, no server bill.</li></ul><h4>How to say it in an interview</h4><p>“It's about where the HTML is produced — browser, server per request, or build time. An SPA ships an empty shell, so first paint waits on JS, which hurts LCP and SEO; SSR sends real HTML then hydrates. The nuance is that SSR improves perceived performance and FCP but can worsen TTI, since you ship HTML and the same bundle — and there's a window where it looks interactive but isn't. Practically I don't pick one app-wide any more: Angular 17 does hybrid rendering, so I'd prerender marketing pages, SSR the SEO-critical dynamic ones, and leave the authenticated dashboard client-rendered.”</p>",
    "revise": "**SPA vs SSR vs SSG**.\n• **SPA** — renders in the browser; fast nav, weaker initial SEO/first paint.\n• **SSR** — server renders HTML per request; better SEO + first paint.\n• **SSG** — pre-render at build time; fastest, best for static content.\n• **Interview tip**: SSR/SSG mainly buy you **SEO and time-to-first-paint** for content-heavy sites."
  },
  {
    "id": "gen-time-complexity",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "algorithms",
      "big-o",
      "performance"
    ],
    "question": "What is Big-O and name common complexities.",
    "answer": "<p>Big-O describes how runtime/space grows with input size (worst case). Common: <code class=\"inline\">O(1)</code> constant, <code class=\"inline\">O(log n)</code> binary search, <code class=\"inline\">O(n)</code> linear scan, <code class=\"inline\">O(n log n)</code> good sorts, <code class=\"inline\">O(n²)</code> nested loops.</p>",
    "tip": "A HashMap lookup is O(1); replacing a nested loop with a map often turns O(n²) into O(n).",
    "code": "",
    "lang": "",
    "deep": "<h4>What it is</h4><p>Big-O describes how runtime <strong>grows with input size</strong>, ignoring constants and hardware. It's about the <em>shape</em> of the curve, not the speed — which is why an O(n²) can beat an O(n log n) on small inputs, and why it stops mattering at scale.</p><table><thead><tr><th>O</th><th>Name</th><th>n=1,000 →</th><th>Example</th></tr></thead><tbody><tr><td>O(1)</td><td>constant</td><td>1</td><td>hash lookup, array index</td></tr><tr><td>O(log n)</td><td>logarithmic</td><td>~10</td><td>binary search, balanced tree</td></tr><tr><td>O(n)</td><td>linear</td><td>1,000</td><td>a loop, <code class=\"inline\">find</code></td></tr><tr><td>O(n log n)</td><td>linearithmic</td><td>~10,000</td><td><strong>sorting</strong></td></tr><tr><td>O(n²)</td><td>quadratic</td><td><strong>1,000,000</strong></td><td>nested loop</td></tr><tr><td>O(2ⁿ)</td><td>exponential</td><td>💀</td><td>naive recursive fib</td></tr></tbody></table><p>The jump from n log n to n² at n=1,000 is 100×. That's why \"it worked in dev with 20 rows\" turns into a production incident with 20,000.</p><h4>The hidden O(n) that catches people</h4><pre><code>// Looks O(n). Is O(n²).\nfor (const user of users) {              // n\n  const order = orders.find(o =&gt; o.userId === user.id);   // ← n INSIDE the loop\n}\n\n// O(n) — build a lookup first\nconst byUser = new Map(orders.map(o =&gt; [o.userId, o]));   // n\nfor (const user of users) {                                // n\n  const order = byUser.get(user.id);                       // O(1)\n}</code></pre><p><strong>A nested <code class=\"inline\">find</code>/<code class=\"inline\">includes</code>/<code class=\"inline\">indexOf</code> is the most common real-world O(n²)</strong> — it doesn't look like a nested loop, because the inner loop is hidden in a method call. Trading memory for a Map is the standard fix, and it's exactly why NgRx's `@ngrx/entity` normalises to a dictionary.</p><h4>The rules</h4><ul><li><strong>Drop constants</strong> — O(2n) → O(n). Two sequential loops are still O(n).</li><li><strong>Drop lower terms</strong> — O(n² + n) → O(n²).</li><li><strong>Nested loops multiply</strong>; sequential loops add.</li><li>Different inputs get different letters: two separate arrays is <strong>O(n + m)</strong>, not O(n).</li></ul><h4>Space complexity counts too</h4><p>The Map fix above is O(n) <em>space</em> to buy O(n) time. That's the classic trade — and memoisation, caching, and lookup tables are all the same bargain. Mentioning space unprompted is a good signal.</p><h4>Best / average / worst</h4><p>Big-O is technically the <strong>upper bound</strong>. Quicksort is O(n log n) average but O(n²) worst case; a hash map is O(1) average but O(n) worst (all keys colliding) — which is exactly why Java 8 treeifies long HashMap chains, capping it at O(log n).</p><h4>Where it actually matters in frontend</h4><p>Honest framing: you rarely implement algorithms — you <em>choose data structures</em>. Using an array where a Map belongs, calling <code class=\"inline\">find</code> inside a loop over 10k rows, or re-sorting on every change-detection cycle. Those are the frontend Big-O bugs.</p><h4>How to say it in an interview</h4><p>“It's how runtime scales with input, dropping constants — the shape of the curve, not the speed. The one I actually hit in frontend code is the accidental O(n²): a <code class=\"inline\">find</code> inside a loop looks linear because the inner loop is hidden in the method call. Building a Map first makes it O(n), trading O(n) space for it. At a thousand items that's a hundred-fold difference, which is why something fine with twenty rows in dev falls over in production.”</p>",
    "revise": "**Big-O** = how runtime/space grows with input (worst case).\n• **O(1)** constant → **O(log n)** binary search → **O(n)** linear → **O(n log n)** good sorts → **O(n²)** nested loops.\n• **Interview tip**: a HashMap lookup is **O(1)** — swapping a nested loop for a map often turns **O(n²) into O(n)**, the most common optimization they want to hear."
  },
  {
    "id": "gen-array-linkedlist",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "data-structures",
      "performance",
      "big-o"
    ],
    "question": "Array vs Linked List vs Hash Table — tradeoffs.",
    "answer": "<ul><li><strong>Array</strong> — O(1) index access, O(n) insert/delete in the middle.</li><li><strong>Linked list</strong> — O(1) insert/delete at a known node, O(n) access.</li><li><strong>Hash table</strong> — O(1) average lookup/insert, unordered.</li></ul>",
    "tip": "Pick the structure by the operation you do most: lookups → hash, ordered access → array.",
    "code": "",
    "lang": "",
    "deep": "<h4>The trade-off table</h4><table><thead><tr><th>Operation</th><th>Array</th><th>Linked List</th><th>Hash Table</th></tr></thead><tbody><tr><td>Access by index</td><td><strong>O(1)</strong></td><td>O(n)</td><td>—</td></tr><tr><td>Lookup by key/value</td><td>O(n)</td><td>O(n)</td><td><strong>O(1)</strong> avg</td></tr><tr><td>Insert/delete at <strong>front</strong></td><td>O(n)</td><td><strong>O(1)</strong></td><td>—</td></tr><tr><td>Insert/delete at <strong>end</strong></td><td>O(1)*</td><td>O(1)</td><td>—</td></tr><tr><td>Insert/delete in <strong>middle</strong></td><td>O(n)</td><td>O(1)†</td><td>—</td></tr><tr><td>Memory</td><td>compact</td><td>+ pointer per node</td><td>+ empty buckets</td></tr><tr><td>Ordered?</td><td>✔</td><td>✔</td><td><strong>✘</strong></td></tr></tbody></table><p><em>*amortised — see below. †O(1) only if you already hold the node; finding it is O(n).</em></p><h4>Why each is what it is</h4><ul><li><strong>Array</strong> — one contiguous block, so element <em>i</em> is at <code class=\"inline\">base + i × size</code>: pure arithmetic, O(1). But inserting at the front means <strong>shifting every element</strong> — O(n).</li><li><strong>Linked list</strong> — nodes scattered on the heap, each pointing to the next. No shifting, but no arithmetic either: reaching index 500 means walking 500 pointers.</li><li><strong>Hash table</strong> — hash the key to a bucket index. Gives up ordering and memory to buy O(1) lookup.</li></ul><h4>The dirty secret: linked lists lose in practice</h4><p>Their O(1) middle-insert is real but usually unreachable — you pay O(n) to <em>find</em> the position first. Worse, arrays win on <strong>cache locality</strong>: contiguous memory means the CPU prefetches the next elements, while a linked list's pointer-chasing causes a cache miss per node. A cache miss is ~100× a cache hit, so an \"O(n) array shift\" routinely beats an \"O(1) linked-list insert\" on real hardware for anything but huge n.</p><p>Saying this is a strong signal — it shows you know Big-O ignores constants, and constants sometimes decide the winner.</p><h4>The JavaScript reality</h4><pre><code>[1, 2, 3]           // NOT a real array — a JS \"array\" is an object with\n                    // integer-ish keys. V8 optimises it into a real\n                    // contiguous array only while it stays \"packed\" —\n                    // same types, no holes.\n\narr[10000] = 1;     // creates a sparse/\"holey\" array -&gt; V8 DEOPTIMISES it\ndelete arr[0];      // same — makes it holey. Use splice().</code></pre><p>So <code class=\"inline\">push</code> is amortised O(1) (the backing store doubles and copies when full), but you can silently lose the fast path. And JS has <strong>no built-in linked list</strong> — if you want one you build it, which is itself a hint you probably don't need one.</p><h4>What to actually reach for</h4><ul><li><strong>Array</strong> — the default. Ordered, indexable, cache-friendly.</li><li><strong>Map</strong> — lookup by key. Real O(1), any key type, keeps insertion order, and unlike an object it has no prototype keys to collide with.</li><li><strong>Set</strong> — uniqueness / membership. <code class=\"inline\">set.has(x)</code> is O(1) versus <code class=\"inline\">arr.includes(x)</code> at O(n) — the single easiest real-world win.</li><li><strong>Linked list</strong> — essentially never in app code. It's inside LRU caches and schedulers.</li></ul><h4>How to say it in an interview</h4><p>“Arrays are contiguous, so index access is arithmetic — O(1) — but inserting at the front shifts everything. Linked lists flip that: O(1) insert if you hold the node, but O(n) to reach it. Hash tables trade ordering and memory for O(1) lookup. In practice arrays usually beat linked lists even where Big-O says otherwise, because contiguous memory is cache-friendly and pointer-chasing isn't. In JS I'd just use an array by default and a Map or Set the moment I'm doing lookups — swapping <code class=\"inline\">includes</code> in a loop for a Set is the easiest real performance win there is.”</p>",
    "revise": "Structure by the operation you do most:\n• **Array** — O(1) index access, O(n) middle insert/delete.\n• **Linked list** — O(1) insert/delete at a known node, O(n) access.\n• **Hash table** — O(1) average lookup/insert, unordered.\n• **Interview tip**: lookups → **hash**; ordered/indexed access → **array**; frequent middle splices → **linked list**."
  },
  {
    "id": "gen-solid",
    "category": "general",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "design",
      "solid",
      "principles",
      "architecture"
    ],
    "question": "What are the SOLID principles?",
    "answer": "<ul><li><strong>S</strong> — Single Responsibility (one reason to change).</li><li><strong>O</strong> — Open/Closed (open to extension, closed to modification).</li><li><strong>L</strong> — Liskov Substitution (subtypes replace base types safely).</li><li><strong>I</strong> — Interface Segregation (small, focused interfaces).</li><li><strong>D</strong> — Dependency Inversion (depend on abstractions).</li></ul>",
    "tip": "Angular's DI is a direct application of the Dependency Inversion principle.",
    "code": "",
    "lang": "",
    "deep": "<h4>SOLID in an Angular/TypeScript codebase</h4><p>Interviewers ask this of frontend devs to see whether you can apply it to <em>components and services</em> rather than recite Java examples. Anchor every principle to Angular.</p><h4>S — Single Responsibility</h4><pre><code>// ✘ A component doing four jobs\nexport class UserListComponent {\n  users: User[] = [];\n  async ngOnInit() {\n    const res = await fetch('/api/users');        // 1. HTTP\n    this.users = (await res.json())\n      .filter(u =&gt; u.active)                      // 2. business rules\n      .sort((a, b) =&gt; a.name.localeCompare(b.name)); // 3. sorting\n    localStorage.setItem('users', ...);           // 4. persistence\n  }\n}</code></pre><p>Four reasons to change; untestable without mocking the world. Split: component renders, <code class=\"inline\">UserService</code> fetches, a selector/pipe derives.</p><p>The frontend smell: a component over ~200 lines, or one whose name contains \"And\".</p><h4>O — Open/Closed</h4><pre><code>// ✘ every new type edits this switch\ngetIcon(type: string) {\n  switch (type) { case 'pdf': return 'file-pdf'; case 'doc': return 'file-word'; }\n}\n\n// ✔ open for extension — add a config entry, change no code\nconst ICONS: Record&lt;FileType, string&gt; = { pdf: 'file-pdf', doc: 'file-word' };</code></pre><p>In Angular this is content projection (<code class=\"inline\">ng-content</code>) and DI tokens: extend behaviour by <em>providing</em> something new, not by editing the component.</p><h4>L — Liskov Substitution</h4><p>A subtype must work anywhere its parent does. The frontend version usually isn't classes — it's <strong>components honouring a contract</strong>: if <code class=\"inline\">BaseInputComponent</code> emits <code class=\"inline\">valueChange</code> on every keystroke, a subclass that only emits on blur breaks every parent silently. Red flag: overriding a method to throw, or to do nothing.</p><h4>I — Interface Segregation</h4><pre><code>// ✘ a fat input surface — most consumers pass 3 of 12\n@Input() config!: { showHeader; showFooter; showToolbar; enableSort; ... };\n\n// ✔ focused inputs with sane defaults, or split the component</code></pre><p>Also: don't force a consumer to depend on methods it never calls. A 12-<code class=\"inline\">@Input</code> component is usually several components.</p><h4>D — Dependency Inversion ← the one Angular is built on</h4><pre><code>// ✘ depends on a concretion — impossible to test without a real network\nexport class OrderService {\n  private http = new HttpClient(...);\n}\n\n// ✔ depends on an abstraction, injected\nexport class OrderService {\n  private repo = inject(ORDER_REPOSITORY);   // an InjectionToken\n}\n\n// test:\nproviders: [{ provide: ORDER_REPOSITORY, useValue: mockRepo }]</code></pre><p><strong>Angular's DI <em>is</em> dependency inversion.</strong> That's the whole reason you can swap a real service for a spy in <code class=\"inline\">TestBed</code>. If asked \"why does Angular have DI?\", this is the answer — and it links straight to testability.</p><h4>The honest caveat</h4><p>SOLID came from 1990s OO; it's guidance, not law. Applied dogmatically you get an interface per class and indirection nobody can follow. The two that pay off daily in frontend are <strong>SRP</strong> and <strong>DIP</strong>; the rest are lenses you reach for when something feels wrong.</p><h4>How to say it in an interview</h4><p>“In Angular terms: single responsibility means the component renders and the service fetches — a component doing HTTP, filtering and persistence has four reasons to change and can't be tested. Dependency inversion is the one Angular is literally built on — DI means I depend on an injection token rather than <code class=\"inline\">new</code>-ing a dependency, which is exactly what lets TestBed swap in a mock. Open/closed shows up as content projection and config-driven behaviour instead of a switch you edit for every new type. I treat them as lenses rather than rules — SRP and DIP earn their keep daily, the others when something already smells.”</p>",
    "revise": "**SOLID** OOP principles:\n• **S** — Single Responsibility (one reason to change).\n• **O** — Open/Closed (extend, don't modify).\n• **L** — Liskov Substitution (subtypes swap in safely).\n• **I** — Interface Segregation (small, focused interfaces).\n• **D** — Dependency Inversion (depend on abstractions).\n• **Interview tip**: **Angular's DI** is a direct application of the **D** — great concrete example to cite."
  },
  {
    "id": "gen-agile",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "agile",
      "scrum",
      "process"
    ],
    "question": "Explain Agile / Scrum basics.",
    "answer": "<p><strong>Scrum</strong> works in fixed <strong>sprints</strong> (1–4 weeks). Ceremonies: sprint planning, daily stand-up, review (demo), and retrospective. Roles: Product Owner (backlog), Scrum Master (process), and the development team.</p>",
    "tip": "Stand-ups answer three things: what I did, what I'll do, what's blocking me.",
    "code": "",
    "lang": "",
    "revise": "**Scrum** = fixed-length sprints (1–4 wks).\n• **Ceremonies**: planning, daily stand-up, review (demo), retrospective.\n• **Roles**: Product Owner (backlog), Scrum Master (process), dev team.\n• **Interview tip**: stand-ups answer three things — what I did, what I'll do, what's blocking me."
  },
  {
    "id": "gen-reverse-proxy",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "networking",
      "nginx",
      "infrastructure"
    ],
    "question": "What is a reverse proxy / load balancer? Nginx basics.",
    "answer": "<p>A <strong>reverse proxy</strong> sits in front of your servers, accepting client traffic and forwarding it inward (clients never see the backends). It centralizes: <strong>TLS termination</strong>, compression, caching, rate limiting, path routing (<code class=\"inline\">/api</code> → backend, <code class=\"inline\">/</code> → static Angular files).</p><p>A <strong>load balancer</strong> is a reverse proxy distributing across multiple instances — round robin, least-connections, IP-hash — with health checks ejecting dead backends. (Forward proxy = the mirror image: sits in front of <em>clients</em>.)</p>",
    "tip": "The Angular+Spring deployment everyone actually runs: nginx serves the built static files, proxies /api to Spring Boot, and try_files handles SPA route refreshes.",
    "code": "server {\n  listen 443 ssl;\n  location /api/ { proxy_pass http://backend:8080/; }\n  location /     { root /usr/share/nginx/html; try_files $uri /index.html; }\n}\nupstream backend { server app1:8080; server app2:8080; }  # least_conn; health checks",
    "lang": "bash",
    "revise": "A **reverse proxy** sits in front of your servers, forwarding client traffic inward (clients never see backends).\n• Centralises **TLS termination**, compression, caching, rate limiting, path routing (`/api`→backend, `/`→static).\n• A **load balancer** = a reverse proxy spreading traffic across instances (round-robin/least-conn/IP-hash) with health checks. (Forward proxy = the mirror image, in front of clients.)\n• **Interview tip**: the real-world Angular+Spring setup — **nginx** serves built static files, proxies `/api` to Spring Boot, and `try_files` fixes SPA route refreshes."
  },
  {
    "id": "gen-user-story-invest",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "agile",
      "requirements"
    ],
    "question": "What is a user story? INVEST criteria and acceptance criteria?",
    "answer": "<p>A user story is a small requirement from the user's viewpoint: <em>As a &lt;role&gt; I want &lt;capability&gt; so that &lt;benefit&gt;</em> — a placeholder for a conversation, not a spec.</p><p><strong>INVEST</strong>: <strong>I</strong>ndependent (schedulable alone), <strong>N</strong>egotiable, <strong>V</strong>aluable (user-visible benefit), <strong>E</strong>stimable, <strong>S</strong>mall (fits a sprint), <strong>T</strong>estable.</p><p><strong>Acceptance criteria</strong> — the concrete conditions that make it done, often Given/When/Then; they become the test cases and kill \"that's not what I meant\" at demo time.</p>",
    "tip": "Be ready with a real example from your project, including one acceptance criterion that caught an edge case (empty state, permission denied) before development started.",
    "code": "Story: As a customer I want to filter orders by date so I can find a past order.\n\nAcceptance criteria:\n  Given orders exist, When I pick a date range, Then only orders in it show\n  Given no orders match, Then an empty state with a clear-filters action shows\n  Date range max 1 year; invalid range shows inline validation",
    "lang": "bash",
    "revise": "**User story** = a need from the user's view: *As a <role> I want <capability> so that <benefit>* — a placeholder for a conversation, not a spec.\n• **INVEST**: Independent, Negotiable, Valuable, Estimable, Small, Testable.\n• **Acceptance criteria** = concrete done-conditions (Given/When/Then) that become the test cases.\n• **Interview tip**: have a real example ready, including one acceptance criterion that caught an edge case (empty state, permission denied) before coding started."
  },
  {
    "id": "gen-estimation",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "agile",
      "estimation",
      "V.Imp"
    ],
    "question": "Story points and estimation — planning poker, velocity, burndown?",
    "answer": "<p><strong>Story points</strong> measure relative effort+complexity+uncertainty (Fibonacci 1,2,3,5,8...), not hours — a 5 is 'about like that other 5', which dodges human badness at absolute estimates.</p><p><strong>Planning poker</strong>: everyone reveals an estimate simultaneously (no anchoring); big splits trigger discussion — the outliers usually know something. <strong>T-shirt sizing</strong> (S/M/L) for coarse roadmap-level sizing.</p><p><strong>Velocity</strong> — points completed per sprint, averaged; it's the team's <em>empirical</em> capacity for forecasting, never a performance KPI. <strong>Burndown</strong> — remaining work vs time in the sprint; a flat line mid-sprint = blocked or bad breakdown.</p>",
    "tip": "For 'how do YOU estimate': decompose to tasks, compare against a reference story, size the unknowns (spike if too fuzzy), add integration/testing effort explicitly — concrete process beats theory.",
    "code": "",
    "lang": "",
    "revise": "**Story points** = relative effort+complexity+uncertainty (Fibonacci), not hours.\n• **Planning poker** — simultaneous reveal (no anchoring); big splits → discuss.\n• **Velocity** — points/sprint averaged = capacity for forecasting, **not a KPI**.\n• **Burndown** — remaining work vs time; flat mid-sprint = blocked.\n• **Interview tip**: for 'how do YOU estimate' — decompose to tasks, compare to a reference story, spike the unknowns, add integration/testing effort explicitly."
  },
  {
    "id": "gen-dor-dod",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "agile",
      "process"
    ],
    "question": "Definition of Ready vs Definition of Done? How do you handle mid-sprint scope changes?",
    "answer": "<p><strong>DoR</strong> — a story may enter a sprint only when: clear acceptance criteria, dependencies identified, estimated, testable. Protects the sprint from half-baked work.</p><p><strong>DoD</strong> — 'done' means: code + review + tests passing, acceptance criteria met, merged, deployed to the agreed environment, docs updated. One shared definition kills \"done but not done-done\".</p><p><strong>Mid-sprint changes</strong>: the sprint goal is protected — genuine urgencies (prod incident) swap something <em>out</em> of equal size rather than piling on; everything else goes to the backlog for next planning. Constant mid-sprint churn is a signal to shorten sprints or move to Kanban.</p>",
    "tip": "The mature answer isn't 'never change scope' — it's 'trade, don't add', and make the cost visible to the product owner.",
    "code": "",
    "lang": "",
    "revise": "**DoR vs DoD**.\n• **Definition of Ready** — a story may enter a sprint only when: clear acceptance criteria, deps identified, estimated, testable.\n• **Definition of Done** — code + review + tests passing, criteria met, merged, deployed, docs updated.\n• Mid-sprint change → **trade, don't add** (swap equal-size work); protect the sprint goal.\n• **Interview tip**: the mature answer isn't 'never change scope' — it's 'trade, don't add, and make the cost visible to the PO'."
  },
  {
    "id": "gen-kanban-scrum",
    "category": "general",
    "difficulty": "beginner",
    "tags": [
      "agile",
      "process"
    ],
    "question": "Kanban vs Scrum?",
    "answer": "<ul><li><strong>Scrum</strong> — fixed-length sprints with commitment, defined roles (PO/SM), ceremonies (planning, standup, review, retro), velocity per sprint. Fits feature work that batches well.</li><li><strong>Kanban</strong> — continuous flow, no sprints: a board with <strong>WIP limits</strong> per column; pull the next item when capacity frees; measure <strong>cycle time</strong> and throughput. Fits support/ops and interrupt-heavy streams.</li></ul><p>Many teams run <strong>Scrumban</strong> — sprint cadence for planning/retros, WIP limits and pull for daily flow.</p>",
    "tip": "The differentiator is WIP limits: Kanban optimizes flow by capping simultaneous work; Scrum optimizes predictability by time-boxed commitment. Say which your team used and why.",
    "code": "",
    "lang": "",
    "revise": "**Kanban vs Scrum**.\n• **Scrum** — fixed sprints, roles (PO/SM), ceremonies, velocity → predictability for batchable feature work.\n• **Kanban** — continuous flow, **WIP limits** per column, pull next item, measure cycle time/throughput → interrupt-heavy/support work.\n• Many run **Scrumban** (sprint cadence + WIP limits).\n• **Interview tip**: the differentiator is **WIP limits** — Kanban optimizes flow, Scrum optimizes predictability. Say which you used and why."
  },
  {
    "id": "gen-requirements-translation",
    "category": "general",
    "difficulty": "intermediate",
    "tags": [
      "requirements",
      "process",
      "V.Imp"
    ],
    "question": "How do you translate functional requirements into technical/system requirements? What is performance engineering?",
    "answer": "<p>A repeatable pipeline:</p><ol><li><strong>Clarify</strong> the functional requirement with concrete scenarios and acceptance criteria (what exactly must happen, for whom, how often).</li><li><strong>Derive system requirements</strong> — data model changes, API contracts (endpoints, payloads, status codes), integration points, authorization rules.</li><li><strong>Extract the non-functionals hidden inside</strong> — expected volumes, latency targets, concurrency, auditability, retention. \"Show order history\" secretly contains pagination, an index, and an SLA.</li><li><strong>Design + spike</strong> unknowns, then split into estimable stories with traceability back to the source requirement.</li></ol><p><strong>Performance engineering</strong> — treating performance as a requirement through the lifecycle, not a post-hoc fix: set numeric targets (p95 latency, throughput), test against them (JMeter/Gatling, Lighthouse for frontend), profile bottlenecks (APM, EXPLAIN plans, bundle analysis), fix, and <strong>guard in CI</strong> (perf budgets) so regressions fail the build.</p>",
    "tip": "Have one worked example ready: functional ask → the API you designed → the NFR you surfaced (e.g. 50k rows ⇒ keyset pagination + index) → the measured result. JD-critical for service companies.",
    "code": "",
    "lang": "",
    "revise": "Turn a functional ask into technical requirements via a repeatable pipeline:\n• **Clarify** with concrete scenarios + acceptance criteria.\n• **Derive system reqs** — data model, API contracts, integrations, authz.\n• **Extract hidden non-functionals** — volumes, latency, concurrency, retention ('show order history' hides pagination + an index + an SLA).\n• Spike unknowns, split into estimable stories with traceability.\n• **Interview tip**: have one worked example — functional ask → API you designed → NFR you surfaced (50k rows ⇒ keyset pagination + index) → measured result."
  }
]
