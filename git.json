[
  {
    "id": "git-merge-rebase",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "merge",
      "rebase",
      "history"
    ],
    "question": "Merge vs rebase.",
    "answer": "<p><strong>Merge</strong> combines branches with a merge commit — preserves the true history, non-destructive.</p><p><strong>Rebase</strong> replays your commits on top of another branch — a linear, clean history, but rewrites commits. Never rebase branches others have pulled.</p>",
    "tip": "Golden rule: don't rebase shared/public branches.",
    "code": "git rebase main   # replay feature commits onto main",
    "lang": "bash"
  },
  {
    "id": "git-fetch-pull",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "fetch",
      "pull"
    ],
    "question": "git fetch vs git pull.",
    "answer": "<p><strong>fetch</strong> downloads remote changes but doesn't touch your working branch — you can inspect first. <strong>pull</strong> = fetch + merge (or rebase) into your current branch in one step.</p>",
    "tip": "Use fetch + review before merging when you want to see what changed.",
    "code": "",
    "lang": ""
  },
  {
    "id": "git-reset-revert",
    "category": "git",
    "difficulty": "advanced",
    "tags": [
      "reset",
      "revert",
      "undo"
    ],
    "question": "reset vs revert vs checkout.",
    "answer": "<ul><li><strong>revert</strong> — creates a new commit that undoes a previous one; safe on shared branches.</li><li><strong>reset</strong> — moves the branch pointer (soft/mixed/hard); rewrites history, use locally.</li><li><strong>checkout / switch / restore</strong> — move between branches or restore files.</li></ul>",
    "tip": "On a shared branch, undo with revert (safe), not reset (rewrites history).",
    "code": "git revert <hash>   # safe undo\ngit reset --hard HEAD~1  # local-only",
    "lang": "bash"
  },
  {
    "id": "git-stash",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "stash"
    ],
    "question": "What does git stash do?",
    "answer": "<p>Temporarily shelves uncommitted changes so you can switch context (e.g. fix an urgent bug on another branch), then reapply them with <code class=\"inline\">git stash pop</code>.</p>",
    "tip": "git stash pop applies and drops; git stash apply keeps the stash.",
    "code": "git stash\ngit switch main\n# ... later ...\ngit switch feature && git stash pop",
    "lang": "bash"
  },
  {
    "id": "git-cherry-pick",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "cherry-pick"
    ],
    "question": "What is git cherry-pick?",
    "answer": "<p>Applies a single specific commit from one branch onto another — useful for hotfixes you need on multiple branches without merging everything.</p>",
    "tip": "Great for porting one bug-fix commit to a release branch.",
    "code": "git cherry-pick <hash>",
    "lang": "bash"
  },
  {
    "id": "git-gitflow",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "workflow",
      "gitflow",
      "branching"
    ],
    "question": "Describe a common branching strategy.",
    "answer": "<p><strong>GitFlow</strong> — long-lived <code class=\"inline\">main</code> and <code class=\"inline\">develop</code>, plus <code class=\"inline\">feature/*</code>, <code class=\"inline\">release/*</code>, and <code class=\"inline\">hotfix/*</code> branches. Simpler alternative: <strong>trunk-based</strong> / <strong>GitHub Flow</strong> — short-lived feature branches merged to main via pull requests.</p>",
    "tip": "Most modern teams favour short-lived branches + PRs over full GitFlow.",
    "code": "",
    "lang": ""
  },
  {
    "id": "git-conflict",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "merge-conflict"
    ],
    "question": "How do you resolve a merge conflict?",
    "answer": "<p>Git marks conflicting regions with <code class=\"inline\">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> / <code class=\"inline\">=======</code> / <code class=\"inline\">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>. Edit to the desired result, remove the markers, <code class=\"inline\">git add</code> the files, then commit (or <code class=\"inline\">git rebase --continue</code>).</p>",
    "tip": "Pull/rebase frequently to keep conflicts small and manageable.",
    "code": "",
    "lang": ""
  },
  {
    "id": "git-reflog",
    "category": "git",
    "difficulty": "advanced",
    "tags": [
      "reflog",
      "recovery"
    ],
    "question": "How do you recover a 'lost' commit?",
    "answer": "<p><code class=\"inline\">git reflog</code> logs where HEAD has been, including commits orphaned by a reset/rebase. Find the hash and <code class=\"inline\">git checkout</code> or <code class=\"inline\">git reset</code> back to it.</p>",
    "tip": "reflog is the safety net after a bad reset — commits aren't truly gone immediately.",
    "code": "git reflog\ngit reset --hard <hash>",
    "lang": "bash"
  },
  {
    "id": "git-squash",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "squash",
      "history"
    ],
    "question": "What is squashing and why do it?",
    "answer": "<p>Combining several commits into one (via interactive rebase or a squash-merge) to keep the main branch history clean — one logical change = one commit.</p>",
    "tip": "Squash-merge on PRs keeps main's history readable without losing branch detail.",
    "code": "git rebase -i HEAD~3   # mark commits as 'squash'",
    "lang": "bash"
  },
  {
    "id": "git-head",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "head",
      "detached"
    ],
    "question": "What is HEAD and a detached HEAD?",
    "answer": "<p><strong>HEAD</strong> points to your current commit/branch. A <strong>detached HEAD</strong> means you've checked out a specific commit rather than a branch — new commits there aren't on any branch and can be lost unless you create one.</p>",
    "tip": "If you commit in detached HEAD, make a branch before switching away.",
    "code": "git switch -c new-branch  # save detached-HEAD work",
    "lang": "bash"
  },
  {
    "id": "git-code-review",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "code-review",
      "process",
      "V.Imp"
    ],
    "question": "What do you look for in a code review? Describe your PR process.",
    "answer": "<p>A layered checklist, most important first:</p><ul><li><strong>Correctness</strong> — does it do what the ticket says; edge cases (null/empty/concurrent); error handling.</li><li><strong>Design</strong> — right layer for the logic, no duplication of existing utilities, sane API shapes, backward compatibility.</li><li><strong>Safety</strong> — SQL injection/XSS, secrets in code, N+1 queries, missing indexes on new queries, transaction boundaries.</li><li><strong>Tests</strong> — meaningful cases (not just coverage), failure paths tested.</li><li><strong>Readability last</strong> — naming, dead code; style nits belong to the linter, not humans.</li></ul><p>Process: small PRs (&lt; ~400 lines review well), a description that says <em>why</em>, CI green before human eyes, comments phrased as questions, and approval means <em>I'd maintain this</em>.</p>",
    "tip": "Have one story ready: a bug you caught in review (concurrency, missing rollback) and one piece of pushback you accepted gracefully — review questions are always secretly behavioral.",
    "code": "",
    "lang": ""
  },
  {
    "id": "git-staging-area",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "staging",
      "index",
      "basics",
      "V.Imp"
    ],
    "question": "Working directory, staging area, and repository.",
    "answer": "<p>Git has three 'trees':</p><ul><li><strong>Working directory</strong> — the actual files you edit.</li><li><strong>Staging area (index)</strong> — a snapshot you're preparing; <code class=\"inline\">git add</code> moves changes here.</li><li><strong>Repository</strong> — committed history; <code class=\"inline\">git commit</code> records the staged snapshot permanently.</li></ul><p>The staging area is what lets you commit only <em>some</em> of your changes.</p>",
    "tip": "git add -p stages hunks selectively — the key to small, logical commits from a messy working tree.",
    "code": "git add file.js      # working dir -> staging\ngit commit -m \"msg\"  # staging -> repository",
    "lang": "bash"
  },
  {
    "id": "git-clone-vs-fork",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "clone",
      "fork",
      "remote"
    ],
    "question": "clone vs fork.",
    "answer": "<p><strong>clone</strong> — a Git operation that makes a local copy of a repository (with full history) that you can pull from and, with access, push to.</p><p><strong>fork</strong> — a hosting-platform (GitHub/GitLab) concept: a server-side copy of a repo under <em>your</em> account. You fork a project you can't push to, clone your fork, then open a pull request back to the original.</p>",
    "tip": "Fork is the open-source contribution model; clone is just getting the code locally. Fork lives on the server, clone on your machine.",
    "code": "git clone https://github.com/user/repo.git",
    "lang": "bash"
  },
  {
    "id": "git-gitignore",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "gitignore",
      "tracking"
    ],
    "question": ".gitignore — how it works and the common mistakes.",
    "answer": "<p><code class=\"inline\">.gitignore</code> lists path patterns Git should not track (build output, <code class=\"inline\">node_modules</code>, secrets). It only affects <strong>untracked</strong> files.</p><p>Gotcha: a file <em>already tracked</em> keeps being tracked even after you add it to .gitignore — you must untrack it explicitly.</p>",
    "tip": "Committed a secret then added it to .gitignore? It's still in history — remove it with git rm --cached AND rotate the secret.",
    "code": "git rm --cached .env      # stop tracking, keep the file\necho \".env\" >> .gitignore",
    "lang": "bash"
  },
  {
    "id": "git-log-diff",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "log",
      "diff",
      "inspection"
    ],
    "question": "Inspecting history: log, diff, and show.",
    "answer": "<ul><li><strong>git log</strong> — commit history; <code class=\"inline\">--oneline --graph</code> for a compact branch view.</li><li><strong>git diff</strong> — unstaged changes; <code class=\"inline\">--staged</code> for what's about to commit; <code class=\"inline\">A..B</code> to compare commits/branches.</li><li><strong>git show &lt;commit&gt;</strong> — the full patch and metadata of one commit.</li></ul>",
    "tip": "git log --oneline --graph --all is the fastest way to see how your branches actually relate.",
    "code": "git log --oneline --graph --all\ngit diff --staged\ngit show HEAD~2",
    "lang": "bash"
  },
  {
    "id": "git-tags",
    "category": "git",
    "difficulty": "beginner",
    "tags": [
      "tags",
      "releases"
    ],
    "question": "Git tags — lightweight vs annotated.",
    "answer": "<p>A <strong>tag</strong> is a named pointer to a specific commit, typically marking a release.</p><ul><li><strong>Lightweight</strong> — just a name → commit pointer.</li><li><strong>Annotated</strong> (<code class=\"inline\">-a</code>) — a full object storing tagger, date, message, and can be signed. Recommended for releases.</li></ul><p>Tags aren't pushed by default — you must push them explicitly.</p>",
    "tip": "Use annotated tags for releases (they carry who/when/why); push with git push --tags or git push origin v1.0.",
    "code": "git tag -a v1.2.0 -m \"Release 1.2.0\"\ngit push origin v1.2.0",
    "lang": "bash"
  },
  {
    "id": "git-amend",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "amend",
      "commit",
      "history"
    ],
    "question": "What does git commit --amend do?",
    "answer": "<p>It <strong>replaces</strong> the most recent commit with a new one — letting you fix the commit message or fold in additional staged changes you forgot. Technically it creates a brand-new commit (new hash) and moves the branch to it.</p>",
    "tip": "Because it rewrites history, never amend a commit you've already pushed to a shared branch — you'd force others to reconcile.",
    "code": "git add forgotten.js\ngit commit --amend --no-edit   # fold in, keep message",
    "lang": "bash"
  },
  {
    "id": "git-rebase-interactive",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "rebase",
      "history",
      "cleanup",
      "V.Imp"
    ],
    "question": "Interactive rebase — what can it do?",
    "answer": "<p><code class=\"inline\">git rebase -i</code> opens an editable list of commits so you can rewrite a series before merging:</p><ul><li><strong>reword</strong> — change a message</li><li><strong>squash / fixup</strong> — combine commits (fixup discards the message)</li><li><strong>edit</strong> — stop to amend a specific commit</li><li><strong>reorder / drop</strong> — rearrange or delete commits</li></ul>",
    "tip": "Clean up your messy WIP commits into a tidy story right before opening the PR — but only on your own un-pushed branch.",
    "code": "git rebase -i HEAD~4   # curate the last 4 commits",
    "lang": "bash"
  },
  {
    "id": "git-fast-forward",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "merge",
      "fast-forward"
    ],
    "question": "Fast-forward vs no-ff merge.",
    "answer": "<p>A <strong>fast-forward</strong> merge happens when the target branch hasn't diverged — Git just moves the branch pointer forward, no merge commit, a perfectly linear history.</p><p><code class=\"inline\">--no-ff</code> forces a merge commit even when a fast-forward is possible, preserving the fact that a feature branch existed as a distinct group of commits.</p>",
    "tip": "Many teams merge PRs with --no-ff so each feature is one revertable merge commit in main's history.",
    "code": "git merge --no-ff feature   # always create a merge commit",
    "lang": "bash"
  },
  {
    "id": "git-pull-rebase",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "pull",
      "rebase",
      "workflow"
    ],
    "question": "git pull --rebase vs a plain pull.",
    "answer": "<p>A default <code class=\"inline\">git pull</code> does fetch + <strong>merge</strong>, which can litter history with 'Merge branch main' commits when you and the remote both moved.</p><p><code class=\"inline\">git pull --rebase</code> instead <strong>replays your local commits on top</strong> of the fetched remote — a clean, linear history with no extra merge commits.</p>",
    "tip": "Set it once and forget it: git config --global pull.rebase true.",
    "code": "git pull --rebase origin main",
    "lang": "bash"
  },
  {
    "id": "git-bisect",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "bisect",
      "debugging"
    ],
    "question": "How does git bisect find a bug?",
    "answer": "<p>A <strong>binary search</strong> through history for the commit that introduced a bug. You mark a known-<code class=\"inline\">bad</code> commit and a known-<code class=\"inline\">good</code> one; Git checks out the midpoint, you test and mark it good/bad, and it halves the range each step until it pinpoints the culprit.</p>",
    "tip": "Automate it: git bisect run ./test.sh lets Git find the breaking commit hands-free across hundreds of commits.",
    "code": "git bisect start\ngit bisect bad            # current is broken\ngit bisect good v1.0     # this tag worked\n# test each checkout, then:  git bisect good|bad\ngit bisect reset",
    "lang": "bash"
  },
  {
    "id": "git-blame",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "blame",
      "history"
    ],
    "question": "What is git blame for?",
    "answer": "<p><code class=\"inline\">git blame</code> annotates each line of a file with the commit, author, and date that <strong>last changed it</strong> — the way to find <em>when</em> and <em>why</em> a specific line came to be, then read that commit's message/PR for context.</p>",
    "tip": "Blame tells you the last change, not the origin — use git log -L to follow a line's full evolution.",
    "code": "git blame -L 40,60 src/app.js   # who touched lines 40-60",
    "lang": "bash"
  },
  {
    "id": "git-hooks",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "hooks",
      "automation"
    ],
    "question": "What are Git hooks?",
    "answer": "<p>Scripts Git runs automatically on events: <strong>pre-commit</strong> (lint/format), <strong>commit-msg</strong> (enforce message format), <strong>pre-push</strong> (run tests), <strong>post-merge</strong>, etc.</p><p>They live in <code class=\"inline\">.git/hooks</code> and are <em>not</em> committed — so teams use tools like <strong>Husky</strong> or <strong>pre-commit</strong> to share and version them.</p>",
    "tip": "Hooks are local-only by design; to guarantee a check for everyone, enforce it in CI too — a dev can always skip a hook with --no-verify.",
    "code": "# .husky/pre-commit\nnpm run lint && npm test",
    "lang": "bash"
  },
  {
    "id": "git-reset-modes",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "reset",
      "undo",
      "V.Imp"
    ],
    "question": "reset --soft vs --mixed vs --hard.",
    "answer": "<p>All three move <code class=\"inline\">HEAD</code> to a target commit; they differ in what else they touch:</p><ul><li><strong>--soft</strong> — moves HEAD only; changes stay <em>staged</em>.</li><li><strong>--mixed</strong> (default) — moves HEAD and unstages; changes remain in the working directory.</li><li><strong>--hard</strong> — moves HEAD and <em>discards</em> both staged and working changes.</li></ul>",
    "tip": "--soft is the 'uncommit but keep everything' undo; --hard is destructive — recover only via reflog if you regret it.",
    "code": "git reset --soft HEAD~1   # undo last commit, keep changes staged\ngit reset --hard HEAD~1   # undo last commit, throw changes away",
    "lang": "bash"
  },
  {
    "id": "git-submodules",
    "category": "git",
    "difficulty": "intermediate",
    "tags": [
      "submodules",
      "dependencies"
    ],
    "question": "What are Git submodules?",
    "answer": "<p>A submodule embeds one repository inside another, pinned to a <strong>specific commit</strong> of the child. The parent tracks only that pointer, not the child's files.</p><p>Pros: share a library across projects with exact version control. Cons: extra ceremony — clones need <code class=\"inline\">--recurse-submodules</code>, and it's easy to forget to commit an updated pointer.</p>",
    "tip": "Fresh clone missing submodule files? Run git submodule update --init --recursive.",
    "code": "git clone --recurse-submodules <url>\ngit submodule update --init --recursive",
    "lang": "bash"
  },
  {
    "id": "git-filter-repo",
    "category": "git",
    "difficulty": "advanced",
    "tags": [
      "history",
      "secrets",
      "rewrite"
    ],
    "question": "How do you remove a secret or large file from all of history?",
    "answer": "<p>Deleting it in a new commit isn't enough — it's still in every prior commit. Use <strong>git filter-repo</strong> (the modern replacement for <code class=\"inline\">filter-branch</code>, or the <strong>BFG</strong> tool) to rewrite <em>every</em> commit and purge the file/string.</p><p>This changes all commit hashes, so you must force-push and everyone must re-clone — and you should <strong>rotate the leaked secret regardless</strong>, since it may already be cloned.</p>",
    "tip": "Rewriting history is the cleanup; rotating the credential is the actual fix — assume any pushed secret is already compromised.",
    "code": "git filter-repo --path secrets.env --invert-paths\ngit push --force --all",
    "lang": "bash"
  },
  {
    "id": "git-worktree",
    "category": "git",
    "difficulty": "advanced",
    "tags": [
      "worktree",
      "workflow"
    ],
    "question": "What is git worktree?",
    "answer": "<p><code class=\"inline\">git worktree</code> checks out multiple branches into <strong>separate directories</strong> backed by a single repository (shared <code class=\"inline\">.git</code>). You can work on an urgent hotfix in one folder without stashing or disturbing your half-done feature in another.</p>",
    "tip": "Worktrees beat stashing when you need to build/run two branches at once — each has its own working files and index.",
    "code": "git worktree add ../hotfix main\n# fix, commit, then:\ngit worktree remove ../hotfix",
    "lang": "bash"
  },
  {
    "id": "git-internals",
    "category": "git",
    "difficulty": "advanced",
    "tags": [
      "internals",
      "objects",
      "model"
    ],
    "question": "Explain Git's object model.",
    "answer": "<p>Git is a content-addressable store of four object types, each keyed by a hash of its content:</p><ul><li><strong>blob</strong> — file contents.</li><li><strong>tree</strong> — a directory listing (names → blobs/trees).</li><li><strong>commit</strong> — a snapshot: root tree + parent commit(s) + author/message.</li><li><strong>tag</strong> — an annotated pointer to a commit.</li></ul><p><strong>Refs</strong> (branches) are just movable pointers to commit hashes.</p>",
    "tip": "The killer insight: a branch is merely a pointer to a commit — that's why creating/switching branches is instant and cheap.",
    "code": "git cat-file -t HEAD    # commit\ngit cat-file -p HEAD    # inspect the object graph",
    "lang": "bash"
  }
]
