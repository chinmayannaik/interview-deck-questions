[
  {
    "id": "kafka-what-is",
    "category": "kafka",
    "difficulty": "beginner",
    "tags": [
      "basics",
      "messaging"
    ],
    "question": "What is Apache Kafka and what problems does it solve?",
    "answer": "<p>Kafka is a <strong>distributed event streaming platform</strong> — a high-throughput, fault-tolerant, durable log for publishing and subscribing to streams of events. Use cases:</p><ul><li><strong>Event streaming</strong> — real-time data pipelines between services.</li><li><strong>Message queuing</strong> — decouple producers and consumers.</li><li><strong>Event sourcing / CQRS</strong> — durable log of all state changes.</li><li><strong>Log aggregation</strong> — collect logs/metrics from many sources.</li></ul><p>Unlike traditional message brokers, Kafka <strong>retains</strong> messages (configurable retention) and allows consumers to replay history.</p>",
    "tip": "Kafka is a log, not a queue — messages are persisted and can be re-read by design.",
    "code": "",
    "lang": "",
    "revise": "**Kafka** = a distributed event-streaming platform — a high-throughput, fault-tolerant, DURABLE log for publishing/subscribing to event streams.\n• Uses: real-time data pipelines, message queuing, event sourcing/CQRS, log aggregation.\nUnlike a classic queue, messages are RETAINED and re-readable."
  },
  {
    "id": "kafka-core-concepts",
    "category": "kafka",
    "difficulty": "beginner",
    "tags": [
      "topics",
      "partitions",
      "brokers"
    ],
    "question": "Explain topics, partitions, and brokers.",
    "answer": "<ul><li><strong>Broker</strong> — a Kafka server that stores data and serves clients. A cluster has multiple brokers for fault tolerance.</li><li><strong>Topic</strong> — a named category/feed of messages (like a table in a DB).</li><li><strong>Partition</strong> — a topic is split into partitions for parallelism. Each partition is an ordered, immutable sequence of messages.</li><li><strong>Offset</strong> — a unique sequential ID for each message within a partition.</li></ul><p>More partitions = more parallelism (more consumers can read concurrently).</p>",
    "tip": "Message ordering is guaranteed only within a single partition — use a partition key to keep related events together.",
    "code": "",
    "lang": "",
    "deep": "<h4>Why partitions matter</h4><p>A topic with 6 partitions can be consumed by up to 6 consumers in the same group in parallel. Each partition is replicated across brokers for durability.</p><h4>Partition key</h4><pre><code>producer.send(new ProducerRecord<>(\"orders\", orderId, event));\n// same orderId -> same partition -> ordered per order</code></pre><h4>How to say it in an interview</h4><p>“A topic is a logical stream split into partitions for scale. Each partition is an append-only log on a broker. I choose partition count based on target throughput and consumer parallelism, and use partition keys to preserve ordering for related events.”</p>",
    "revise": "• **Broker** — a Kafka server storing data (a cluster has several for fault tolerance).\n• **Topic** — a named feed of messages.\n• **Partition** — a topic is split into ordered, immutable partitions for PARALLELISM.\n• **Offset** — a message's sequential position within a partition."
  },
  {
    "id": "kafka-producer",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "producer",
      "publishing"
    ],
    "question": "How does a Kafka producer work?",
    "answer": "<p>The producer sends records to a topic. Key config:</p><ul><li><strong>key</strong> — determines partition (hash of key % num partitions). Same key → same partition.</li><li><strong>acks</strong> — durability guarantee: <code class=\"inline\">0</code> (fire-and-forget), <code class=\"inline\">1</code> (leader ack), <code class=\"inline\">all</code> (all ISR replicas ack).</li><li><strong>retries</strong> — automatic retry on transient failures.</li><li><strong>idempotence</strong> — exactly-once per partition (no duplicates on retry).</li></ul><p>Producer batches messages for efficiency and compresses (snappy, lz4, zstd).</p>",
    "tip": "acks=all is the production default when durability matters — acks=1 risks data loss if leader dies before replication.",
    "code": "Properties props = new Properties();\nprops.put(\"acks\", \"all\");\nprops.put(\"enable.idempotence\", true);\nProducer<String, Order> producer = new KafkaProducer<>(props);\nproducer.send(new ProducerRecord<>(\"orders\", orderId, order));",
    "lang": "java",
    "revise": "A producer sends records to a topic. Key config:\n• **key** — decides the partition (`hash(key) % partitions`); same key → same partition (ordering).\n• **acks** — durability: `0` (fire-and-forget), `1` (leader), `all` (all in-sync replicas).\n• **retries** + **idempotence** — exactly-once per partition (no duplicates on retry)."
  },
  {
    "id": "kafka-consumer-groups",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "consumer",
      "consumer-group"
    ],
    "question": "What is a consumer group?",
    "answer": "<p>A <strong>consumer group</strong> is a set of consumers that jointly consume a topic. Kafka assigns each partition to <strong>exactly one consumer</strong> in the group — enabling parallel consumption while avoiding duplicate processing within the group.</p><p>Rules:</p><ul><li>Consumers in the <em>same</em> group share the work (load balancing).</li><li>Consumers in <em>different</em> groups each get all messages (pub/sub).</li><li>Max useful consumers per group = number of partitions.</li></ul>",
    "tip": "If you have 10 partitions and 15 consumers in a group, 5 consumers sit idle.",
    "code": "props.put(\"group.id\", \"order-processor\");\nprops.put(\"auto.offset.reset\", \"earliest\");\nKafkaConsumer<String, Order> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(List.of(\"orders\"));",
    "lang": "java",
    "revise": "A **consumer group** jointly consumes a topic.\n• Each partition goes to EXACTLY ONE consumer in the group → parallel, no duplicate processing within the group.\n• Different groups each get ALL messages independently.\nMax useful consumers per group = partition count."
  },
  {
    "id": "kafka-offsets",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "offsets",
      "commit"
    ],
    "question": "What are offsets and how are they committed?",
    "answer": "<p>An <strong>offset</strong> is the consumer's position in a partition log. Committing an offset tells Kafka \"I've processed up to here.\"</p><ul><li><strong>Auto-commit</strong> — periodic background commit (<code class=\"inline\">enable.auto.commit=true</code>). Risk: message processed but crash before commit → reprocessed; commit before processing → lost on crash.</li><li><strong>Manual commit</strong> — commit after successful processing. <code class=\"inline\">commitSync()</code> (blocking) or <code class=\"inline\">commitAsync()</code>.</li></ul><p>Offsets are stored in the internal <code class=\"inline\">__consumer_offsets</code> topic.</p>",
    "tip": "Production consumers should manually commit after processing — auto-commit can cause duplicates or data loss.",
    "code": "while (true) {\n  ConsumerRecords<String, Order> records = consumer.poll(Duration.ofMillis(100));\n  for (var record : records) {\n    process(record.value());\n  }\n  consumer.commitSync();  // after batch processed\n}",
    "lang": "java",
    "revise": "An **offset** is the consumer's position in a partition; committing says 'processed up to here'.\n• **Auto-commit** — periodic; risks reprocessing (crash after processing, before commit) or loss (commit before processing).\n• **Manual commit** AFTER successful processing → at-least-once delivery."
  },
  {
    "id": "kafka-replication",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "replication",
      "isr",
      "durability"
    ],
    "question": "How does Kafka replication work?",
    "answer": "<p>Each partition has one <strong>leader</strong> and zero or more <strong>followers</strong> (replicas). Producers and consumers talk to the leader; followers replicate data.</p><p><strong>ISR (In-Sync Replicas)</strong> — followers caught up with the leader. <code class=\"inline\">acks=all</code> waits for all ISR replicas. If a leader dies, a follower from ISR is elected as the new leader.</p><p>Set <code class=\"inline\">replication.factor=3</code> in production for fault tolerance.</p>",
    "tip": "min.insync.replicas=2 with replication.factor=3 means you can lose one broker without losing writes.",
    "code": "",
    "lang": "",
    "revise": "Each partition has one **leader** + follower replicas.\n• Producers/consumers talk to the leader; followers replicate.\n• **ISR** (in-sync replicas) are caught up; `acks=all` waits for all ISR.\n• Leader dies → an ISR follower is elected. Use `replication.factor=3` in prod."
  },
  {
    "id": "kafka-delivery-semantics",
    "category": "kafka",
    "difficulty": "advanced",
    "tags": [
      "delivery",
      "exactly-once",
      "semantics"
    ],
    "question": "At-most-once, at-least-once, and exactly-once semantics.",
    "answer": "<table><tr><th>Semantic</th><th>How</th><th>Tradeoff</th></tr><tr><td><strong>At-most-once</strong></td><td>Commit offset before processing</td><td>Messages may be lost on crash</td></tr><tr><td><strong>At-least-once</strong></td><td>Commit offset after processing</td><td>Messages may be reprocessed (duplicates)</td></tr><tr><td><strong>Exactly-once</strong></td><td>Idempotent producer + transactional consumer</td><td>Higher latency, more complex</td></tr></table><p>Most apps use at-least-once + <strong>idempotent consumers</strong> (deduplicate by message ID).</p>",
    "tip": "True exactly-once end-to-end requires transactional writes to external systems too — Kafka EOS only covers Kafka-to-Kafka.",
    "code": "props.put(\"enable.idempotence\", true);\nprops.put(\"transactional.id\", \"order-tx\");\nproducer.initTransactions();",
    "lang": "java",
    "deep": "<h4>Idempotent consumer pattern</h4><p>Store processed message IDs in a DB. On redelivery, check if already processed — skip if yes. Simpler than Kafka transactions for most apps.</p><h4>How to say it in an interview</h4><p>“I default to at-least-once with idempotent processing — consumers track processed IDs or use upserts. Exactly-once Kafka transactions are for Kafka Streams pipelines where duplicate side effects are unacceptable.”</p>",
    "revise": "• **At-most-once** — commit BEFORE processing; messages may be LOST.\n• **At-least-once** — commit AFTER processing; messages may be DUPLICATED.\n• **Exactly-once** — idempotent producer + transactions; higher latency/complexity.\nMost apps use at-least-once + idempotent consumers (dedupe by message id)."
  },
  {
    "id": "kafka-vs-rabbitmq",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "comparison",
      "rabbitmq"
    ],
    "question": "Kafka vs RabbitMQ — when to use each?",
    "answer": "<table><tr><th></th><th>Kafka</th><th>RabbitMQ</th></tr><tr><td><strong>Model</strong></td><td>Distributed commit log</td><td>Traditional message broker (AMQP)</td></tr><tr><td><strong>Throughput</strong></td><td>Very high (millions/sec)</td><td>Moderate (tens of thousands/sec)</td></tr><tr><td><strong>Message retention</strong></td><td>Configurable (days/forever)</td><td>Deleted after ack</td></tr><tr><td><strong>Replay</strong></td><td>Yes — re-read from any offset</td><td>No — once consumed, gone</td></tr><tr><td><strong>Routing</strong></td><td>Topic + partition key</td><td>Rich (exchanges, bindings, routing keys)</td></tr><tr><td><strong>Best for</strong></td><td>Event streaming, analytics, log pipelines</td><td>Task queues, RPC, complex routing</td></tr></table>",
    "tip": "Need replay and high throughput? Kafka. Need flexible routing and per-message ack? RabbitMQ.",
    "code": "",
    "lang": "",
    "revise": "• **Kafka** — a distributed COMMIT LOG; very high throughput; configurable retention; REPLAY from any offset; ordering per partition.\n• **RabbitMQ** — a traditional broker (AMQP); rich routing (exchanges/bindings); messages deleted after ack; no replay.\nKafka for streaming/event pipelines; RabbitMQ for complex routing / task queues."
  },
  {
    "id": "kafka-ordering",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "ordering",
      "partitions"
    ],
    "question": "How do you guarantee message ordering in Kafka?",
    "answer": "<p>Ordering is guaranteed <strong>only within a single partition</strong>. To preserve order for related events (e.g. all events for order #123):</p><ul><li>Use the same <strong>partition key</strong> (order ID) — hash routes to the same partition.</li><li>Use a <strong>single partition</strong> topic if global ordering is required (limits throughput).</li><li>Don't increase partitions without understanding the ordering impact.</li></ul>",
    "tip": "Reprocessing after a consumer rebalance can temporarily break ordering — design consumers to handle out-of-order gracefully when possible.",
    "code": "producer.send(new ProducerRecord<>(\"orders\", order.getId(), event));\n// all events for same order ID land in same partition",
    "lang": "java",
    "revise": "Ordering is guaranteed ONLY within a single partition.\n• To keep related events ordered, use the SAME partition key (e.g. order id) → same partition.\n• Global ordering needs a single-partition topic (limits throughput).\nChanging partition count breaks key→partition mapping."
  },
  {
    "id": "kafka-retention",
    "category": "kafka",
    "difficulty": "beginner",
    "tags": [
      "retention",
      "storage"
    ],
    "question": "How does Kafka retain messages?",
    "answer": "<p>Messages are stored on disk (sequential writes — very fast). Retention controlled by:</p><ul><li><strong>retention.ms</strong> — time-based (default 7 days).</li><li><strong>retention.bytes</strong> — size-based per partition.</li><li><strong>log compaction</strong> — keeps only the latest value per key (like a changelog/topic).</li></ul><p>Consumers track their own offset — slow consumers don't block others. But if a consumer falls behind beyond retention, data is lost for that consumer.</p>",
    "tip": "Compacted topics (__consumer_offsets, schema registry) use log compaction — only latest key value kept.",
    "code": "",
    "lang": "",
    "revise": "Messages are stored on disk (fast sequential writes) and retained by:\n• `retention.ms` — time-based (default 7 days).\n• `retention.bytes` — size per partition.\n• **Log compaction** — keep only the latest value per key.\nConsumers track their OWN offset — slow consumers don't block others."
  },
  {
    "id": "kafka-rebalancing",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "rebalance",
      "consumer-group"
    ],
    "question": "What is a consumer group rebalance?",
    "answer": "<p>When consumers join or leave a group (startup, shutdown, crash, timeout), Kafka <strong>reassigns partitions</strong> among remaining consumers. During rebalance, consumption pauses — a \"stop-the-world\" event.</p><p>Triggers: new consumer joins, consumer misses heartbeat (<code class=\"inline\">session.timeout.ms</code>), partition count changes.</p><p>Mitigate: cooperative rebalancing (incremental), static group membership, keep <code class=\"inline\">max.poll.interval.ms</code> above processing time.</p>",
    "tip": "Long processing in poll loop causes rebalance storms — process quickly or pause consumption during heavy work.",
    "code": "",
    "lang": "",
    "revise": "A **rebalance** reassigns partitions when consumers join/leave (startup, crash, missed heartbeat).\n• Consumption PAUSES during it — a stop-the-world event.\n• Triggers: new consumer, missed `session.timeout.ms`, partition change.\nMitigate with cooperative (incremental) rebalancing + static membership."
  },
  {
    "id": "kafka-schema-registry",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "schema-registry",
      "avro",
      "serialization"
    ],
    "question": "What is the Schema Registry?",
    "answer": "<p>A separate service (Confluent) that stores <strong>schemas</strong> for Kafka message keys and values. Supports Avro, JSON Schema, and Protobuf.</p><p>Benefits:</p><ul><li><strong>Schema evolution</strong> — backward/forward compatibility rules.</li><li><strong>Compact serialization</strong> — Avro stores schema ID in the message, not the full schema.</li><li><strong>Contract enforcement</strong> — producers can't publish incompatible data.</li></ul>",
    "tip": "Without Schema Registry, schema changes can silently break consumers — it's essential for production Avro/Protobuf usage.",
    "code": "",
    "lang": "",
    "revise": "The **Schema Registry** (Confluent) stores schemas for message keys/values (Avro, JSON Schema, Protobuf).\n• Enforces schema EVOLUTION rules (backward/forward compatibility).\n• Compact serialization — the message carries a schema ID, not the full schema.\nPrevents producers publishing incompatible data."
  },
  {
    "id": "kafka-connect",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "kafka-connect",
      "integration"
    ],
    "question": "What is Kafka Connect?",
    "answer": "<p>A framework for <strong>streaming data between Kafka and external systems</strong> without writing custom producer/consumer code. Uses connectors:</p><ul><li><strong>Source connectors</strong> — ingest from DB (Debezium CDC), files, APIs into Kafka topics.</li><li><strong>Sink connectors</strong> — export from Kafka to Elasticsearch, S3, JDBC, etc.</li></ul><p>Runs as a distributed, scalable service with fault tolerance and offset management built in.</p>",
    "tip": "Debezium (CDC connector) is the go-to for capturing database changes as Kafka events.",
    "code": "",
    "lang": "",
    "revise": "**Kafka Connect** streams data between Kafka and external systems WITHOUT custom code, via connectors:\n• **Source** — ingest from DBs (Debezium CDC), files, APIs into topics.\n• **Sink** — export from topics to Elasticsearch, S3, JDBC.\nRuns as a distributed, fault-tolerant service."
  },
  {
    "id": "kafka-streams",
    "category": "kafka",
    "difficulty": "advanced",
    "tags": [
      "kafka-streams",
      "processing"
    ],
    "question": "What is Kafka Streams?",
    "answer": "<p>A <strong>client library</strong> (not a separate cluster) for building real-time stream processing applications. Provides:</p><ul><li>map, filter, groupBy, aggregate, join operations on streams.</li><li>Stateful processing with local RocksDB stores.</li><li>Exactly-once semantics within the Kafka ecosystem.</li><li>Windowing (tumbling, hopping, session windows).</li></ul><p>Alternative: Apache Flink or ksqlDB for more complex processing at scale.</p>",
    "tip": "Kafka Streams apps are just Java apps — deploy as microservices, scale by adding instances.",
    "code": "StreamsBuilder builder = new StreamsBuilder();\nbuilder.stream(\"orders\")\n  .groupByKey()\n  .count()\n  .toStream()\n  .to(\"order-counts\");",
    "lang": "java",
    "revise": "**Kafka Streams** is a client LIBRARY (not a cluster) for real-time stream processing.\n• Operations: `map`, `filter`, `groupBy`, `aggregate`, joins.\n• Stateful processing with local RocksDB stores; windowing; exactly-once within Kafka.\nAlternatives: Apache Flink / Spark Streaming."
  },
  {
    "id": "kafka-kraft",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "kraft",
      "zookeeper"
    ],
    "question": "KRaft vs ZooKeeper — what changed?",
    "answer": "<p>Historically, Kafka depended on <strong>Apache ZooKeeper</strong> for cluster metadata, leader election, and controller management. <strong>KRaft</strong> (Kafka Raft) mode removes this dependency — Kafka manages its own metadata quorum using the Raft consensus protocol.</p><p>Benefits: simpler deployment (one less system), faster metadata operations, better scalability for large partition counts. KRaft is the default in Kafka 3.3+.</p>",
    "tip": "New deployments should use KRaft — ZooKeeper is deprecated and will be removed.",
    "code": "",
    "lang": "",
    "revise": "• **ZooKeeper** — historically managed Kafka's metadata, leader election, and controller.\n• **KRaft** (Kafka Raft) removes that dependency — Kafka manages its own metadata quorum via Raft.\nBenefits: one less system, faster metadata ops, better scalability. The modern default."
  },
  {
    "id": "kafka-monitoring",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "monitoring",
      "operations"
    ],
    "question": "Key Kafka metrics to monitor.",
    "answer": "<ul><li><strong>Consumer lag</strong> — how far behind consumers are. High lag = processing bottleneck or consumer failure.</li><li><strong>Under-replicated partitions</strong> — replicas falling behind leader. Risk of data loss.</li><li><strong>Request rate / latency</strong> — broker throughput and response times.</li><li><strong>Active controller count</strong> — should always be 1.</li><li><strong>Offline partitions</strong> — partitions with no leader. Critical alert.</li></ul><p>Tools: Prometheus + Grafana, Confluent Control Center, LinkedIn Burrow for lag monitoring.</p>",
    "tip": "Consumer lag is the single most important metric — alert on it before consumers fall past retention.",
    "code": "",
    "lang": "",
    "revise": "Key metrics:\n• **Consumer lag** — how far behind consumers are (bottleneck/failure).\n• **Under-replicated partitions** — replicas behind the leader (data-loss risk).\n• **Request rate/latency**, **active controller count** (should be 1), **offline partitions** (critical)."
  },
  {
    "id": "kafka-dead-letter",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "dead-letter",
      "error-handling"
    ],
    "question": "How do you handle poison messages in Kafka?",
    "answer": "<p>A <strong>poison message</strong> repeatedly fails processing and blocks the partition. Patterns:</p><ul><li><strong>Dead Letter Topic (DLT)</strong> — after N retries, publish the message to a separate topic for manual inspection.</li><li><strong>Skip and commit</strong> — log the error, commit offset, move on (data loss risk).</li><li><strong>Quarantine</strong> — store in a DB with error details for replay after fix.</li></ul><p>Spring Kafka and Kafka Connect have built-in DLT support.</p>",
    "tip": "Always include the original message, error reason, and timestamp in the DLT for debugging.",
    "code": "",
    "lang": "",
    "revise": "A **poison message** repeatedly fails and blocks its partition. Patterns:\n• **Dead Letter Topic** — after N retries, publish it to a separate topic for inspection.\n• **Skip and commit** — log + move on (data-loss risk).\n• **Quarantine** in a DB for later replay.\nSpring Kafka's `DefaultErrorHandler` + `@RetryableTopic` automate this."
  },
  {
    "id": "kafka-spring",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "spring-kafka",
      "integration"
    ],
    "question": "Using Kafka with Spring Boot.",
    "answer": "<p><code class=\"inline\">spring-kafka</code> provides:</p><ul><li><code class=\"inline\">@KafkaListener(topics = \"orders\", groupId = \"processor\")</code> — declarative consumer.</li><li><code class=\"inline\">KafkaTemplate</code> — send messages programmatically.</li><li>Auto-configuration from <code class=\"inline\">spring.kafka.*</code> properties.</li><li>Error handling with <code class=\"inline\">DefaultErrorHandler</code> and dead letter topics.</li><li><code class=\"inline\">@RetryableTopic</code> — automatic retry with backoff to DLT.</li></ul>",
    "tip": "Configure concurrency on @KafkaListener to match partition count for max parallelism.",
    "code": "@KafkaListener(topics = \"orders\", groupId = \"processor\")\npublic void handle(Order order) {\n  orderService.process(order);\n}",
    "lang": "java",
    "revise": "`spring-kafka` provides:\n• `@KafkaListener(topics, groupId)` — declarative consumer.\n• `KafkaTemplate` — send messages.\n• Auto-config from `spring.kafka.*`.\n• Error handling via `DefaultErrorHandler` + DLT; `@RetryableTopic` for retry-with-backoff."
  },
  {
    "id": "kafka-compacted-topics",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "compaction",
      "changelog"
    ],
    "question": "What are compacted topics?",
    "answer": "<p>Log compaction retains only the <strong>latest value for each key</strong> — older values with the same key are eventually removed. The topic acts as a <strong>changelog</strong> (key → latest state).</p><p>Use cases: user profile updates, config changes, CDC snapshots. Combined with retention.ms for time + key-based cleanup.</p>",
    "tip": "Messages with null key are never compacted — they're deleted after retention period.",
    "code": "",
    "lang": "",
    "revise": "**Log compaction** keeps only the LATEST value per key (older same-key values are removed).\n• The topic becomes a changelog (key → latest state).\n• Uses: profile/config updates, CDC snapshots.\nCombine with `retention.ms` for time + key-based cleanup."
  },
  {
    "id": "kafka-partition-strategy",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "partitions",
      "design"
    ],
    "question": "How do you choose the number of partitions?",
    "answer": "<p>Consider:</p><ul><li><strong>Target throughput</strong> — more partitions = more parallel consumers and producers.</li><li><strong>Consumer parallelism</strong> — max consumers per group = partition count.</li><li><strong>Ordering needs</strong> — fewer partitions = easier ordering.</li><li><strong>Broker resources</strong> — each partition has overhead (files, memory).</li></ul><p>Rule of thumb: start with <code class=\"inline\"># of consumers you need × 2</code>. Increasing partitions later is possible but can break key-based ordering if keys hash differently.</p>",
    "tip": "You can increase partitions but never decrease — plan ahead.",
    "code": "",
    "lang": "",
    "revise": "Choosing partition count balances:\n• **Throughput / parallelism** — more partitions = more parallel producers/consumers (max consumers per group = partitions).\n• **Ordering** — fewer partitions is easier.\n• **Broker overhead** — each partition costs files/memory.\nRule of thumb: start at (consumers needed × 2); increasing partitions breaks key ordering."
  },
  {
    "id": "kafka-idempotent-producer",
    "category": "kafka",
    "difficulty": "intermediate",
    "tags": [
      "idempotence",
      "producer"
    ],
    "question": "What is an idempotent producer?",
    "answer": "<p>With <code class=\"inline\">enable.idempotence=true</code>, the producer assigns a PID (Producer ID) and sequence numbers per partition. If a batch is retried due to network error, the broker deduplicates using the sequence number — preventing duplicate messages within a session.</p><p>Required settings: <code class=\"inline\">acks=all</code>, <code class=\"inline\">retries &gt; 0</code>, <code class=\"inline\">max.in.flight.requests.per.connection ≤ 5</code>.</p>",
    "tip": "Enable idempotence by default in production — the overhead is negligible.",
    "code": "props.put(\"enable.idempotence\", true);",
    "lang": "java",
    "revise": "`enable.idempotence=true` gives the producer a **PID** + per-partition sequence numbers.\n• On a retry, the broker DEDUPLICATES by sequence number → no duplicates within a session.\n• Requires `acks=all`, `retries > 0`, `max.in.flight ≤ 5`.\nThe basis of exactly-once."
  },
  {
    "id": "kafka-event-sourcing",
    "category": "kafka",
    "difficulty": "advanced",
    "tags": [
      "event-sourcing",
      "cqrs"
    ],
    "question": "How is Kafka used in event sourcing?",
    "answer": "<p><strong>Event sourcing</strong> — store state changes as a sequence of events, not current state. Kafka's durable log is a natural event store:</p><ol><li>Commands produce events to a topic.</li><li>Events are the source of truth.</li><li>Current state is derived by replaying events (or reading from a materialised view).</li></ol><p><strong>CQRS</strong> — separate write (command) and read (query) models, connected via Kafka events.</p>",
    "tip": "Event sourcing + Kafka gives audit trail and replay for free — but adds complexity to read models.",
    "code": "",
    "lang": "",
    "revise": "**Event sourcing** stores state changes as a sequence of EVENTS, not current state — Kafka's durable log is a natural event store.\n• Commands produce events (the source of truth); current state is derived by REPLAYING them.\n• Pairs with **CQRS** — separate write (command) and read (query) models."
  }
]
