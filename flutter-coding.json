[
  {
    "id": "fcoding-counter-provider",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "provider",
      "changenotifier",
      "state-management"
    ],
    "question": "Build a counter app using Provider and ChangeNotifier.",
    "answer": "<p>Create a <code class=\"inline\">ChangeNotifier</code> model that holds the count and calls <code class=\"inline\">notifyListeners()</code> on change. Expose it via <code class=\"inline\">ChangeNotifierProvider</code> at the top of the tree, and read/update it in widgets with <code class=\"inline\">context.watch&lt;CounterModel&gt;()</code> (rebuilds) or <code class=\"inline\">context.read&lt;CounterModel&gt;()</code> (fire-and-forget calls).</p>",
    "tip": "Use context.read inside onPressed handlers and context.watch inside build—mixing them up is a common interview slip.",
    "code": "class CounterModel extends ChangeNotifier {\n  int _count = 0;\n  int get count => _count;\n  void increment() { _count++; notifyListeners(); }\n}\n\n// UI\nText('${context.watch<CounterModel>().count}');\nElevatedButton(\n  onPressed: () => context.read<CounterModel>().increment(),\n  child: Text('+'),\n);",
    "lang": "dart",
    "revise": "**Provider counter**: a `ChangeNotifier` holds the count + calls `notifyListeners()`; expose via `ChangeNotifierProvider`.\n• Read with `context.watch<T>()` (rebuilds) or `context.read<T>()` (fire-and-forget).\n• **Gotcha**: use **`read` in `onPressed`**, **`watch` in `build`** — mixing them up is the classic bug."
  },
  {
    "id": "fcoding-todo-crud",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "crud",
      "statefulwidget",
      "todo"
    ],
    "question": "Build a simple todo list with add, toggle-complete, and delete.",
    "answer": "<p>Keep a <code class=\"inline\">List&lt;Todo&gt;</code> in a <code class=\"inline\">StatefulWidget</code>. Adding/removing/toggling all call <code class=\"inline\">setState</code> to mutate the list and trigger a rebuild. Render with <code class=\"inline\">ListView.builder</code> and use each item's id as a <code class=\"inline\">ValueKey</code> so Flutter tracks identity correctly across reorders/deletes.</p>",
    "tip": "Always give list items a stable key when the list can be reordered or filtered—prevents state 'jumping' between items.",
    "code": "List<Todo> todos = [];\n\nvoid add(String title) => setState(() => todos.add(Todo(title)));\nvoid toggle(Todo t) => setState(() => t.done = !t.done);\nvoid remove(Todo t) => setState(() => todos.remove(t));",
    "lang": "dart",
    "revise": "**Todo CRUD**: keep a `List<Todo>` in state; add/toggle/delete mutate it inside `setState`; render with `ListView.builder`.\n• **Gotcha**: give each item a **stable `ValueKey(id)`** so state doesn't 'jump' between rows on reorder/delete."
  },
  {
    "id": "fcoding-debounced-search",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "debounce",
      "search",
      "timer"
    ],
    "question": "Implement a debounced search field in Flutter.",
    "answer": "<p>Listen to the <code class=\"inline\">TextEditingController</code>, cancel any pending <code class=\"inline\">Timer</code> on each keystroke, and start a new short-delay timer that fires the actual API call—so a network request only fires after the user pauses typing.</p>",
    "tip": "This is the imperative Timer-based equivalent of RxJS's debounceTime—know both approaches (Timer vs rxdart's debounce).",
    "code": "Timer? _debounce;\n\nvoid onChanged(String query) {\n  _debounce?.cancel();\n  _debounce = Timer(const Duration(milliseconds: 400), () {\n    searchApi(query);\n  });\n}\n\n@override\nvoid dispose() {\n  _debounce?.cancel();\n  super.dispose();\n}",
    "lang": "dart",
    "revise": "**Debounced search**: on each keystroke, **cancel the pending `Timer`** and start a new short-delay one that fires the API — so it only calls after the user pauses.\n• **Gotcha**: this is the Timer equivalent of RxJS `debounceTime` — know both (Timer vs rxdart)."
  },
  {
    "id": "fcoding-infinite-scroll",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "infinite-scroll",
      "scrollcontroller",
      "pagination"
    ],
    "question": "Implement infinite scroll pagination in a ListView.",
    "answer": "<p>Attach a <code class=\"inline\">ScrollController</code> to the <code class=\"inline\">ListView</code> and listen for the scroll position nearing <code class=\"inline\">maxScrollExtent</code>; when close enough, fetch the next page and append it to the data list with <code class=\"inline\">setState</code>. Guard with an <code class=\"inline\">isLoading</code> flag to avoid firing duplicate requests.</p>",
    "tip": "Always dispose the ScrollController and guard against calling setState after the widget is disposed (check 'mounted').",
    "code": "final controller = ScrollController();\nbool isLoading = false;\n\n@override\nvoid initState() {\n  super.initState();\n  controller.addListener(() {\n    if (controller.position.pixels >=\n        controller.position.maxScrollExtent - 200 && !isLoading) {\n      loadMore();\n    }\n  });\n}",
    "lang": "dart",
    "revise": "**Infinite scroll**: a `ScrollController` listens for position near `maxScrollExtent`, then fetches the next page and appends via `setState`.\n• **Gotcha**: guard with an **`isLoading` flag** (no duplicate requests), **dispose the controller**, and check **`mounted`** before `setState`."
  },
  {
    "id": "fcoding-futurebuilder-fetch",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "futurebuilder",
      "api",
      "networking"
    ],
    "question": "Fetch and render API data using FutureBuilder.",
    "answer": "<p>Store the Future in <code class=\"inline\">initState</code> (not in <code class=\"inline\">build</code>, or it re-fetches on every rebuild), then pass it to <code class=\"inline\">FutureBuilder</code> and branch on <code class=\"inline\">snapshot.connectionState</code> and <code class=\"inline\">snapshot.hasError</code> to render loading, error, or data states.</p>",
    "tip": "Creating the Future inside build() is a classic bug—it restarts the network call on every rebuild.",
    "code": "late Future<List<User>> usersFuture;\n\n@override\nvoid initState() {\n  super.initState();\n  usersFuture = api.fetchUsers();\n}\n\nFutureBuilder<List<User>>(\n  future: usersFuture,\n  builder: (context, snapshot) {\n    if (snapshot.connectionState != ConnectionState.done) {\n      return const CircularProgressIndicator();\n    }\n    if (snapshot.hasError) return Text('Error: ${snapshot.error}');\n    return ListView(children: [for (final u in snapshot.data!) Text(u.name)]);\n  },\n);",
    "lang": "dart",
    "revise": "**FutureBuilder fetch**: store the `Future` in **`initState`** (not `build`), then branch on `snapshot.connectionState`/`hasError` for loading/error/data.\n• **Gotcha**: creating the Future **inside `build()`** restarts the network call on **every rebuild** — the classic bug."
  },
  {
    "id": "fcoding-streambuilder-timer",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "streambuilder",
      "timer",
      "countdown"
    ],
    "question": "Build a live countdown timer using Stream and StreamBuilder.",
    "answer": "<p>Use <code class=\"inline\">Stream.periodic</code> to emit a decreasing value every second, <code class=\"inline\">take()</code> to limit it, and feed it to a <code class=\"inline\">StreamBuilder</code> so the UI updates automatically each tick without manual <code class=\"inline\">setState</code> calls.</p>",
    "tip": "Stream.periodic starts emitting immediately on subscription—use .map or an initial value if you need to show the starting count first.",
    "code": "Stream<int> countdown(int from) => Stream.periodic(\n  const Duration(seconds: 1), (i) => from - i - 1,\n).take(from);\n\nStreamBuilder<int>(\n  stream: countdown(10),\n  builder: (context, snapshot) => Text('${snapshot.data ?? 10}'),\n);",
    "lang": "dart",
    "revise": "**Live countdown**: `Stream.periodic` emits a decreasing value each second, `.take()` limits it, `StreamBuilder` renders each tick — no manual `setState`.\n• **Gotcha**: `Stream.periodic` emits **after** the first interval — use `.map`/an initial value to show the start immediately."
  },
  {
    "id": "fcoding-form-login",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "forms",
      "validation",
      "login"
    ],
    "question": "Build a login form with email/password validation.",
    "answer": "<p>Wrap fields in a <code class=\"inline\">Form</code> with a <code class=\"inline\">GlobalKey&lt;FormState&gt;</code>. Each <code class=\"inline\">TextFormField</code> validates its own input (email format, min password length); on submit, call <code class=\"inline\">formKey.currentState!.validate()</code> and only proceed if it returns true.</p>",
    "tip": "Use autovalidateMode: AutovalidateMode.onUserInteraction so errors appear as the user types, not only on submit.",
    "code": "final formKey = GlobalKey<FormState>();\n\nForm(\n  key: formKey,\n  child: Column(children: [\n    TextFormField(\n      validator: (v) => v != null && v.contains('@') ? null : 'Invalid email',\n    ),\n    TextFormField(\n      obscureText: true,\n      validator: (v) => (v?.length ?? 0) >= 6 ? null : 'Min 6 characters',\n    ),\n  ]),\n);",
    "lang": "dart",
    "revise": "**Login form**: wrap fields in `Form` + `GlobalKey<FormState>`; each `TextFormField` validates itself; on submit call `formKey.currentState!.validate()`.\n• **Gotcha**: set **`autovalidateMode: onUserInteraction`** so errors show as the user types, not only on submit."
  },
  {
    "id": "fcoding-custom-reusable-widget",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "custom-widget",
      "reusable-component"
    ],
    "question": "Build a reusable custom button widget with a loading state.",
    "answer": "<p>Wrap the design in a <code class=\"inline\">StatelessWidget</code> exposing <code class=\"inline\">label</code>, <code class=\"inline\">onPressed</code>, and <code class=\"inline\">isLoading</code> as parameters. When <code class=\"inline\">isLoading</code> is true, disable the button (pass <code class=\"inline\">null</code> as the callback) and swap the label for a small <code class=\"inline\">CircularProgressIndicator</code>.</p>",
    "tip": "Disabling by passing null to onPressed is the idiomatic Flutter way—no need for a separate 'enabled' flag.",
    "code": "class AppButton extends StatelessWidget {\n  final String label;\n  final VoidCallback? onPressed;\n  final bool isLoading;\n  const AppButton({super.key, required this.label, this.onPressed, this.isLoading = false});\n\n  @override\n  Widget build(BuildContext context) => ElevatedButton(\n    onPressed: isLoading ? null : onPressed,\n    child: isLoading\n        ? const SizedBox(width: 16, height: 16, child: CircularProgressIndicator(strokeWidth: 2))\n        : Text(label),\n  );\n}",
    "lang": "dart",
    "revise": "**Reusable button**: a `StatelessWidget` with `label`/`onPressed`/`isLoading` params; when loading, swap the label for a `CircularProgressIndicator`.\n• **Gotcha**: disable by passing **`null` to `onPressed`** — the idiomatic way, no separate `enabled` flag."
  },
  {
    "id": "fcoding-bottom-nav-indexedstack",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "bottom-navigation",
      "indexedstack"
    ],
    "question": "Build a bottom navigation bar that preserves each tab's state.",
    "answer": "<p>Use <code class=\"inline\">BottomNavigationBar</code> to switch a selected index, but render all tab pages inside an <code class=\"inline\">IndexedStack</code> instead of swapping widgets directly—<code class=\"inline\">IndexedStack</code> keeps every child alive in the tree (just hidden), preserving scroll position and state when switching tabs.</p>",
    "tip": "Swapping the body widget directly (e.g. with a switch statement) rebuilds each tab from scratch—IndexedStack is the fix.",
    "code": "int index = 0;\nfinal pages = [HomePage(), SearchPage(), ProfilePage()];\n\nScaffold(\n  body: IndexedStack(index: index, children: pages),\n  bottomNavigationBar: BottomNavigationBar(\n    currentIndex: index,\n    onTap: (i) => setState(() => index = i),\n    items: const [],\n  ),\n);",
    "lang": "dart",
    "revise": "**State-preserving tabs**: switch a selected index but render all pages inside an **`IndexedStack`** (keeps every child alive/hidden), preserving scroll + state.\n• **Gotcha**: swapping the body via a `switch` **rebuilds each tab from scratch** — `IndexedStack` is the fix."
  },
  {
    "id": "fcoding-responsive-layoutbuilder",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "responsive",
      "layoutbuilder"
    ],
    "question": "Build a layout that switches between mobile and tablet designs.",
    "answer": "<p>Use <code class=\"inline\">LayoutBuilder</code> to read the available width from <code class=\"inline\">constraints.maxWidth</code> and branch the widget tree at a breakpoint (e.g. 600px)—rendering a single column on narrow screens and a two-pane layout on wider ones.</p>",
    "tip": "Prefer LayoutBuilder's constraints over MediaQuery's screen size when the widget could be nested inside something narrower than the full screen (e.g. a side panel).",
    "code": "LayoutBuilder(\n  builder: (context, constraints) {\n    if (constraints.maxWidth < 600) {\n      return const MobileLayout();\n    }\n    return const TabletLayout();\n  },\n);",
    "lang": "dart",
    "revise": "**Responsive layout**: `LayoutBuilder` reads `constraints.maxWidth` and branches at a breakpoint (~600px) — one column narrow, two-pane wide.\n• **Gotcha**: prefer `LayoutBuilder`'s **parent constraints** over `MediaQuery`'s screen size for nested/reusable widgets."
  },
  {
    "id": "fcoding-bloc-counter",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "bloc",
      "cubit",
      "state-management"
    ],
    "question": "Implement a counter using the Bloc/Cubit pattern.",
    "answer": "<p>Define a <code class=\"inline\">Cubit&lt;int&gt;</code> exposing <code class=\"inline\">increment()</code>/<code class=\"inline\">decrement()</code> methods that call <code class=\"inline\">emit()</code> with the new state. Provide it with <code class=\"inline\">BlocProvider</code> and rebuild the UI with <code class=\"inline\">BlocBuilder&lt;CounterCubit, int&gt;</code>.</p>",
    "tip": "Cubit state should be immutable—emit a brand-new value/object each time rather than mutating in place.",
    "code": "class CounterCubit extends Cubit<int> {\n  CounterCubit() : super(0);\n  void increment() => emit(state + 1);\n  void decrement() => emit(state - 1);\n}\n\nBlocBuilder<CounterCubit, int>(\n  builder: (context, count) => Text('$count'),\n);",
    "lang": "dart",
    "revise": "**Cubit counter**: `Cubit<int>` with `increment()`/`decrement()` calling `emit(newValue)`; provide via `BlocProvider`, render with `BlocBuilder`.\n• **Gotcha**: Cubit state must be **immutable** — emit a brand-new value/object, never mutate in place."
  },
  {
    "id": "fcoding-dio-interceptor-auth",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "dio",
      "interceptor",
      "auth"
    ],
    "question": "Add an auth-token interceptor with automatic refresh using Dio.",
    "answer": "<p>Register an <code class=\"inline\">InterceptorsWrapper</code> that attaches the current access token to every outgoing request in <code class=\"inline\">onRequest</code>. In <code class=\"inline\">onError</code>, check for a 401, call the refresh-token endpoint, update stored tokens, and retry the original request via <code class=\"inline\">handler.resolve(await dio.fetch(options))</code>.</p>",
    "tip": "Use a separate Dio instance (without the interceptor) for the refresh-token call itself to avoid infinite retry loops.",
    "code": "dio.interceptors.add(InterceptorsWrapper(\n  onRequest: (options, handler) {\n    options.headers['Authorization'] = 'Bearer $accessToken';\n    handler.next(options);\n  },\n  onError: (error, handler) async {\n    if (error.response?.statusCode == 401) {\n      accessToken = await refreshToken();\n      return handler.resolve(await dio.fetch(error.requestOptions));\n    }\n    handler.next(error);\n  },\n));",
    "lang": "dart",
    "revise": "**Auth interceptor + refresh**: `InterceptorsWrapper` attaches the token in `onRequest`; in `onError` on a 401, refresh the token and retry via `handler.resolve(...)`.\n• **Gotcha**: use a **separate Dio instance for the refresh call** to avoid an **infinite retry loop**."
  },
  {
    "id": "fcoding-palindrome-check",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "string",
      "palindrome"
    ],
    "question": "Write a Dart function to check if a string is a palindrome.",
    "answer": "<p>Normalize the string (lowercase, strip non-alphanumeric characters if needed), then compare it to its reverse. This runs in <strong>O(n)</strong> time and O(n) space.</p>",
    "tip": "Ask the interviewer whether to ignore case/spaces/punctuation before coding—shows you clarify requirements.",
    "code": "bool isPalindrome(String input) {\n  final s = input.toLowerCase().replaceAll(RegExp(r'[^a-z0-9]'), '');\n  return s == s.split('').reversed.join();\n}",
    "lang": "dart",
    "revise": "**Palindrome**: normalize (lowercase, strip non-alphanumeric), compare to its reverse — O(n).\n• **Gotcha**: **ask whether to ignore case/spaces/punctuation** before coding — it shows you clarify requirements."
  },
  {
    "id": "fcoding-fibonacci-memo",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "fibonacci",
      "memoization",
      "recursion"
    ],
    "question": "Write a Dart function to compute the nth Fibonacci number, and explain how to optimize the naive recursive version.",
    "answer": "<p>The naive recursive solution recomputes overlapping subproblems, giving <strong>O(2^n)</strong> time. Adding <strong>memoization</strong> (caching already-computed results in a Map) brings it down to <strong>O(n)</strong> time and O(n) space. An iterative version achieves O(n) time with O(1) space.</p>",
    "tip": "Mention Big-O for both the naive and optimized versions—interviewers want to see you recognize overlapping subproblems.",
    "code": "final _cache = <int, int>{};\n\nint fib(int n) {\n  if (n <= 1) return n;\n  return _cache[n] ??= fib(n - 1) + fib(n - 2);\n}",
    "lang": "dart",
    "revise": "**Fibonacci**: naive recursion is **O(2ⁿ)** (overlapping subproblems); **memoize** with a Map → **O(n)**; iterative → O(n) time, **O(1) space**.\n• **Gotcha**: state Big-O for **both** versions — interviewers want you to spot overlapping subproblems."
  },
  {
    "id": "fcoding-flatten-nested-list",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "list",
      "recursion",
      "flatten"
    ],
    "question": "Write a Dart function to flatten an arbitrarily nested list.",
    "answer": "<p>Recursively walk each element: if it's itself a <code class=\"inline\">List</code>, recurse into it and spread the flattened result; otherwise add it directly to the output. This handles any depth of nesting.</p>",
    "tip": "Dart's dynamic/List<dynamic> typing makes this natural—mention how you'd type it more strictly with generics if asked.",
    "code": "List<dynamic> flatten(List<dynamic> input) {\n  final result = <dynamic>[];\n  for (final item in input) {\n    if (item is List) {\n      result.addAll(flatten(item));\n    } else {\n      result.add(item);\n    }\n  }\n  return result;\n}",
    "lang": "dart",
    "revise": "**Flatten nested list**: recursively walk elements — if a `List`, recurse + spread; else add directly. Handles any depth.\n• **Gotcha**: mention you'd **type it with generics** more strictly if asked (Dart's `dynamic` makes the naive version easy)."
  },
  {
    "id": "fcoding-debounce-function",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "algorithm",
      "debounce",
      "closures"
    ],
    "question": "Implement a generic debounce function from scratch in Dart.",
    "answer": "<p>A debounce wraps a callback so repeated calls within a delay window cancel the pending invocation and restart the timer—only the last call within the quiet period actually executes. Implemented with a closure holding a single <code class=\"inline\">Timer?</code> across calls.</p>",
    "tip": "This is the same idea behind fcoding-debounced-search but generalized as a reusable utility, not tied to a TextField.",
    "code": "void Function() debounce(void Function() action, Duration delay) {\n  Timer? timer;\n  return () {\n    timer?.cancel();\n    timer = Timer(delay, action);\n  };\n}\n\nfinal debouncedSave = debounce(() => save(), const Duration(milliseconds: 500));",
    "lang": "dart",
    "revise": "**Generic debounce**: a closure holding one `Timer?`; each call cancels the pending timer and restarts it — only the **last** call in the quiet window runs.\n• **Gotcha**: same idea as debounced-search but **generalized as a reusable utility**, not tied to a TextField."
  },
  {
    "id": "fcoding-two-sum-dart",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "hashmap",
      "two-sum"
    ],
    "question": "Solve the Two Sum problem in Dart.",
    "answer": "<p>Iterate through the list once, tracking each value's index in a <code class=\"inline\">Map</code>. For each element, check whether <code class=\"inline\">target - element</code> already exists in the map—if so, return both indices. This runs in <strong>O(n)</strong> time and O(n) space, versus O(n²) for the brute-force nested loop.</p>",
    "tip": "Always mention the brute-force O(n²) approach first, then explain the hashmap trade-off (time for space)—shows structured thinking.",
    "code": "List<int> twoSum(List<int> nums, int target) {\n  final seen = <int, int>{};\n  for (var i = 0; i < nums.length; i++) {\n    final complement = target - nums[i];\n    if (seen.containsKey(complement)) return [seen[complement]!, i];\n    seen[nums[i]] = i;\n  }\n  throw ArgumentError('No solution found');\n}",
    "lang": "dart",
    "revise": "**Two Sum**: one pass, store each value→index in a Map; for each element check if `target - element` exists → **O(n)** vs O(n²) brute force.\n• **Gotcha**: mention brute force **first**, then the hashmap trade-off (**time for space**) — shows reasoning."
  },
  {
    "id": "fcoding-stopwatch",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "stopwatch",
      "timer",
      "statefulwidget"
    ],
    "question": "Build a stopwatch with start, pause, and reset.",
    "answer": "<p>Use a <code class=\"inline\">Timer.periodic</code> (e.g. every 30-50ms) to increment an elapsed-time counter and call <code class=\"inline\">setState</code>. Start creates the timer, pause cancels it while keeping the elapsed value, and reset cancels the timer and zeroes the counter.</p>",
    "tip": "Cancel the existing timer before creating a new one on 'start' to avoid accidentally running two timers at once if the button is tapped twice.",
    "code": "Timer? _timer;\nDuration _elapsed = Duration.zero;\n\nvoid start() {\n  _timer?.cancel();\n  _timer = Timer.periodic(const Duration(milliseconds: 30), (_) {\n    setState(() => _elapsed += const Duration(milliseconds: 30));\n  });\n}\nvoid pause() => _timer?.cancel();\nvoid reset() { _timer?.cancel(); setState(() => _elapsed = Duration.zero); }",
    "lang": "dart",
    "revise": "**Stopwatch**: `Timer.periodic` (~30–50ms) increments elapsed + `setState`; start creates it, pause cancels keeping elapsed, reset zeroes.\n• **Gotcha**: **cancel the existing timer before creating a new one** on 'start' — else two timers run at once."
  },
  {
    "id": "fcoding-quiz-app",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "quiz",
      "score",
      "statefulwidget"
    ],
    "question": "Build a quiz app that tracks the score across questions.",
    "answer": "<p>Keep a <code class=\"inline\">List&lt;Question&gt;</code>, a <code class=\"inline\">currentIndex</code>, and a <code class=\"inline\">score</code> in state. Selecting an answer checks it against the correct answer, increments the score if correct, then advances <code class=\"inline\">currentIndex</code>; when the index reaches the list length, show a results screen.</p>",
    "tip": "Disable answer buttons immediately after the first tap (e.g. via a 'answered' flag) to prevent double-scoring from a fast double-tap.",
    "code": "int currentIndex = 0;\nint score = 0;\n\nvoid answer(String selected) {\n  if (selected == questions[currentIndex].correctAnswer) score++;\n  setState(() => currentIndex++);\n}",
    "lang": "dart",
    "revise": "**Quiz**: hold `List<Question>`, `currentIndex`, `score`; a correct answer bumps score + advances; at the end show results.\n• **Gotcha**: **disable answer buttons after the first tap** (an `answered` flag) to prevent double-scoring."
  },
  {
    "id": "fcoding-shopping-cart",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "shopping-cart",
      "provider",
      "crud"
    ],
    "question": "Build a shopping cart with add, remove, quantity adjustment, and a running total.",
    "answer": "<p>Model the cart as a <code class=\"inline\">Map&lt;Product, int&gt;</code> (product to quantity) inside a <code class=\"inline\">ChangeNotifier</code>. Adding increments the quantity (or inserts at 1), removing decrements/removes at 0, and a getter computes the total by summing <code class=\"inline\">price * quantity</code> across entries.</p>",
    "tip": "Compute the total as a derived getter, not a stored field—storing it separately risks it going out of sync with the actual cart contents.",
    "code": "class Cart extends ChangeNotifier {\n  final Map<Product, int> _items = {};\n\n  void add(Product p) { _items[p] = (_items[p] ?? 0) + 1; notifyListeners(); }\n  void remove(Product p) {\n    if ((_items[p] ?? 0) <= 1) { _items.remove(p); } else { _items[p] = _items[p]! - 1; }\n    notifyListeners();\n  }\n  double get total => _items.entries.fold(0, (sum, e) => sum + e.key.price * e.value);\n}",
    "lang": "dart",
    "revise": "**Cart**: model as `Map<Product,int>` in a `ChangeNotifier`; add increments, remove decrements/removes at 0; a **getter** computes the total.\n• **Gotcha**: compute the total as a **derived getter**, not a stored field — a stored total drifts out of sync."
  },
  {
    "id": "fcoding-weather-app-ui",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "weather-app",
      "futurebuilder",
      "api"
    ],
    "question": "Build a weather app screen that fetches and displays current conditions.",
    "answer": "<p>Fetch weather data in <code class=\"inline\">initState</code> and store the Future; render with <code class=\"inline\">FutureBuilder</code>, mapping the response to a model (temperature, condition, icon). Add a manual refresh button that re-assigns the Future and calls <code class=\"inline\">setState</code> to re-trigger the FutureBuilder.</p>",
    "tip": "Cache the last successful result so a failed refresh can still show stale data with an error banner, rather than blanking the whole screen.",
    "code": "late Future<Weather> weatherFuture;\n\nvoid refresh() => setState(() => weatherFuture = api.fetchWeather(city));",
    "lang": "dart",
    "revise": "**Weather screen**: fetch in `initState`, store the Future, render with `FutureBuilder`; a refresh button re-assigns the Future + `setState`.\n• **Gotcha**: **cache the last successful result** so a failed refresh shows stale data + an error banner, not a blank screen."
  },
  {
    "id": "fcoding-chat-ui",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "chat-ui",
      "listview",
      "reverse"
    ],
    "question": "Build a basic chat screen UI (message list + input bar).",
    "answer": "<p>Render messages with <code class=\"inline\">ListView.builder(reverse: true)</code> so the list stays pinned to the newest message at the bottom, feeding it a reversed data list (or just inserting new messages at index 0). Pair it with a bottom input row (TextField + send IconButton) wrapped in a <code class=\"inline\">SafeArea</code> so it isn't hidden by the keyboard or home indicator.</p>",
    "tip": "Wrap the Scaffold body in resizeToAvoidBottomInset (default true) so the input bar rides up above the keyboard automatically.",
    "code": "ListView.builder(\n  reverse: true,\n  itemCount: messages.length,\n  itemBuilder: (context, i) => MessageBubble(message: messages[messages.length - 1 - i]),\n);",
    "lang": "dart",
    "revise": "**Chat UI**: `ListView.builder(reverse: true)` pins to the newest message (insert new at index 0) + a bottom input row in a `SafeArea`.\n• **Gotcha**: keep **`resizeToAvoidBottomInset` true** so the input bar rides above the keyboard."
  },
  {
    "id": "fcoding-image-gallery-grid",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "gridview",
      "gallery"
    ],
    "question": "Build an image gallery using GridView.builder.",
    "answer": "<p><code class=\"inline\">GridView.builder</code> with a <code class=\"inline\">SliverGridDelegateWithFixedCrossAxisCount</code> (or <code class=\"inline\">...WithMaxCrossAxisExtent</code> for responsive column counts) lazily builds only the visible thumbnails, using <code class=\"inline\">Image.network</code> with a placeholder/error builder for each cell.</p>",
    "tip": "Use WithMaxCrossAxisExtent instead of a fixed count when the gallery needs to adapt to different screen widths without a manual breakpoint check.",
    "code": "GridView.builder(\n  gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3),\n  itemCount: images.length,\n  itemBuilder: (context, i) => Image.network(images[i], fit: BoxFit.cover),\n);",
    "lang": "dart",
    "revise": "**Gallery grid**: `GridView.builder` with a `SliverGridDelegate...` lazily builds visible thumbnails; `Image.network` with placeholder/error builders.\n• **Gotcha**: use **`WithMaxCrossAxisExtent`** (not a fixed count) so columns adapt to screen width."
  },
  {
    "id": "fcoding-expandable-list",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "expansiontile",
      "accordion"
    ],
    "question": "Build an expandable/collapsible FAQ list.",
    "answer": "<p><code class=\"inline\">ExpansionTile</code> handles the expand/collapse animation and state internally—just give each one a <code class=\"inline\">title</code> (the question) and a <code class=\"inline\">children</code> list (the answer). For a custom look, track expanded state per item manually with an <code class=\"inline\">AnimatedCrossFade</code> or <code class=\"inline\">AnimatedSize</code>.</p>",
    "tip": "",
    "code": "ExpansionTile(\n  title: Text(faq.question),\n  children: [Padding(padding: const EdgeInsets.all(16), child: Text(faq.answer))],\n);",
    "lang": "dart",
    "revise": "**Expandable FAQ**: `ExpansionTile` handles expand/collapse + animation internally — give it a `title` + `children`.\n• **Gotcha**: for a custom look, track expanded state manually with `AnimatedCrossFade`/`AnimatedSize`."
  },
  {
    "id": "fcoding-reorderable-list",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "reorderablelistview",
      "drag-and-drop"
    ],
    "question": "Build a drag-to-reorder list.",
    "answer": "<p><code class=\"inline\">ReorderableListView</code> handles the drag gesture and animation automatically; you just implement <code class=\"inline\">onReorder(oldIndex, newIndex)</code> to update your underlying list's order (adjusting the index since Flutter passes newIndex as if the item hadn't been removed yet). Each child needs a unique <code class=\"inline\">key</code>.</p>",
    "tip": "Remember the classic off-by-one fix: if newIndex > oldIndex, decrement newIndex by 1 before inserting.",
    "code": "ReorderableListView(\n  onReorder: (oldIndex, newIndex) {\n    setState(() {\n      if (newIndex > oldIndex) newIndex--;\n      final item = items.removeAt(oldIndex);\n      items.insert(newIndex, item);\n    });\n  },\n  children: [for (final i in items) ListTile(key: ValueKey(i.id), title: Text(i.name))],\n);",
    "lang": "dart",
    "revise": "**Drag-to-reorder**: `ReorderableListView` handles the gesture; implement `onReorder` to update your list; each child needs a **unique key**.\n• **Gotcha**: the classic fix — **if `newIndex > oldIndex`, decrement `newIndex` by 1** before inserting."
  },
  {
    "id": "fcoding-star-rating-widget",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "rating",
      "custom-widget"
    ],
    "question": "Build a reusable star-rating widget.",
    "answer": "<p>Render N icon widgets in a Row, filling stars up to the current <code class=\"inline\">rating</code> value; wrap each in a <code class=\"inline\">GestureDetector</code> that sets the rating to its index+1 on tap and calls a passed-in <code class=\"inline\">onChanged</code> callback so the parent controls the actual state.</p>",
    "tip": "Keep the widget stateless and controlled (rating + onChanged passed in) rather than stateful, so it's reusable in a form with validation.",
    "code": "Row(children: List.generate(5, (i) => GestureDetector(\n  onTap: () => onChanged(i + 1),\n  child: Icon(i < rating ? Icons.star : Icons.star_border, color: Colors.amber),\n)));",
    "lang": "dart",
    "revise": "**Star rating**: a Row of icons filled up to the value; each in a `GestureDetector` that sets rating to `index+1` and calls `onChanged`.\n• **Gotcha**: keep it **stateless + controlled** (rating + onChanged passed in) so it's reusable."
  },
  {
    "id": "fcoding-search-filter-list",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "search",
      "filter",
      "list"
    ],
    "question": "Build a search bar that filters a local list as the user types.",
    "answer": "<p>Keep the full source list and a <code class=\"inline\">filtered</code> list in state. On each <code class=\"inline\">TextField</code> change, filter the source list case-insensitively and call <code class=\"inline\">setState</code> to update the displayed list—no debounce/API call needed since it's all in-memory.</p>",
    "tip": "Only debounce this if the filter triggers something expensive (like re-rendering thousands of items)—for small local lists, filtering on every keystroke is fine.",
    "code": "void onSearch(String query) {\n  setState(() {\n    filtered = allItems.where((i) => i.name.toLowerCase().contains(query.toLowerCase())).toList();\n  });\n}",
    "lang": "dart",
    "revise": "**Local search filter**: keep the full source + a filtered list; on each change, filter case-insensitively + `setState` — no debounce/API needed (in-memory).\n• **Gotcha**: only **debounce if the filter is expensive** (thousands of items); for small lists it's needless."
  },
  {
    "id": "fcoding-custom-tabbar",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "tabbar",
      "tabcontroller"
    ],
    "question": "Build a screen with a custom-styled TabBar and three tabs.",
    "answer": "<p>Use a <code class=\"inline\">DefaultTabController</code> (or a manual <code class=\"inline\">TabController</code> with a <code class=\"inline\">SingleTickerProviderStateMixin</code>) with a <code class=\"inline\">TabBar</code> in the AppBar's <code class=\"inline\">bottom</code> slot, and a matching <code class=\"inline\">TabBarView</code> in the body holding one widget per tab in the same order.</p>",
    "tip": "DefaultTabController is simpler when you don't need to listen to tab-change events; use a manual TabController when you need to react to index changes elsewhere.",
    "code": "DefaultTabController(\n  length: 3,\n  child: Scaffold(\n    appBar: AppBar(bottom: const TabBar(tabs: [Tab(text: 'A'), Tab(text: 'B'), Tab(text: 'C')])),\n    body: const TabBarView(children: [PageA(), PageB(), PageC()]),\n  ),\n);",
    "lang": "dart",
    "revise": "**Custom TabBar**: `DefaultTabController` + `TabBar` in the AppBar bottom + a matching `TabBarView` (same order).\n• **Gotcha**: `DefaultTabController` is simplest; use a **manual `TabController`** only when you need to listen to tab-change events."
  },
  {
    "id": "fcoding-draggable-bottom-sheet",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "draggablescrollablesheet",
      "bottomsheet"
    ],
    "question": "Build a draggable bottom sheet that expands from partial to full height.",
    "answer": "<p><code class=\"inline\">DraggableScrollableSheet</code> lets a bottom sheet be dragged between a min, initial, and max fractional size of the screen, with its own scroll controller so content scrolls once the sheet reaches max size instead of dragging further.</p>",
    "tip": "",
    "code": "DraggableScrollableSheet(\n  initialChildSize: 0.4,\n  minChildSize: 0.2,\n  maxChildSize: 0.9,\n  builder: (context, scrollController) => ListView.builder(\n    controller: scrollController,\n    itemCount: items.length,\n    itemBuilder: (context, i) => ListTile(title: Text(items[i])),\n  ),\n);",
    "lang": "dart",
    "revise": "**Draggable sheet**: `DraggableScrollableSheet` drags between min/initial/max fractional heights, with its own scroll controller so content scrolls once fully expanded.\n• **Gotcha**: wire its `scrollController` into the inner scrollable, or drag and scroll fight each other."
  },
  {
    "id": "fcoding-splash-navigation",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "splash-screen",
      "navigation",
      "delay"
    ],
    "question": "Build a splash screen that navigates to home after a delay.",
    "answer": "<p>In <code class=\"inline\">initState</code>, start a <code class=\"inline\">Future.delayed</code> (or check an async auth-status call) and then call <code class=\"inline\">Navigator.pushReplacement</code> so the splash screen isn't left in the back stack (preventing the user from navigating back to it).</p>",
    "tip": "Always check 'if (!mounted) return;' before navigating after an await—the widget could be disposed if the user backgrounds the app during the delay.",
    "code": "@override\nvoid initState() {\n  super.initState();\n  Future.delayed(const Duration(seconds: 2), () {\n    if (!mounted) return;\n    Navigator.pushReplacement(context, MaterialPageRoute(builder: (_) => const HomePage()));\n  });\n}",
    "lang": "dart",
    "revise": "**Splash → home**: in `initState`, `Future.delayed` (or await auth), then **`Navigator.pushReplacement`** so splash leaves the back stack.\n• **Gotcha**: check **`if (!mounted) return;`** before navigating after an await — the widget could be disposed."
  },
  {
    "id": "fcoding-onboarding-slider",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "onboarding",
      "pageview",
      "indicator"
    ],
    "question": "Build an onboarding intro slider with page indicator dots.",
    "answer": "<p>Use a <code class=\"inline\">PageView.builder</code> for the swipeable slides and listen to its <code class=\"inline\">PageController</code> to track the current page index, rendering a row of dots that highlight the active index. A 'Next'/'Get Started' button on the last page navigates away.</p>",
    "tip": "Persist a 'hasSeenOnboarding' flag (shared_preferences) so the slider only shows once, not every app launch.",
    "code": "final controller = PageController();\nint currentPage = 0;\n\nPageView.builder(\n  controller: controller,\n  onPageChanged: (i) => setState(() => currentPage = i),\n  itemCount: slides.length,\n  itemBuilder: (context, i) => OnboardSlide(slides[i]),\n);",
    "lang": "dart",
    "revise": "**Onboarding slider**: `PageView.builder` for slides + a `PageController` to track the index for indicator dots; last page navigates away.\n• **Gotcha**: persist a **`hasSeenOnboarding` flag** (shared_preferences) so it shows once, not every launch."
  },
  {
    "id": "fcoding-theme-switcher",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "theme",
      "dark-mode",
      "provider"
    ],
    "question": "Build a light/dark theme switcher using Provider.",
    "answer": "<p>A <code class=\"inline\">ChangeNotifier</code> holds a <code class=\"inline\">ThemeMode</code> field and a toggle method that calls <code class=\"inline\">notifyListeners()</code>. <code class=\"inline\">MaterialApp</code> reads it with <code class=\"inline\">context.watch</code> and passes it to its <code class=\"inline\">themeMode</code> parameter, alongside separate <code class=\"inline\">theme</code> and <code class=\"inline\">darkTheme</code> definitions.</p>",
    "tip": "Persist the chosen mode with shared_preferences so the user's choice survives an app restart.",
    "code": "class ThemeController extends ChangeNotifier {\n  ThemeMode mode = ThemeMode.system;\n  void toggle() {\n    mode = mode == ThemeMode.dark ? ThemeMode.light : ThemeMode.dark;\n    notifyListeners();\n  }\n}",
    "lang": "dart",
    "revise": "**Theme switcher**: a `ChangeNotifier` holds `ThemeMode`; `MaterialApp` reads it via `context.watch` into `themeMode` alongside `theme`/`darkTheme`.\n• **Gotcha**: **persist the choice** (shared_preferences) so it survives an app restart."
  },
  {
    "id": "fcoding-custom-snackbar-util",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "snackbar",
      "utility"
    ],
    "question": "Build a reusable snackbar utility function for success/error messages.",
    "answer": "<p>Wrap <code class=\"inline\">ScaffoldMessenger.of(context).showSnackBar</code> in a static helper that takes a message and a type (success/error), picking a background color and icon accordingly—centralizing styling so every screen shows consistent feedback with one function call.</p>",
    "tip": "Call ScaffoldMessenger.of(context).hideCurrentSnackBar() first if messages can fire in quick succession, so they don't queue up awkwardly.",
    "code": "class AppSnackbar {\n  static void show(BuildContext context, String message, {bool isError = false}) {\n    ScaffoldMessenger.of(context)\n      ..hideCurrentSnackBar()\n      ..showSnackBar(SnackBar(\n        content: Text(message),\n        backgroundColor: isError ? Colors.red : Colors.green,\n      ));\n  }\n}",
    "lang": "dart",
    "revise": "**Snackbar util**: a static helper wrapping `ScaffoldMessenger...showSnackBar`, taking message + type → picks color/icon, centralizing consistent feedback.\n• **Gotcha**: call **`hideCurrentSnackBar()` first** if messages fire in quick succession, so they don't queue up."
  },
  {
    "id": "fcoding-connectivity-listener",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "connectivity",
      "network-status"
    ],
    "question": "Build a widget that shows a banner when the device loses internet connectivity.",
    "answer": "<p>The <code class=\"inline\">connectivity_plus</code> package exposes <code class=\"inline\">Connectivity().onConnectivityChanged</code> as a Stream; feed it into a <code class=\"inline\">StreamBuilder</code> at the app's root, and conditionally show a persistent banner ('No internet connection') when the result is <code class=\"inline\">ConnectivityResult.none</code>.</p>",
    "tip": "Connectivity state (WiFi connected) doesn't guarantee actual internet access—for a stricter check, ping a lightweight endpoint too.",
    "code": "StreamBuilder<List<ConnectivityResult>>(\n  stream: Connectivity().onConnectivityChanged,\n  builder: (context, snapshot) {\n    final offline = snapshot.data?.contains(ConnectivityResult.none) ?? false;\n    return offline ? const OfflineBanner() : const SizedBox.shrink();\n  },\n);",
    "lang": "dart",
    "revise": "**Offline banner**: `Connectivity().onConnectivityChanged` (connectivity_plus) → a root `StreamBuilder` shows a banner on `ConnectivityResult.none`.\n• **Gotcha**: connectivity **≠ real internet** — for a stricter check, ping a lightweight endpoint."
  },
  {
    "id": "fcoding-biometric-auth",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "biometrics",
      "local_auth",
      "security"
    ],
    "question": "Implement a biometric (fingerprint/Face ID) login gate using local_auth.",
    "answer": "<p>The <code class=\"inline\">local_auth</code> package's <code class=\"inline\">authenticate()</code> method triggers the native biometric prompt and returns a bool; check <code class=\"inline\">canCheckBiometrics</code> first to confirm the device supports it, and always provide a fallback (PIN/password) path for devices or users without biometrics enrolled.</p>",
    "tip": "",
    "code": "final auth = LocalAuthentication();\nif (await auth.canCheckBiometrics) {\n  final didAuth = await auth.authenticate(\n    localizedReason: 'Please authenticate to continue',\n  );\n}",
    "lang": "dart",
    "revise": "**Biometric gate**: `local_auth`'s `authenticate()` triggers the native prompt → bool; check `canCheckBiometrics` first.\n• **Gotcha**: always provide a **PIN/password fallback** for devices/users without biometrics enrolled."
  },
  {
    "id": "fcoding-image-picker-upload",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "image-upload",
      "multipart",
      "dio"
    ],
    "question": "Implement picking an image and uploading it to an API with progress tracking.",
    "answer": "<p>Pick the file with <code class=\"inline\">image_picker</code>, then build a <code class=\"inline\">FormData</code> with <code class=\"inline\">MultipartFile.fromFile</code> and send it via Dio's <code class=\"inline\">post</code>, passing an <code class=\"inline\">onSendProgress</code> callback to update a progress indicator as bytes are uploaded.</p>",
    "tip": "",
    "code": "final formData = FormData.fromMap({\n  'file': await MultipartFile.fromFile(image.path, filename: 'upload.jpg'),\n});\nawait dio.post('/upload', data: formData, onSendProgress: (sent, total) {\n  setState(() => progress = sent / total);\n});",
    "lang": "dart",
    "revise": "**Pick + upload with progress**: `image_picker` gets the file → build `FormData` with `MultipartFile.fromFile` → Dio `post` with **`onSendProgress`** to drive a progress bar.\n• **Gotcha**: validate type/size before upload and handle the user cancelling the picker (null result)."
  },
  {
    "id": "fcoding-video-player",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "video-player",
      "media"
    ],
    "question": "Build a video player screen with play/pause controls.",
    "answer": "<p>Initialize a <code class=\"inline\">VideoPlayerController</code> from a network/asset source in <code class=\"inline\">initState</code>, await <code class=\"inline\">initialize()</code>, then wrap the <code class=\"inline\">VideoPlayer</code> widget with custom play/pause buttons calling <code class=\"inline\">controller.play()</code>/<code class=\"inline\">pause()</code>. Always dispose the controller.</p>",
    "tip": "Show a loading spinner until controller.value.isInitialized is true—the video widget renders a blank/zero-size box before that.",
    "code": "late VideoPlayerController controller;\n\n@override\nvoid initState() {\n  super.initState();\n  controller = VideoPlayerController.networkUrl(Uri.parse(url))..initialize().then((_) => setState(() {}));\n}\n\n@override\nvoid dispose() { controller.dispose(); super.dispose(); }",
    "lang": "dart",
    "revise": "**Video player**: init `VideoPlayerController` in `initState`, `await initialize()`, wrap `VideoPlayer` with play/pause buttons; **dispose** the controller.\n• **Gotcha**: show a spinner until **`controller.value.isInitialized`** — else it renders a blank/zero-size box."
  },
  {
    "id": "fcoding-circular-progress-percentage",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "custompainter",
      "progress-indicator"
    ],
    "question": "Build a custom circular progress indicator that shows a percentage in the center.",
    "answer": "<p>Stack a <code class=\"inline\">CircularProgressIndicator(value: progress)</code> (or a <code class=\"inline\">CustomPainter</code> drawing an arc with <code class=\"inline\">Canvas.drawArc</code> for full styling control) with a centered <code class=\"inline\">Text('${(progress * 100).round()}%')</code> using a <code class=\"inline\">Stack</code> + <code class=\"inline\">Alignment.center</code>.</p>",
    "tip": "",
    "code": "Stack(\n  alignment: Alignment.center,\n  children: [\n    CircularProgressIndicator(value: progress, strokeWidth: 8),\n    Text('${(progress * 100).round()}%'),\n  ],\n);",
    "lang": "dart",
    "revise": "**Circular % indicator**: `Stack` a `CircularProgressIndicator(value:)` (or a `CustomPainter` arc for full control) with a centered `Text('${(progress*100).round()}%')`.\n• **Gotcha**: reach for `CustomPainter` + `drawArc` only when you need styling the built-in widget can't give."
  },
  {
    "id": "fcoding-calculator-logic",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "calculator",
      "logic"
    ],
    "question": "Implement the core logic for a basic calculator app (digits, operators, equals).",
    "answer": "<p>Keep the current input string, a pending operator, and a stored first operand in state. Digit taps append to the display string; an operator tap stores the current value and operator, clearing the display for the next number; equals applies the stored operator between the stored value and the current display value.</p>",
    "tip": "Handle 'AC' (clear all) and consecutive-operator-taps (replace the pending operator instead of stacking) as your key edge cases when asked to extend this.",
    "code": "String display = '0';\ndouble? stored;\nString? pendingOp;\n\nvoid onOperator(String op) {\n  stored = double.parse(display);\n  pendingOp = op;\n  display = '0';\n}\nvoid onEquals() {\n  final current = double.parse(display);\n  display = switch (pendingOp) {\n    '+' => (stored! + current).toString(),\n    '-' => (stored! - current).toString(),\n    _ => display,\n  };\n}",
    "lang": "dart",
    "revise": "**Calculator logic**: hold the current input, a pending operator, and a stored operand; digits append, an operator stores value+op, equals applies it.\n• **Gotcha**: handle **'AC' (clear all)** and **consecutive operator taps** (replace, don't stack the operator)."
  },
  {
    "id": "fcoding-currency-converter",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "currency-converter",
      "api",
      "futurebuilder"
    ],
    "question": "Build a currency converter that fetches live exchange rates.",
    "answer": "<p>Fetch a rates map (base currency to rate) from an API once and cache it in state; conversion itself is then a pure local calculation (<code class=\"inline\">amount * rate</code>) on every keystroke, avoiding an API call per digit typed. Refresh the rates periodically or via a manual refresh button.</p>",
    "tip": "Round display output to 2 decimal places but keep full precision internally for the actual calculation—rounding too early compounds error on repeated conversions.",
    "code": "double convert(double amount, double rate) => amount * rate;",
    "lang": "dart",
    "revise": "**Currency converter**: fetch the rates map **once**, cache it; conversion is then a **pure local `amount * rate`** per keystroke — no API call per digit.\n• **Gotcha**: **round only the display** (2 dp), keep full precision internally for the calculation."
  },
  {
    "id": "fcoding-notes-app-hive",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "hive",
      "local-storage",
      "notes-app"
    ],
    "question": "Build a notes app that persists notes locally using Hive.",
    "answer": "<p>Register a Hive adapter for the Note model (or store as a Map for simplicity), open a Hive box at startup, and treat the box like a reactive local database—use <code class=\"inline\">ValueListenableBuilder</code> on <code class=\"inline\">box.listenable()</code> to automatically rebuild the UI whenever notes are added, edited, or deleted, without manual setState plumbing.</p>",
    "tip": "Open the Hive box once in main() before runApp(), not inside a widget's initState, so it's ready before the first frame needs it.",
    "code": "final box = await Hive.openBox('notes');\n\nValueListenableBuilder(\n  valueListenable: box.listenable(),\n  builder: (context, Box box, _) => ListView(\n    children: box.values.map((n) => ListTile(title: Text(n.title))).toList(),\n  ),\n);",
    "lang": "dart",
    "revise": "**Notes with Hive**: register an adapter, open a box, and use **`ValueListenableBuilder(box.listenable())`** so the UI auto-rebuilds on add/edit/delete — no manual `setState`.\n• **Gotcha**: **open the box in `main()` before `runApp()`**, not in a widget's `initState`."
  },
  {
    "id": "fcoding-favorites-toggle",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "favorites",
      "bookmark",
      "shared_preferences"
    ],
    "question": "Build a favorite/bookmark toggle that persists across app restarts.",
    "answer": "<p>Store favorited item IDs as a <code class=\"inline\">Set&lt;String&gt;</code> (serialized to a <code class=\"inline\">List&lt;String&gt;</code> for <code class=\"inline\">shared_preferences</code>, since it can't store sets directly). Toggling adds/removes the ID from the set, persists it, and calls <code class=\"inline\">notifyListeners()</code> so every screen showing that item reflects the change immediately.</p>",
    "tip": "Centralize favorite state in one ChangeNotifier/Provider rather than per-screen local state—otherwise the same item can show as favorited on one screen and not on another.",
    "code": "Future<void> toggleFavorite(String id) async {\n  favorites.contains(id) ? favorites.remove(id) : favorites.add(id);\n  final prefs = await SharedPreferences.getInstance();\n  await prefs.setStringList('favorites', favorites.toList());\n  notifyListeners();\n}",
    "lang": "dart",
    "revise": "**Persistent favorites**: store IDs as a `Set<String>` (serialize to `List` for shared_preferences); toggling persists + `notifyListeners()`.\n• **Gotcha**: **centralize favorite state in one Provider**, not per-screen — else the same item shows inconsistent states."
  },
  {
    "id": "fcoding-multistep-form-wizard",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "multi-step-form",
      "wizard",
      "pageview"
    ],
    "question": "Build a multi-step form wizard (e.g. a 3-step signup flow).",
    "answer": "<p>Use a <code class=\"inline\">PageView</code> with <code class=\"inline\">NeverScrollableScrollPhysics</code> (so users can't swipe past validation) driven by a <code class=\"inline\">PageController</code>; each step has its own <code class=\"inline\">Form</code>/<code class=\"inline\">GlobalKey&lt;FormState&gt;</code>, and 'Next' only advances the page if that step's form validates. Collect all step data into one shared model object passed down to each step.</p>",
    "tip": "Keep one GlobalKey per step, not one shared key—reusing a single FormState key across pages that are still in the widget tree (even if off-screen) causes validation conflicts.",
    "code": "void next() {\n  if (formKeys[currentStep].currentState!.validate()) {\n    pageController.nextPage(duration: const Duration(milliseconds: 300), curve: Curves.easeInOut);\n  }\n}",
    "lang": "dart",
    "revise": "**Form wizard**: a `PageView` (`NeverScrollableScrollPhysics`) driven by a controller; each step has its own `Form`/key; 'Next' advances only if that step validates; collect into one shared model.\n• **Gotcha**: **one `GlobalKey` per step**, not a shared key across still-mounted pages."
  },
  {
    "id": "fcoding-custom-date-picker",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "date-picker",
      "showdatepicker"
    ],
    "question": "Implement date selection using showDatePicker, and format the result.",
    "answer": "<p><code class=\"inline\">showDatePicker</code> returns a <code class=\"inline\">Future&lt;DateTime?&gt;</code> that resolves to null if the user cancels. Store the selected date in state and format it for display using the <code class=\"inline\">intl</code> package's <code class=\"inline\">DateFormat</code> rather than manually concatenating date fields.</p>",
    "tip": "Always null-check the result—showDatePicker returns null on cancel, and forgetting that check is a very common crash.",
    "code": "final picked = await showDatePicker(\n  context: context,\n  initialDate: DateTime.now(),\n  firstDate: DateTime(2000),\n  lastDate: DateTime(2100),\n);\nif (picked != null) setState(() => selectedDate = picked);",
    "lang": "dart",
    "revise": "**Date picker**: `showDatePicker` returns a `Future<DateTime?>` (**null on cancel**); store the date, format with **`intl`'s `DateFormat`**.\n• **Gotcha**: **null-check the result** — forgetting the cancel case is a very common bug."
  },
  {
    "id": "fcoding-otp-verification",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "otp",
      "verification",
      "textfield"
    ],
    "question": "Build a 6-digit OTP verification input with auto-focus advance.",
    "answer": "<p>Use 6 individual single-character <code class=\"inline\">TextField</code>s, each with its own <code class=\"inline\">FocusNode</code> and <code class=\"inline\">TextEditingController</code>; on each field's <code class=\"inline\">onChanged</code>, if a digit was entered, request focus on the next field's FocusNode (or the previous one on backspace/empty), and concatenate all controllers' text to validate the full code.</p>",
    "tip": "Dispose all 6 FocusNodes and controllers in dispose()—forgetting even one in a list of similar objects is an easy leak to introduce.",
    "code": "void onChanged(int index, String value) {\n  if (value.isNotEmpty && index < 5) {\n    focusNodes[index + 1].requestFocus();\n  } else if (value.isEmpty && index > 0) {\n    focusNodes[index - 1].requestFocus();\n  }\n}",
    "lang": "dart",
    "revise": "**6-digit OTP**: 6 single-char `TextField`s, each with its own `FocusNode`/controller; `onChanged` advances/retreats focus; concatenate to validate.\n• **Gotcha**: **dispose ALL 6 FocusNodes + controllers** — missing one in a repeated list is an easy leak."
  },
  {
    "id": "fcoding-google-signin",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "google-sign-in",
      "firebase-auth"
    ],
    "question": "Implement Google Sign-In with Firebase Auth.",
    "answer": "<p>Trigger the native Google sign-in flow via the <code class=\"inline\">google_sign_in</code> package to get an auth token, then exchange it for a Firebase credential with <code class=\"inline\">GoogleAuthProvider.credential(...)</code> and sign in to Firebase via <code class=\"inline\">FirebaseAuth.instance.signInWithCredential(...)</code>, giving you a unified Firebase user regardless of provider.</p>",
    "tip": "",
    "code": "final googleUser = await GoogleSignIn().signIn();\nfinal googleAuth = await googleUser!.authentication;\nfinal credential = GoogleAuthProvider.credential(\n  accessToken: googleAuth.accessToken,\n  idToken: googleAuth.idToken,\n);\nawait FirebaseAuth.instance.signInWithCredential(credential);",
    "lang": "dart",
    "revise": "**Google Sign-In**: `google_sign_in` gets a token → `GoogleAuthProvider.credential(...)` → `FirebaseAuth.instance.signInWithCredential(...)` for a unified Firebase user.\n• **Gotcha**: handle the user cancelling the native picker (null account) gracefully."
  },
  {
    "id": "fcoding-sticky-header-list",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "sliverpersistentheader",
      "sticky-header"
    ],
    "question": "Build a list with section headers that stick to the top while scrolling.",
    "answer": "<p>Use a <code class=\"inline\">CustomScrollView</code> with one <code class=\"inline\">SliverPersistentHeader(pinned: true)</code> per section (implementing a <code class=\"inline\">SliverPersistentHeaderDelegate</code> for the section label) followed by a <code class=\"inline\">SliverList</code> of that section's items—so headers pin to the top as their section scrolls past, then get pushed off by the next section's header.</p>",
    "tip": "",
    "code": "CustomScrollView(slivers: [\n  for (final section in sections) ...[\n    SliverPersistentHeader(pinned: true, delegate: SectionHeaderDelegate(section.title)),\n    SliverList(delegate: SliverChildListDelegate(section.items.map((i) => ListTile(title: Text(i))).toList())),\n  ],\n]);",
    "lang": "dart",
    "revise": "**Sticky section headers**: a `CustomScrollView` with a `SliverPersistentHeader(pinned: true)` per section followed by that section's `SliverList`.\n• **Gotcha**: you must implement a `SliverPersistentHeaderDelegate` with min/max extent for the header."
  },
  {
    "id": "fcoding-shimmer-loading",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "shimmer",
      "skeleton-loader",
      "loading-state"
    ],
    "question": "Build a shimmer/skeleton loading placeholder while content loads.",
    "answer": "<p>The <code class=\"inline\">shimmer</code> package wraps a gray placeholder layout (boxes matching the shape of real content) in a <code class=\"inline\">Shimmer.fromColors</code> widget that animates a moving gradient over it. Show the shimmer skeleton while <code class=\"inline\">FutureBuilder</code>'s connectionState is waiting, then swap to real content once data arrives.</p>",
    "tip": "Match the skeleton's shapes/sizes closely to the real content's layout—this avoids a jarring layout shift once real data replaces the placeholder.",
    "code": "",
    "lang": "dart",
    "revise": "**Shimmer skeleton**: the `shimmer` package animates a gradient over grey placeholder boxes; show it while `FutureBuilder` is `waiting`, then swap to real content.\n• **Gotcha**: **match the skeleton's shapes/sizes** to real content to avoid a jarring layout shift."
  },
  {
    "id": "fcoding-error-empty-state",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "error-state",
      "empty-state",
      "ux"
    ],
    "question": "Build a reusable error/empty-state widget pattern for list screens.",
    "answer": "<p>A small stateless <code class=\"inline\">StateView</code> widget takes an icon, message, and optional retry callback/button—reused across every screen for 'no results', 'no internet', and 'something went wrong', so the same three states aren't reimplemented ad hoc on each screen.</p>",
    "tip": "Always give the empty/error state a retry action when the cause is recoverable (network error)—a dead-end message with no way forward frustrates users.",
    "code": "class StateView extends StatelessWidget {\n  final IconData icon;\n  final String message;\n  final VoidCallback? onRetry;\n  const StateView({super.key, required this.icon, required this.message, this.onRetry});\n\n  @override\n  Widget build(BuildContext context) => Center(child: Column(\n    mainAxisSize: MainAxisSize.min,\n    children: [\n      Icon(icon, size: 48),\n      Text(message),\n      if (onRetry != null) ElevatedButton(onPressed: onRetry, child: const Text('Retry')),\n    ],\n  ));\n}",
    "lang": "dart",
    "revise": "**Reusable state view**: a small stateless widget taking icon + message + optional retry, reused for 'no results'/'no internet'/'error' across screens.\n• **Gotcha**: always give a **retry action** when the cause is recoverable — a dead-end message frustrates users."
  },
  {
    "id": "fcoding-retry-mechanism",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "retry",
      "exponential-backoff",
      "networking"
    ],
    "question": "Implement a retry mechanism with exponential backoff for a failing API call.",
    "answer": "<p>Wrap the API call in a loop up to a max attempt count; on failure, wait an increasing delay (e.g. doubling each time, optionally with jitter) before retrying, and rethrow once the max attempts are exhausted. This avoids hammering a struggling server while still recovering from transient failures.</p>",
    "tip": "Only retry on transient errors (timeouts, 5xx)—retrying a 4xx client error (like invalid credentials) just wastes time since the outcome won't change.",
    "code": "Future<T> retry<T>(Future<T> Function() action, {int maxAttempts = 3}) async {\n  var attempt = 0;\n  while (true) {\n    try {\n      return await action();\n    } catch (e) {\n      attempt++;\n      if (attempt >= maxAttempts) rethrow;\n      await Future.delayed(Duration(seconds: 1 << attempt));\n    }\n  }\n}",
    "lang": "dart",
    "revise": "**Retry with backoff**: loop up to a max; on failure wait an **increasing delay (double + jitter)** before retrying; rethrow when exhausted.\n• **Gotcha**: **only retry transient errors** (timeouts, 5xx) — retrying a 4xx (bad credentials) just wastes attempts."
  },
  {
    "id": "fcoding-offline-first-sync",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "offline-first",
      "sync",
      "repository-pattern"
    ],
    "question": "Design an offline-first data layer that syncs local changes when connectivity returns.",
    "answer": "<p>A repository always reads/writes to local storage (Hive/sqflite) first for instant UI response, queuing an 'unsynced' flag on each changed record. A background sync routine—triggered on connectivity restore or app resume—pushes queued changes to the API, then marks them synced or handles conflicts (e.g. last-write-wins or a merge strategy).</p>",
    "tip": "Mention conflict resolution explicitly (e.g. compare updatedAt timestamps)—interviewers probing this topic want to see you've thought past the happy path.",
    "code": "",
    "lang": "",
    "revise": "**Offline-first**: the repo reads/writes **local storage first** (instant UI), flags records 'unsynced'; a background routine pushes queued changes on reconnect.\n• **Gotcha**: address **conflict resolution explicitly** (compare `updatedAt` timestamps) — interviewers probe this."
  },
  {
    "id": "fcoding-websocket-chat",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "websocket",
      "real-time",
      "streambuilder"
    ],
    "question": "Build a real-time chat feature using WebSockets.",
    "answer": "<p>Connect with <code class=\"inline\">WebSocketChannel.connect(uri)</code>; its <code class=\"inline\">.stream</code> is a broadcast Stream of incoming messages fed directly into a <code class=\"inline\">StreamBuilder</code>, and <code class=\"inline\">channel.sink.add(message)</code> sends outgoing messages. Always close the channel (<code class=\"inline\">channel.sink.close()</code>) in dispose to avoid a dangling connection.</p>",
    "tip": "Implement a reconnect-with-backoff strategy for when the socket drops—raw WebSocket connections don't auto-reconnect.",
    "code": "final channel = WebSocketChannel.connect(Uri.parse('wss://example.com/chat'));\nchannel.sink.add(jsonEncode({'text': 'hello'}));\n\nStreamBuilder(\n  stream: channel.stream,\n  builder: (context, snapshot) => Text(snapshot.data?.toString() ?? ''),\n);",
    "lang": "dart",
    "revise": "**WebSocket chat**: `WebSocketChannel.connect(uri)`; feed `.stream` into a `StreamBuilder`, send via `channel.sink.add(...)`; **close** the sink in `dispose`.\n• **Gotcha**: raw sockets **don't auto-reconnect** — add a reconnect-with-backoff strategy for drops."
  },
  {
    "id": "fcoding-custom-appbar-search",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "appbar",
      "search",
      "showsearch"
    ],
    "question": "Add a search icon to an AppBar that expands into a search field.",
    "answer": "<p>Toggle a boolean <code class=\"inline\">isSearching</code> state; when true, replace the AppBar's <code class=\"inline\">title</code> with a <code class=\"inline\">TextField</code> and swap the search icon for a close icon that resets the state and clears the query. For a full search experience, Flutter's built-in <code class=\"inline\">showSearch(context: context, delegate: MySearchDelegate())</code> handles this pattern with its own dedicated screen.</p>",
    "tip": "",
    "code": "",
    "lang": "dart",
    "revise": "**Expanding AppBar search**: toggle `isSearching`; when true, replace the title with a `TextField` and the icon with a close button that resets state.\n• **Gotcha**: for a full experience, Flutter's built-in **`showSearch` + `SearchDelegate`** handles this pattern."
  },
  {
    "id": "fcoding-bottom-nav-badges",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "bottom-navigation",
      "badge",
      "notifications"
    ],
    "question": "Add unread-count badges to a bottom navigation bar's icons.",
    "answer": "<p>Wrap each tab icon in a <code class=\"inline\">Badge</code> widget (Material 3's built-in <code class=\"inline\">Badge</code>, or a <code class=\"inline\">Stack</code> + <code class=\"inline\">Positioned</code> circle for older versions), conditionally showing it only when that tab's unread count is greater than zero, sourced from a shared state/provider tracking notification counts per tab.</p>",
    "tip": "",
    "code": "BottomNavigationBarItem(\n  icon: Badge(\n    label: Text('$unreadCount'),\n    isLabelVisible: unreadCount > 0,\n    child: const Icon(Icons.chat),\n  ),\n  label: 'Chats',\n);",
    "lang": "dart",
    "revise": "**Nav badges**: wrap each tab icon in a `Badge` (Material 3) or a `Stack + Positioned` circle, shown only when that tab's unread count > 0, sourced from shared state.\n• **Gotcha**: drive counts from a **provider**, not per-widget local state, so they stay consistent."
  },
  {
    "id": "fcoding-drawer-navigation",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "drawer",
      "navigation"
    ],
    "question": "Build a side drawer navigation menu.",
    "answer": "<p>Pass a <code class=\"inline\">Drawer</code> widget to <code class=\"inline\">Scaffold(drawer: ...)</code>, containing a <code class=\"inline\">ListView</code> of menu items (often with a <code class=\"inline\">DrawerHeader</code> showing user info at top); each item's <code class=\"inline\">onTap</code> should first call <code class=\"inline\">Navigator.pop(context)</code> to close the drawer, then navigate to the target screen.</p>",
    "tip": "Always pop the drawer before navigating—forgetting this leaves the drawer open behind the new screen's transition.",
    "code": "",
    "lang": "dart",
    "revise": "**Side drawer**: `Scaffold(drawer: Drawer(...))` with a `ListView` of items (often a `DrawerHeader`); each item **pops the drawer, then navigates**.\n• **Gotcha**: **`Navigator.pop` first** — forgetting it leaves the drawer open behind the new screen."
  },
  {
    "id": "fcoding-settings-screen",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "settings",
      "switch",
      "slider"
    ],
    "question": "Build a settings screen with toggle switches and a slider.",
    "answer": "<p>Each setting (notifications on/off, font size) is a controlled widget—<code class=\"inline\">Switch(value: ..., onChanged: ...)</code> or <code class=\"inline\">Slider(value: ..., onChanged: ...)</code>—backed by a settings model/provider that persists changes immediately to <code class=\"inline\">shared_preferences</code> as the user interacts, rather than requiring a separate 'Save' button.</p>",
    "tip": "",
    "code": "SwitchListTile(\n  title: const Text('Enable notifications'),\n  value: settings.notificationsEnabled,\n  onChanged: (v) => settings.setNotifications(v),\n);",
    "lang": "dart",
    "revise": "**Settings screen**: each setting is a **controlled** `Switch`/`Slider` (`value` + `onChanged`) backed by a provider that **persists immediately** to shared_preferences.\n• **Gotcha**: persist on change, not via a separate 'Save' button, so settings can't get lost."
  },
  {
    "id": "fcoding-custom-slider",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "custom-slider",
      "gesturedetector"
    ],
    "question": "Build a custom-styled slider from scratch using GestureDetector.",
    "answer": "<p>Use a <code class=\"inline\">GestureDetector</code>'s <code class=\"inline\">onHorizontalDragUpdate</code> to read the drag's local position, clamp it to the track's width, and convert it to a 0-1 value which drives the thumb's position and the value reported to the parent—giving full control over the visual design that the built-in <code class=\"inline\">Slider</code> doesn't allow.</p>",
    "tip": "",
    "code": "GestureDetector(\n  onHorizontalDragUpdate: (details) {\n    final box = context.findRenderObject() as RenderBox;\n    final localX = box.globalToLocal(details.globalPosition).dx;\n    final value = (localX / box.size.width).clamp(0.0, 1.0);\n    onChanged(value);\n  },\n);",
    "lang": "dart",
    "revise": "**Custom slider**: `GestureDetector`'s `onHorizontalDragUpdate` reads the local position, **clamp to track width**, convert to a 0–1 value driving the thumb + `onChanged`.\n• **Gotcha**: clamping is essential — an unclamped drag pushes the thumb off the track."
  },
  {
    "id": "fcoding-color-picker",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "color-picker",
      "custom-widget"
    ],
    "question": "Build a simple color picker grid.",
    "answer": "<p>Render a <code class=\"inline\">Wrap</code> or <code class=\"inline\">GridView</code> of predefined color swatches, each a tappable circle that sets the selected color in state and shows a check/border on the currently selected one; expose the selection via an <code class=\"inline\">onColorSelected</code> callback so the parent controls what happens with it.</p>",
    "tip": "",
    "code": "Wrap(children: colors.map((c) => GestureDetector(\n  onTap: () => onSelected(c),\n  child: CircleAvatar(backgroundColor: c, child: c == selected ? const Icon(Icons.check) : null),\n)).toList());",
    "lang": "dart",
    "revise": "**Color picker**: a `Wrap`/`GridView` of tappable swatch circles; tapping sets the selected color + shows a check/border; expose via `onColorSelected`.\n• **Gotcha**: keep it **controlled** (selection + callback passed in) so the parent owns the state."
  },
  {
    "id": "fcoding-multiselect-dropdown",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "multi-select",
      "dropdown"
    ],
    "question": "Build a multi-select dropdown for choosing multiple tags.",
    "answer": "<p>Show current selections as chips, and tapping opens a <code class=\"inline\">showModalBottomSheet</code> or dialog with a checkbox list bound to a <code class=\"inline\">Set&lt;String&gt;</code> of selected values; toggling a checkbox adds/removes from the set, and closing the sheet returns the updated set to the parent.</p>",
    "tip": "",
    "code": "Set<String> selected = {};\n\nvoid toggle(String tag) {\n  setState(() => selected.contains(tag) ? selected.remove(tag) : selected.add(tag));\n}",
    "lang": "dart",
    "revise": "**Multi-select**: show selections as chips; tapping opens a sheet/dialog of checkboxes bound to a **`Set<String>`**; toggling adds/removes, closing returns the set.\n• **Gotcha**: a `Set` (not a `List`) makes toggle membership O(1) and dedupes automatically."
  },
  {
    "id": "fcoding-autocomplete-typeahead",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "autocomplete",
      "typeahead"
    ],
    "question": "Build an autocomplete text field that suggests matches as the user types.",
    "answer": "<p>Flutter's built-in <code class=\"inline\">Autocomplete&lt;T&gt;</code> widget handles the overlay suggestion list, keyboard navigation, and selection callback for you—just provide <code class=\"inline\">optionsBuilder</code> to filter your data source based on the current text value.</p>",
    "tip": "",
    "code": "Autocomplete<String>(\n  optionsBuilder: (textValue) => allCities.where(\n    (city) => city.toLowerCase().contains(textValue.text.toLowerCase()),\n  ),\n  onSelected: (city) => print('Selected: $city'),\n);",
    "lang": "dart",
    "revise": "**Autocomplete**: Flutter's built-in **`Autocomplete<T>`** handles the overlay list, keyboard nav, and selection — just provide `optionsBuilder` to filter your data.\n• **Gotcha**: for async/remote suggestions, debounce inside `optionsBuilder` so you don't query per keystroke."
  },
  {
    "id": "fcoding-masked-phone-input",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "input-formatter",
      "masking"
    ],
    "question": "Format a phone number field as the user types (e.g. (123) 456-7890).",
    "answer": "<p>Attach a custom <code class=\"inline\">TextInputFormatter</code> to the <code class=\"inline\">TextField</code>'s <code class=\"inline\">inputFormatters</code> that strips non-digit characters from the raw input, then re-inserts formatting characters (parentheses, dash) at fixed digit positions, returning a new <code class=\"inline\">TextEditingValue</code> with the cursor repositioned correctly.</p>",
    "tip": "Getting cursor position right after reformatting is the tricky part—test backspacing in the middle of the string, not just typing forward.",
    "code": "",
    "lang": "dart",
    "revise": "**Phone mask**: a custom **`TextInputFormatter`** strips non-digits then re-inserts `()`/`-` at fixed positions, returning a new `TextEditingValue`.\n• **Gotcha**: **getting the cursor position right after reformatting** is the tricky part — test mid-string backspace."
  },
  {
    "id": "fcoding-combined-validator",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "validation",
      "form"
    ],
    "question": "Write a reusable validator that chains multiple rules (required, min length, pattern).",
    "answer": "<p>Compose small validator functions (<code class=\"inline\">String? Function(String?)</code>) into one that runs each in order and returns the first non-null error message, short-circuiting on the first failure—so a single field can combine 'required' + 'min length' + 'format' rules declaratively.</p>",
    "tip": "",
    "code": "String? Function(String?) combine(List<String? Function(String?)> validators) {\n  return (value) {\n    for (final v in validators) {\n      final error = v(value);\n      if (error != null) return error;\n    }\n    return null;\n  };\n}",
    "lang": "dart",
    "revise": "**Chained validator**: compose small `String? Function(String?)` rules into one that runs each in order and **returns the first non-null error** (short-circuits).\n• **Gotcha**: order matters — put 'required' first so blank input fails before format checks run."
  },
  {
    "id": "fcoding-dynamic-form-fields",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "dynamic-form",
      "add-remove-fields"
    ],
    "question": "Build a form where the user can dynamically add or remove fields (e.g. multiple phone numbers).",
    "answer": "<p>Keep a <code class=\"inline\">List</code> of controllers (one per dynamic field) in state. 'Add' appends a new <code class=\"inline\">TextEditingController</code> and rebuilds; 'remove' disposes that specific controller and removes it from the list. Use the list index (or a generated id, not the controller itself) as each row's <code class=\"inline\">Key</code>.</p>",
    "tip": "Dispose every controller when removed individually, and dispose any remaining ones in the widget's own dispose()—leaking controllers here is easy since the list length changes.",
    "code": "List<TextEditingController> controllers = [TextEditingController()];\n\nvoid addField() => setState(() => controllers.add(TextEditingController()));\nvoid removeField(int i) {\n  controllers[i].dispose();\n  setState(() => controllers.removeAt(i));\n}",
    "lang": "dart",
    "revise": "**Dynamic fields**: keep a `List` of controllers; 'Add' appends a controller, 'remove' **disposes that specific one** and drops it; key rows by index/id (not the controller).\n• **Gotcha**: dispose each removed controller AND any remaining ones in the widget's `dispose`."
  },
  {
    "id": "fcoding-download-progress-dio",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "download",
      "dio",
      "progress"
    ],
    "question": "Download a file with Dio and show download progress.",
    "answer": "<p><code class=\"inline\">dio.download(url, savePath, onReceiveProgress: (received, total))</code> streams the file directly to disk while reporting progress, avoiding loading the whole file into memory first—important for large files.</p>",
    "tip": "Guard against total == -1 (some servers don't send Content-Length)—show an indeterminate spinner instead of dividing by -1.",
    "code": "await dio.download(url, savePath, onReceiveProgress: (received, total) {\n  if (total != -1) setState(() => progress = received / total);\n});",
    "lang": "dart",
    "revise": "**Download with progress**: `dio.download(url, savePath, onReceiveProgress: (received, total) => ...)` streams straight to disk (no full-file-in-memory).\n• **Gotcha**: guard against **`total == -1`** (no Content-Length) — show an indeterminate spinner instead of dividing by it."
  },
  {
    "id": "fcoding-image-cache-manager",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "image-cache",
      "cache-manager"
    ],
    "question": "Build a custom disk cache manager for downloaded images.",
    "answer": "<p>On first request, check if a hashed version of the URL exists as a file in the app's cache directory (<code class=\"inline\">path_provider</code>'s <code class=\"inline\">getTemporaryDirectory()</code>); if not, download it and write it to disk before returning the bytes, so subsequent requests read from disk instead of the network.</p>",
    "tip": "In practice, prefer the well-tested cached_network_image/flutter_cache_manager packages over rolling your own—but knowing the underlying mechanism is exactly what this question tests.",
    "code": "",
    "lang": "",
    "revise": "**Disk image cache**: hash the URL → check the temp dir (`path_provider`) for that file; if missing, download + write, then serve from disk next time.\n• **Gotcha**: in practice **prefer `cached_network_image`/`flutter_cache_manager`** over rolling your own."
  },
  {
    "id": "fcoding-paginated-gridview",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "pagination",
      "gridview",
      "infinite-scroll"
    ],
    "question": "Implement infinite-scroll pagination in a GridView.",
    "answer": "<p>Same pattern as an infinite-scroll ListView: attach a <code class=\"inline\">ScrollController</code>, detect nearing <code class=\"inline\">maxScrollExtent</code>, and append the next page's items to the data list—the only difference is the grid uses a <code class=\"inline\">SliverGridDelegate</code> instead of a linear layout, so the same scroll-position logic applies unchanged.</p>",
    "tip": "",
    "code": "",
    "lang": "dart",
    "revise": "**Paginated grid**: identical to infinite-scroll ListView — `ScrollController` detects nearing `maxScrollExtent` and appends the next page; only the `SliverGridDelegate` differs.\n• **Gotcha**: same `isLoading` guard + `mounted` check apply unchanged."
  },
  {
    "id": "fcoding-animatedlist",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "animatedlist",
      "animation"
    ],
    "question": "Build a list where items animate in and out when added/removed (AnimatedList).",
    "answer": "<p><code class=\"inline\">AnimatedList</code> requires a <code class=\"inline\">GlobalKey&lt;AnimatedListState&gt;</code>; instead of calling setState directly, call <code class=\"inline\">listKey.currentState!.insertItem(index)</code> or <code class=\"inline\">removeItem(index, builder)</code>, which drives an animation for exactly that item while keeping the underlying data list in sync manually.</p>",
    "tip": "removeItem's builder must render what the item looked like BEFORE removal (since it's animating out an item no longer in the data list)—a common source of bugs.",
    "code": "final listKey = GlobalKey<AnimatedListState>();\n\nvoid addItem(Item item) {\n  items.add(item);\n  listKey.currentState!.insertItem(items.length - 1);\n}",
    "lang": "dart",
    "revise": "**AnimatedList**: needs a `GlobalKey<AnimatedListState>`; call `insertItem(index)`/`removeItem(index, builder)` instead of `setState`, keeping the data list in sync manually.\n• **Gotcha**: `removeItem`'s builder must render the item **as it looked BEFORE removal** (it's animating out)."
  },
  {
    "id": "fcoding-hero-transition-screens",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "hero",
      "navigation",
      "transition"
    ],
    "question": "Implement a Hero animation from a grid thumbnail to a full-screen detail view.",
    "answer": "<p>Wrap the thumbnail image and the detail screen's full image in matching <code class=\"inline\">Hero(tag: item.id, ...)</code> widgets; when navigating between the two routes via <code class=\"inline\">Navigator.push</code>, Flutter automatically animates the shared element between its two positions/sizes.</p>",
    "tip": "",
    "code": "// Grid tile\nHero(tag: item.id, child: Image.network(item.thumbUrl));\n\n// Detail screen\nHero(tag: item.id, child: Image.network(item.fullUrl));",
    "lang": "dart",
    "revise": "**Hero grid→detail**: wrap both the thumbnail and the full image in `Hero(tag: item.id)`; `Navigator.push` auto-animates the shared element.\n• **Gotcha**: tags must be **unique per screen** — in a grid, tag by item id."
  },
  {
    "id": "fcoding-custom-page-transition",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "page-transition",
      "pageroutebuilder"
    ],
    "question": "Implement a custom page transition animation (e.g. slide-up) using PageRouteBuilder.",
    "answer": "<p><code class=\"inline\">PageRouteBuilder</code> lets you override <code class=\"inline\">transitionsBuilder</code>, wrapping the destination page in an animated widget (like <code class=\"inline\">SlideTransition</code>) driven by the provided <code class=\"inline\">animation</code> controller—giving full control over enter/exit transitions beyond MaterialPageRoute's default.</p>",
    "tip": "",
    "code": "Navigator.push(context, PageRouteBuilder(\n  pageBuilder: (context, animation, secondaryAnimation) => const DetailPage(),\n  transitionsBuilder: (context, animation, secondaryAnimation, child) => SlideTransition(\n    position: Tween(begin: const Offset(0, 1), end: Offset.zero).animate(animation),\n    child: child,\n  ),\n));",
    "lang": "dart",
    "revise": "**Custom transition**: `PageRouteBuilder` overrides `transitionsBuilder`, wrapping the page in e.g. `SlideTransition` driven by the provided animation.\n• **Gotcha**: this is how you go beyond `MaterialPageRoute`'s default for full enter/exit control."
  },
  {
    "id": "fcoding-nested-navigators-tabs",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "nested-navigator",
      "bottom-navigation"
    ],
    "question": "Implement per-tab navigation stacks in a bottom-navigation app (each tab keeps its own back stack).",
    "answer": "<p>Give each tab its own nested <code class=\"inline\">Navigator</code> (with a unique <code class=\"inline\">GlobalKey&lt;NavigatorState&gt;</code>) inside an <code class=\"inline\">IndexedStack</code>, so pushing a detail screen within Tab A doesn't affect Tab B's stack, and switching tabs preserves each one's navigation history independently.</p>",
    "tip": "Override the root PopScope/WillPopScope to pop the active tab's nested Navigator first, and only exit the app if that tab's stack is already at its root.",
    "code": "",
    "lang": "",
    "revise": "**Per-tab back stacks**: give each tab its own nested `Navigator` (unique `GlobalKey<NavigatorState>`) inside an `IndexedStack`, so each tab keeps independent history.\n• **Gotcha**: override root `PopScope` to **pop the active tab's nested Navigator first**, exit the app only when it can't pop."
  },
  {
    "id": "fcoding-deep-link-handler",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "deep-linking",
      "routing"
    ],
    "question": "Handle an incoming deep link and navigate to the corresponding screen.",
    "answer": "<p>Parse the incoming URI's path/query parameters into a route name and arguments, then use the navigator (ideally via go_router's declarative route matching, or a manual <code class=\"inline\">Navigator.pushNamed</code>) to open the matching screen—handling the case where the app is cold-started from the link versus already running.</p>",
    "tip": "Test both cold-start (app not running) and warm (app in background) deep link cases separately—they often take different code paths.",
    "code": "",
    "lang": "",
    "revise": "**Deep link**: parse the URI's path/query into a route + args, then navigate (ideally **go_router** matching, or `pushNamed`).\n• **Gotcha**: handle **cold-start vs warm** launch separately — they take different code paths and both need testing."
  },
  {
    "id": "fcoding-riverpod-async-notifier",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "riverpod",
      "asyncnotifier",
      "state-management"
    ],
    "question": "Implement an async counter/data-fetcher using Riverpod's AsyncNotifier.",
    "answer": "<p><code class=\"inline\">AsyncNotifier&lt;T&gt;</code> exposes <code class=\"inline\">state</code> as an <code class=\"inline\">AsyncValue&lt;T&gt;</code> (data/loading/error) automatically, and its <code class=\"inline\">build()</code> method is where you fetch the initial data. Methods can update state optimistically or re-fetch, and the UI just calls <code class=\"inline\">.when(data:, loading:, error:)</code> on the watched state.</p>",
    "tip": "",
    "code": "class CounterNotifier extends AsyncNotifier<int> {\n  @override\n  Future<int> build() async => 0;\n\n  Future<void> increment() async {\n    final current = state.value ?? 0;\n    state = AsyncValue.data(current + 1);\n  }\n}",
    "lang": "dart",
    "revise": "**AsyncNotifier**: `AsyncNotifier<T>` exposes state as an **`AsyncValue<T>`** (data/loading/error); `build()` fetches initial data; the UI calls **`.when(data:, loading:, error:)`**.\n• **Gotcha**: it handles loading/error state automatically — don't hand-roll your own flags."
  },
  {
    "id": "fcoding-repository-pattern-cache",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "repository-pattern",
      "cache-fallback"
    ],
    "question": "Implement a repository that returns cached data immediately and falls back to it if the network call fails.",
    "answer": "<p>The repository first tries the remote data source; on success, it writes the fresh result to the local cache and returns it. On failure (timeout, no connection), it catches the error and returns whatever is in the local cache instead of propagating the failure, only throwing if the cache is also empty.</p>",
    "tip": "",
    "code": "Future<List<Post>> getPosts() async {\n  try {\n    final posts = await remote.fetchPosts();\n    await local.savePosts(posts);\n    return posts;\n  } catch (_) {\n    final cached = await local.getPosts();\n    if (cached.isNotEmpty) return cached;\n    rethrow;\n  }\n}",
    "lang": "dart",
    "revise": "**Cache-fallback repo**: try remote first → on success write to cache + return; on failure return **cached data** instead of throwing (throw only if the cache is also empty).\n• **Gotcha**: this gives graceful degradation — the UI shows stale data rather than an error on a flaky network."
  },
  {
    "id": "fcoding-debounced-search-canceltoken",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "debounce",
      "canceltoken",
      "dio"
    ],
    "question": "Combine debouncing with request cancellation for a search-as-you-type feature.",
    "answer": "<p>Debounce keystrokes with a Timer as usual, but also cancel the previous in-flight request using a Dio <code class=\"inline\">CancelToken</code> before firing a new one—this prevents a slow earlier response from arriving after a newer one and overwriting the UI with stale results (a race condition debounce alone doesn't fully solve).</p>",
    "tip": "Debounce reduces how often you fire requests; CancelToken protects against the ones that DO fire out of order—both are needed for a fully correct implementation.",
    "code": "CancelToken? _cancelToken;\n\nFuture<void> search(String query) async {\n  _cancelToken?.cancel();\n  _cancelToken = CancelToken();\n  try {\n    final results = await dio.get('/search?q=$query', cancelToken: _cancelToken);\n  } catch (e) {\n    if (e is! DioException || e.type != DioExceptionType.cancel) rethrow;\n  }\n}",
    "lang": "dart",
    "revise": "**Debounce + cancel**: debounce keystrokes with a Timer AND **cancel the previous in-flight request** via a Dio `CancelToken` before firing a new one.\n• **Gotcha**: debounce reduces how often you fire; **CancelToken stops a slow earlier response overwriting a newer one** (the race debounce alone can't fix)."
  },
  {
    "id": "fcoding-throttle-function",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "throttle",
      "algorithm"
    ],
    "question": "Implement a throttle function that limits how often an action can run.",
    "answer": "<p>Unlike debounce (which waits for a pause), <strong>throttle</strong> guarantees the action runs at most once per fixed interval, executing immediately on the first call and then ignoring subsequent calls until the interval elapses—useful for things like limiting scroll-triggered API calls.</p>",
    "tip": "Contrast clearly with debounce when asked: throttle guarantees regular execution during continuous activity; debounce guarantees execution only after activity stops.",
    "code": "void Function() throttle(void Function() action, Duration interval) {\n  DateTime? lastRun;\n  return () {\n    final now = DateTime.now();\n    if (lastRun == null || now.difference(lastRun!) >= interval) {\n      lastRun = now;\n      action();\n    }\n  };\n}",
    "lang": "dart",
    "revise": "**Throttle**: runs the action **at most once per interval** — immediately on the first call, ignoring the rest until the interval elapses.\n• **Gotcha**: contrast with debounce — **throttle guarantees regular execution during continuous activity**; debounce waits for a pause."
  },
  {
    "id": "fcoding-lru-cache",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "lru-cache",
      "algorithm",
      "linkedhashmap"
    ],
    "question": "Implement an LRU (Least Recently Used) cache in Dart.",
    "answer": "<p>A <code class=\"inline\">LinkedHashMap</code> preserves insertion order and lets you re-insert a key to move it to the end (marking it 'most recently used'). On <code class=\"inline\">get</code>, remove and re-add the key to refresh its position; on <code class=\"inline\">put</code>, evict the oldest entry (the map's first key) once capacity is exceeded. Both operations run in O(1).</p>",
    "tip": "",
    "code": "class LRUCache<K, V> {\n  final int capacity;\n  final _map = <K, V>{};\n  LRUCache(this.capacity);\n\n  V? get(K key) {\n    if (!_map.containsKey(key)) return null;\n    final value = _map.remove(key) as V;\n    _map[key] = value;\n    return value;\n  }\n\n  void put(K key, V value) {\n    _map.remove(key);\n    _map[key] = value;\n    if (_map.length > capacity) _map.remove(_map.keys.first);\n  }\n}",
    "lang": "dart",
    "revise": "**LRU cache**: a `LinkedHashMap` preserves order; on `get`, remove + re-add the key (mark most-recent); on `put` over capacity, evict the **first (oldest)** key — all **O(1)**.\n• **Gotcha**: the trick is re-inserting on access to refresh recency order."
  },
  {
    "id": "fcoding-binary-search",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "binary-search"
    ],
    "question": "Implement binary search on a sorted list in Dart.",
    "answer": "<p>Binary search repeatedly halves the search range by comparing the middle element to the target, running in <strong>O(log n)</strong> time versus O(n) for a linear scan—but it requires the input list to already be sorted.</p>",
    "tip": "State the precondition out loud: binary search only works on sorted data—an easy point interviewers check for.",
    "code": "int binarySearch(List<int> sorted, int target) {\n  var low = 0, high = sorted.length - 1;\n  while (low <= high) {\n    final mid = low + (high - low) ~/ 2;\n    if (sorted[mid] == target) return mid;\n    if (sorted[mid] < target) { low = mid + 1; } else { high = mid - 1; }\n  }\n  return -1;\n}",
    "lang": "dart",
    "revise": "**Binary search**: halve the range each step by comparing the middle to the target — **O(log n)** vs O(n) linear.\n• **Gotcha**: **state the precondition out loud — the list must be sorted** — an easy point interviewers check."
  },
  {
    "id": "fcoding-merge-sort",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "algorithm",
      "merge-sort",
      "recursion"
    ],
    "question": "Implement merge sort in Dart.",
    "answer": "<p>Merge sort recursively splits the list in half until sublists have one element, then merges sorted sublists back together in order. It runs in guaranteed <strong>O(n log n)</strong> time (unlike quicksort's worst-case O(n²)) but uses O(n) extra space for the merge step.</p>",
    "tip": "",
    "code": "List<int> mergeSort(List<int> list) {\n  if (list.length <= 1) return list;\n  final mid = list.length ~/ 2;\n  final left = mergeSort(list.sublist(0, mid));\n  final right = mergeSort(list.sublist(mid));\n  final result = <int>[];\n  var i = 0, j = 0;\n  while (i < left.length && j < right.length) {\n    result.add(left[i] <= right[j] ? left[i++] : right[j++]);\n  }\n  result.addAll(left.skip(i));\n  result.addAll(right.skip(j));\n  return result;\n}",
    "lang": "dart",
    "revise": "**Merge sort**: recursively split to single elements, then **merge sorted halves** back together — **guaranteed O(n log n)**, **O(n) extra space**.\n• **Gotcha**: unlike quicksort it has no O(n²) worst case, but it isn't in-place."
  },
  {
    "id": "fcoding-quick-sort",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "algorithm",
      "quick-sort",
      "recursion"
    ],
    "question": "Implement quicksort in Dart.",
    "answer": "<p>Quicksort picks a pivot, partitions the list into elements less than and greater than the pivot, then recursively sorts each partition. Average time is <strong>O(n log n)</strong>, but a poor pivot choice on already-sorted input can degrade to O(n²).</p>",
    "tip": "Mention that choosing a random or median pivot mitigates the worst-case scenario on already-sorted or adversarial input.",
    "code": "List<int> quickSort(List<int> list) {\n  if (list.length <= 1) return list;\n  final pivot = list[list.length ~/ 2];\n  final less = list.where((x) => x < pivot).toList();\n  final equal = list.where((x) => x == pivot).toList();\n  final greater = list.where((x) => x > pivot).toList();\n  return [...quickSort(less), ...equal, ...quickSort(greater)];\n}",
    "lang": "dart",
    "revise": "**Quicksort**: pick a pivot, partition into <pivot / >pivot, recurse — **average O(n log n)**, worst **O(n²)** on bad pivots.\n• **Gotcha**: a **random/median pivot** mitigates the worst case on already-sorted/adversarial input."
  },
  {
    "id": "fcoding-linked-list-impl",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "linked-list",
      "data-structures"
    ],
    "question": "Implement a singly linked list in Dart.",
    "answer": "<p>A linked list is a chain of nodes, each holding a value and a reference to the next node, with a <code class=\"inline\">head</code> pointer to the first one. Insertion at the head is O(1); appending at the tail or searching by value is O(n) without a tracked tail pointer.</p>",
    "tip": "",
    "code": "class Node<T> {\n  T value;\n  Node<T>? next;\n  Node(this.value);\n}\n\nclass LinkedList<T> {\n  Node<T>? head;\n  void addFirst(T value) {\n    final node = Node(value)..next = head;\n    head = node;\n  }\n}",
    "lang": "dart",
    "revise": "**Singly linked list**: nodes of value + `next`, with a head pointer. Head insert = **O(1)**; tail append/search = **O(n)** without a tail pointer.\n• **Gotcha**: track a tail pointer if you append often, to make it O(1)."
  },
  {
    "id": "fcoding-reverse-linked-list",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "linked-list",
      "algorithm",
      "reverse"
    ],
    "question": "Write a function to reverse a singly linked list in Dart.",
    "answer": "<p>Walk the list once, keeping track of the previous node; for each node, redirect its <code class=\"inline\">next</code> pointer to the previous node before moving forward—reversing all links in a single <strong>O(n)</strong> pass with O(1) extra space.</p>",
    "tip": "",
    "code": "Node<T>? reverse<T>(Node<T>? head) {\n  Node<T>? prev;\n  var current = head;\n  while (current != null) {\n    final next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}",
    "lang": "dart",
    "revise": "**Reverse a list**: walk once keeping a `previous`; redirect each node's `next` to `previous` before advancing — **O(n) time, O(1) space**.\n• **Gotcha**: save `node.next` before you overwrite it, or you lose the rest of the list."
  },
  {
    "id": "fcoding-stack-impl",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "stack",
      "data-structures"
    ],
    "question": "Implement a Stack (LIFO) data structure in Dart.",
    "answer": "<p>A stack can be implemented directly on top of a Dart <code class=\"inline\">List</code>, using <code class=\"inline\">add()</code> for push and <code class=\"inline\">removeLast()</code> for pop—both O(1) operations since they only touch the end of the underlying array.</p>",
    "tip": "",
    "code": "class Stack<T> {\n  final _items = <T>[];\n  void push(T item) => _items.add(item);\n  T pop() => _items.removeLast();\n  T get peek => _items.last;\n  bool get isEmpty => _items.isEmpty;\n}",
    "lang": "dart",
    "revise": "**Stack (LIFO)**: build on a Dart `List` — `add()` for push, `removeLast()` for pop, both **O(1)** (only touch the end).\n• **Gotcha**: check `isEmpty` before `pop`/`peek` to avoid a range error."
  },
  {
    "id": "fcoding-queue-two-stacks",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "queue",
      "stack",
      "algorithm"
    ],
    "question": "Implement a Queue (FIFO) using two Stacks.",
    "answer": "<p>Push new elements onto an 'in' stack. To dequeue, if the 'out' stack is empty, pop everything from 'in' and push it onto 'out' (reversing the order), then pop from 'out'. Each element moves between stacks at most once, giving <strong>amortized O(1)</strong> per operation overall.</p>",
    "tip": "A classic whiteboard question testing amortized complexity reasoning, not just syntax.",
    "code": "class QueueFromStacks<T> {\n  final _inStack = <T>[];\n  final _outStack = <T>[];\n\n  void enqueue(T item) => _inStack.add(item);\n\n  T dequeue() {\n    if (_outStack.isEmpty) {\n      while (_inStack.isNotEmpty) _outStack.add(_inStack.removeLast());\n    }\n    return _outStack.removeLast();\n  }\n}",
    "lang": "dart",
    "revise": "**Queue from 2 stacks**: push onto 'in'; to dequeue, if 'out' is empty, drain 'in'→'out' (reversing), then pop 'out' — **amortized O(1)**.\n• **Gotcha**: a classic whiteboard test of **amortized complexity reasoning**, not syntax."
  },
  {
    "id": "fcoding-bst-impl",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "binary-search-tree",
      "data-structures"
    ],
    "question": "Implement insertion and search for a Binary Search Tree in Dart.",
    "answer": "<p>Each node holds a value and left/right child references. Insertion recursively compares the new value to the current node, going left if smaller and right if larger, until it finds an empty spot. Search follows the same comparison logic. Both run in <strong>O(log n)</strong> on a balanced tree, but degrade to O(n) on a skewed one.</p>",
    "tip": "Mention the worst case explicitly—a BST built from already-sorted input degenerates into a linked list with O(n) operations, which is why self-balancing trees (AVL/Red-Black) exist.",
    "code": "class BSTNode {\n  int value;\n  BSTNode? left, right;\n  BSTNode(this.value);\n\n  void insert(int newValue) {\n    if (newValue < value) {\n      left == null ? left = BSTNode(newValue) : left!.insert(newValue);\n    } else {\n      right == null ? right = BSTNode(newValue) : right!.insert(newValue);\n    }\n  }\n}",
    "lang": "dart",
    "revise": "**BST insert/search**: compare to the node, go left if smaller / right if larger; **O(log n)** balanced, **O(n)** degenerate.\n• **Gotcha**: name the worst case explicitly — a BST built from **already-sorted input degenerates into a linked list**."
  },
  {
    "id": "fcoding-detect-duplicates",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "duplicates",
      "set"
    ],
    "question": "Write a function to detect if a list contains any duplicate elements.",
    "answer": "<p>Insert each element into a <code class=\"inline\">Set</code> while iterating; if an element is already in the set, a duplicate exists. This runs in <strong>O(n)</strong> time versus O(n²) for comparing every pair.</p>",
    "tip": "",
    "code": "bool hasDuplicates(List<int> list) {\n  final seen = <int>{};\n  for (final item in list) {\n    if (!seen.add(item)) return true; // add() returns false if already present\n  }\n  return false;\n}",
    "lang": "dart",
    "revise": "**Detect duplicates**: insert each element into a `Set`; if already present, a duplicate exists — **O(n)** vs O(n²) pairwise.\n• **Gotcha**: the Set membership check is what buys the linear time — the space trade-off is worth naming."
  },
  {
    "id": "fcoding-max-subarray-kadane",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "algorithm",
      "kadane",
      "dynamic-programming"
    ],
    "question": "Implement Kadane's algorithm to find the maximum sum of a contiguous subarray.",
    "answer": "<p>Track a running sum that resets to the current element whenever including previous elements would make it smaller than starting fresh, and track the best sum seen so far. This solves the problem in a single <strong>O(n)</strong> pass, versus O(n²) for checking every subarray.</p>",
    "tip": "",
    "code": "int maxSubArray(List<int> nums) {\n  var currentSum = nums[0];\n  var maxSum = nums[0];\n  for (var i = 1; i < nums.length; i++) {\n    currentSum = [nums[i], currentSum + nums[i]].reduce((a, b) => a > b ? a : b);\n    maxSum = [maxSum, currentSum].reduce((a, b) => a > b ? a : b);\n  }\n  return maxSum;\n}",
    "lang": "dart",
    "revise": "**Kadane's algorithm**: keep a running sum that **resets to the current element** when carrying the previous sum would hurt, tracking the best seen — single **O(n)** pass.\n• **Gotcha**: it beats the O(n²) all-subarrays approach — the reset condition is the whole trick."
  },
  {
    "id": "fcoding-balanced-parentheses",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "stack",
      "parentheses"
    ],
    "question": "Write a function to check if a string has balanced parentheses/brackets.",
    "answer": "<p>Push every opening bracket onto a stack; on a closing bracket, check that it matches the type on top of the stack (pop it if so, otherwise the string is unbalanced). At the end, the stack must be empty for the string to be fully balanced.</p>",
    "tip": "Don't forget to check the stack is empty at the end—a string like '(()' passes every individual character check but has an unclosed bracket.",
    "code": "bool isBalanced(String s) {\n  final stack = <String>[];\n  final pairs = {')': '(', ']': '[', '}': '{'};\n  for (final char in s.split('')) {\n    if ('([{'.contains(char)) {\n      stack.add(char);\n    } else if (pairs.containsKey(char)) {\n      if (stack.isEmpty || stack.removeLast() != pairs[char]) return false;\n    }\n  }\n  return stack.isEmpty;\n}",
    "lang": "dart",
    "revise": "**Balanced brackets**: push openers on a stack; on a closer, check it **matches the top** (pop) else unbalanced.\n• **Gotcha**: the stack **must be empty at the end** — `'(()'` passes every char check but is still unbalanced."
  },
  {
    "id": "fcoding-anagram-check",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "anagram",
      "string"
    ],
    "question": "Write a function to check if two strings are anagrams of each other.",
    "answer": "<p>Two strings are anagrams if they contain exactly the same characters with the same frequencies. Sort both strings' characters and compare (<strong>O(n log n)</strong>), or count character frequencies with a Map and compare counts (<strong>O(n)</strong>, faster for longer strings).</p>",
    "tip": "Mention both approaches and their complexity trade-off—shows you can pick the right tool, not just one memorized solution.",
    "code": "bool isAnagram(String a, String b) {\n  final sortedA = a.toLowerCase().split('')..sort();\n  final sortedB = b.toLowerCase().split('')..sort();\n  return sortedA.join() == sortedB.join();\n}",
    "lang": "dart",
    "revise": "**Anagram**: same characters, same frequencies. **Sort + compare** (O(n log n)) or **count with a Map** (O(n), faster for long strings).\n• **Gotcha**: mention **both approaches + their trade-off** — shows you pick the right tool."
  },
  {
    "id": "fcoding-fizzbuzz",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "fizzbuzz"
    ],
    "question": "Write FizzBuzz in Dart (print Fizz/Buzz/FizzBuzz for multiples of 3/5/both from 1 to n).",
    "answer": "<p>Check divisibility by 15 (3 and 5) first, since checking 3 and 5 separately in the wrong order would print 'Fizz' or 'Buzz' instead of 'FizzBuzz' for multiples of both.</p>",
    "tip": "A trivial problem, but interviewers watch for the divisible-by-15-first ordering mistake as a basic sanity/attention-to-detail check.",
    "code": "for (var i = 1; i <= n; i++) {\n  if (i % 15 == 0) {\n    print('FizzBuzz');\n  } else if (i % 3 == 0) {\n    print('Fizz');\n  } else if (i % 5 == 0) {\n    print('Buzz');\n  } else {\n    print(i);\n  }\n}",
    "lang": "dart",
    "revise": "**FizzBuzz**: check divisibility by **15 first**, then 3, then 5.\n• **Gotcha**: checking 3/5 before 15 prints 'Fizz'/'Buzz' instead of 'FizzBuzz' — interviewers watch for this ordering slip as a basic sanity check."
  },
  {
    "id": "fcoding-missing-number",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "missing-number",
      "math"
    ],
    "question": "Given a list containing n distinct numbers from 0 to n, find the missing one.",
    "answer": "<p>Compute the expected sum of 0..n using the formula <code class=\"inline\">n*(n+1)/2</code>, then subtract the actual sum of the given list—the difference is the missing number. This runs in <strong>O(n)</strong> time and O(1) space, avoiding a sort or a hash set.</p>",
    "tip": "",
    "code": "int findMissing(List<int> nums) {\n  final n = nums.length;\n  final expectedSum = n * (n + 1) ~/ 2;\n  final actualSum = nums.fold(0, (a, b) => a + b);\n  return expectedSum - actualSum;\n}",
    "lang": "dart",
    "revise": "**Missing number (0..n)**: expected sum `n*(n+1)/2` minus the actual sum = the missing one — **O(n) time, O(1) space**, no sort/set.\n• **Gotcha**: the formula approach beats hashing here — cleaner and constant space."
  },
  {
    "id": "fcoding-merge-sorted-lists",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "merge",
      "two-pointers"
    ],
    "question": "Merge two already-sorted lists into one sorted list.",
    "answer": "<p>Use two pointers, one per list, repeatedly picking the smaller of the two current elements and advancing that pointer, then append whatever remains from the list that isn't exhausted yet. This is the same merge step used inside merge sort, running in <strong>O(n + m)</strong> time.</p>",
    "tip": "",
    "code": "List<int> mergeSorted(List<int> a, List<int> b) {\n  final result = <int>[];\n  var i = 0, j = 0;\n  while (i < a.length && j < b.length) {\n    result.add(a[i] <= b[j] ? a[i++] : b[j++]);\n  }\n  result.addAll(a.skip(i));\n  result.addAll(b.skip(j));\n  return result;\n}",
    "lang": "dart",
    "revise": "**Merge two sorted lists**: two pointers, repeatedly take the **smaller current** element and advance, then append the leftover tail — **O(n + m)**.\n• **Gotcha**: this is the exact merge step inside merge sort — worth naming the connection."
  },
  {
    "id": "fcoding-deep-clone-nested",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "deep-clone",
      "recursion",
      "map",
      "list"
    ],
    "question": "Write a function to deep clone a nested Map/List structure of primitives.",
    "answer": "<p>Recursively check each value's runtime type: if it's a <code class=\"inline\">Map</code>, clone each entry recursively into a new map; if it's a <code class=\"inline\">List</code>, clone each element recursively into a new list; otherwise (a primitive), copy it directly since primitives are already immutable.</p>",
    "tip": "",
    "code": "dynamic deepClone(dynamic value) {\n  if (value is Map) {\n    return value.map((k, v) => MapEntry(k, deepClone(v)));\n  } else if (value is List) {\n    return value.map(deepClone).toList();\n  }\n  return value;\n}",
    "lang": "dart",
    "revise": "**Deep clone**: recurse on runtime type — `Map` → clone each entry, `List` → clone each element, else copy the primitive directly.\n• **Gotcha**: primitives are immutable so a direct copy is safe; only containers need recursion."
  },
  {
    "id": "fcoding-deep-equality-check",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "deep-equality",
      "recursion"
    ],
    "question": "Write a function that deeply compares two nested Lists/Maps for equality.",
    "answer": "<p>Dart's default <code class=\"inline\">==</code> on List/Map is identity-based (or only shallow via <code class=\"inline\">ListEquality</code>/<code class=\"inline\">MapEquality</code> from <code class=\"inline\">package:collection</code>), so a custom deep check must recursively compare each nested structure, handling type mismatches, differing lengths/key sets, and primitive comparisons as base cases.</p>",
    "tip": "Mention package:collection's DeepCollectionEquality first—reinventing this from scratch is rarely necessary in production code, but shows you understand what it does under the hood.",
    "code": "bool deepEquals(dynamic a, dynamic b) {\n  if (a is Map && b is Map) {\n    if (a.length != b.length) return false;\n    return a.keys.every((k) => b.containsKey(k) && deepEquals(a[k], b[k]));\n  }\n  if (a is List && b is List) {\n    if (a.length != b.length) return false;\n    return List.generate(a.length, (i) => i).every((i) => deepEquals(a[i], b[i]));\n  }\n  return a == b;\n}",
    "lang": "dart",
    "revise": "**Deep equality**: Dart's `==` on List/Map is identity-based, so recurse — compare types, lengths/key-sets, and primitives as base cases.\n• **Gotcha**: **mention `package:collection`'s `DeepCollectionEquality` first** — rolling your own is rarely necessary."
  },
  {
    "id": "fcoding-producer-consumer-streams",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "producer-consumer",
      "streams",
      "concurrency"
    ],
    "question": "Simulate a producer-consumer pattern using a Dart Stream.",
    "answer": "<p>A <code class=\"inline\">StreamController</code> acts as the shared buffer: a 'producer' calls <code class=\"inline\">controller.sink.add(item)</code> as items become available, and a 'consumer' calls <code class=\"inline\">controller.stream.listen((item) => process(item))</code>—Dart's event loop naturally serializes delivery, so no manual locking is needed like in a multi-threaded language.</p>",
    "tip": "",
    "code": "final controller = StreamController<int>();\n\ncontroller.stream.listen((item) => print('Consumed: $item'));\n\nfor (var i = 0; i < 5; i++) {\n  controller.sink.add(i); // produce\n}\ncontroller.close();",
    "lang": "dart",
    "revise": "**Producer-consumer**: a `StreamController` is the shared buffer — the producer calls `sink.add(item)`, the consumer `stream.listen(...)`; the event loop serializes delivery (no locks).\n• **Gotcha**: Dart's single-threaded model means **no manual locking** unlike multi-threaded languages."
  },
  {
    "id": "fcoding-pubsub-event-bus",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "event-bus",
      "pub-sub",
      "streams"
    ],
    "question": "Implement a simple type-safe event bus (pub/sub) using Streams.",
    "answer": "<p>A broadcast <code class=\"inline\">StreamController</code> lets multiple independent listeners subscribe to the same event stream. Publishing calls <code class=\"inline\">add(event)</code>; subscribing filters by event type using <code class=\"inline\">stream.where((e) => e is T)</code>—giving a decoupled way for distant parts of the app to communicate without direct references to each other.</p>",
    "tip": "An event bus is convenient but can make data flow hard to trace in a large app—mention that Bloc/Riverpod are usually a better-structured alternative for anything beyond simple cross-cutting notifications.",
    "code": "class EventBus {\n  final _controller = StreamController<dynamic>.broadcast();\n  void fire(dynamic event) => _controller.add(event);\n  Stream<T> on<T>() => _controller.stream.where((e) => e is T).cast<T>();\n}",
    "lang": "dart",
    "revise": "**Event bus**: a **broadcast** `StreamController` lets many listeners subscribe; publish with `add(event)`, subscribe with `stream.where((e) => e is T)`.\n• **Gotcha**: convenient but makes data flow **hard to trace** at scale — note Bloc/Riverpod are usually better."
  },
  {
    "id": "fcoding-getx-reactive-counter",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "getx",
      "reactive-state"
    ],
    "question": "Implement a reactive counter using GetX.",
    "answer": "<p>GetX's <code class=\"inline\">.obs</code> extension turns a value into an observable (<code class=\"inline\">Rx</code>) type; wrapping UI in <code class=\"inline\">Obx(() => ...)</code> automatically rebuilds whenever any observable it reads changes—no BuildContext, Provider, or StreamBuilder boilerplate required.</p>",
    "tip": "",
    "code": "class CounterController extends GetxController {\n  var count = 0.obs;\n  void increment() => count++;\n}\n\n// UI\nObx(() => Text('${controller.count}'));",
    "lang": "dart",
    "revise": "**GetX counter**: `.obs` makes a value observable (`Rx`); wrap UI in **`Obx(() => ...)`** to auto-rebuild when any observable it reads changes — no BuildContext/Provider.\n• **Gotcha**: `Obx` only tracks observables **actually read inside** its builder."
  },
  {
    "id": "fcoding-state-restoration",
    "category": "fluttercoding",
    "difficulty": "advanced",
    "tags": [
      "state-restoration",
      "restorationmixin"
    ],
    "question": "Implement state restoration so a form survives the app being killed by the OS.",
    "answer": "<p>Mix in <code class=\"inline\">RestorationMixin</code>, provide a unique <code class=\"inline\">restorationId</code>, and register each piece of state that must survive process death (e.g. a <code class=\"inline\">RestorableTextEditingController</code>) inside <code class=\"inline\">restoreState</code> using <code class=\"inline\">registerForRestoration</code>—the OS-provided restoration bucket persists these values so Android/iOS can restore the exact UI after the app is killed in the background and relaunched.</p>",
    "tip": "This is different from simply persisting to shared_preferences—it specifically restores the in-progress UI state after an OS-initiated kill, matching native platform expectations.",
    "code": "",
    "lang": "",
    "revise": "**State restoration**: mix in `RestorationMixin`, set a `restorationId`, register survivable state (e.g. `RestorableTextEditingController`) in `restoreState`; the OS bucket persists it across process death.\n• **Gotcha**: this is **different from shared_preferences** — it restores in-progress UI state after the OS kills the app."
  },
  {
    "id": "fcoding-word-frequency-counter",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "word-frequency",
      "map"
    ],
    "question": "Write a function that counts the frequency of each word in a string.",
    "answer": "<p>Split the string on whitespace, normalize case, and use a <code class=\"inline\">Map&lt;String, int&gt;</code> with <code class=\"inline\">update(word, (v) => v + 1, ifAbsent: () => 1)</code> to tally occurrences in a single <strong>O(n)</strong> pass.</p>",
    "tip": "",
    "code": "Map<String, int> wordFrequency(String text) {\n  final counts = <String, int>{};\n  for (final word in text.toLowerCase().split(RegExp(r'\\s+'))) {\n    if (word.isEmpty) continue;\n    counts.update(word, (v) => v + 1, ifAbsent: () => 1);\n  }\n  return counts;\n}",
    "lang": "dart",
    "revise": "**Word frequency**: split on whitespace, normalize case, tally with `Map` **`update(w, (v)=>v+1, ifAbsent: ()=>1)`** — single **O(n)** pass.\n• **Gotcha**: `update` with `ifAbsent` is the clean increment-or-initialize idiom."
  },
  {
    "id": "fcoding-chunk-list",
    "category": "fluttercoding",
    "difficulty": "beginner",
    "tags": [
      "algorithm",
      "list",
      "chunking"
    ],
    "question": "Write a function that splits a list into chunks of a given size.",
    "answer": "<p>Iterate in steps of <code class=\"inline\">size</code>, slicing out a sublist at each step using <code class=\"inline\">sublist(start, end)</code>, clamping the end index to the list's length for the final (possibly shorter) chunk. Runs in <strong>O(n)</strong> overall.</p>",
    "tip": "A practical real use case: splitting a large list of items into pages for a paginated UI, or batching API requests to stay under a payload size limit.",
    "code": "List<List<T>> chunk<T>(List<T> list, int size) {\n  final chunks = <List<T>>[];\n  for (var i = 0; i < list.length; i += size) {\n    chunks.add(list.sublist(i, i + size > list.length ? list.length : i + size));\n  }\n  return chunks;\n}",
    "lang": "dart",
    "revise": "**Chunk a list**: iterate in steps of `size`, slicing with `sublist(start, end)`, **clamping `end` to length** for the final (shorter) chunk — **O(n)**.\n• **Gotcha**: real use — paginating a large list into pages or batching API requests."
  },
  {
    "id": "fcoding-first-non-repeating-char",
    "category": "fluttercoding",
    "difficulty": "intermediate",
    "tags": [
      "algorithm",
      "string",
      "hashmap"
    ],
    "question": "Write a function to find the first non-repeating character in a string.",
    "answer": "<p>Make one pass to build a character-frequency <code class=\"inline\">Map</code>, then a second pass over the original string (to preserve order) returning the first character whose count is exactly 1. This runs in <strong>O(n)</strong> time with two linear passes rather than an O(n²) nested-loop comparison.</p>",
    "tip": "Iterating the original string again (not the map, which has no guaranteed order relative to first occurrence) is what preserves correct left-to-right order.",
    "code": "String? firstNonRepeating(String s) {\n  final counts = <String, int>{};\n  for (final c in s.split('')) {\n    counts.update(c, (v) => v + 1, ifAbsent: () => 1);\n  }\n  for (final c in s.split('')) {\n    if (counts[c] == 1) return c;\n  }\n  return null;\n}",
    "lang": "dart",
    "revise": "**First non-repeating char**: pass 1 builds a frequency `Map`; **pass 2 over the original string** returns the first char with count 1 — **O(n)** two passes.\n• **Gotcha**: iterate the **original string** (not the map) in pass 2 to preserve first-occurrence order."
  }
]
